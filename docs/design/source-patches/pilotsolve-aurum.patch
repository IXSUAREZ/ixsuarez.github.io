--- a/AGENTS.md
+++ b/AGENTS.md
@@ -22,7 +22,7 @@
 - Preserve the mobile device runtime unless the user's task explicitly asks otherwise. Do not replace it with a standalone page. Visual fidelity applies to app-owned content inside the device screen, not to template-owned device chrome.
 - Keep `App` composed around `PhoneFrame` -> `KeyboardProvider`, with `StatusBar`, app content, `HomeIndicator`, and `KeyboardDock` mounted inside the phone frame. `StatusBar` and the iOS home indicator are overlaid device chrome. When the Android keyboard is closed, the app viewport reserves the protected navigation-bar region instead of painting behind it. When the Android keyboard is open, preserve the current full-screen keyboard layout: its asset includes the IME navigation strip and the separate black navigation bar is hidden. iOS screens continue to paint behind the home-indicator area and own their safe-area content padding.
 - Preserve the `iPhone` / `Pixel 10` device picker and both calibrated device presets. The Pixel screen is `427 x 952`; its `32 x 32` camera circle and `public/assets/android/navigation-bar.svg` bottom navigation bar are protected device chrome, not app content.
-- Preserve the device picker's intentionally lightweight Codex styling in the top-right corner: its trigger wrapper is borderless and transparent, its trigger sizes to content, and its right-aligned menu uses the compact 3px inset plus the specified hairline and elevation shadow layers. Keep the prototype root and default app screen white.
+- Preserve the device picker's intentionally lightweight Codex styling in the top-right corner: its trigger wrapper is borderless and transparent, its trigger sizes to content, and its right-aligned menu uses the compact 3px inset plus the specified hairline and elevation shadow layers. The app canvas follows its selected Day/Night appearance; device chrome remains runtime-owned.
 - Preserve `StatusBar` as live device chrome, including its platform-specific typography, source status-icon assets, and spacing. Pixel 10 uses Roboto, Android indicators, and 32px top, left, and right padding. iPhone uses its iOS indicators, system typography, and calibrated spacing. Do not hardcode screenshot times like `9:41` into the status bar, replace its real-time clock, or move status bar content into app markup unless the user explicitly asks for a fixed/mock device time.
 - `PhoneFrame` owns the calibrated device frame, screen portal, device picker, camera cutout, and custom cursor. Keep device assets in `public/assets/iphone/` and `public/assets/android/`; if an asset fails to load, repair the asset path or restore the asset instead of removing the frame, keyboard, or image render.
 - Use `MobileScroll` directly for simple single-screen prototypes. Use `FlowStack` for conventional multi-screen flows whose routes can own their fixed header and footer; when using it, define each route as a `FlowScreen`: `{ id, header?, headerHeight?, footer?, footerHeight?, render }`, and use `flow.push(screen)`, `flow.pop()`, and `flow.replace(screen)` from `FlowStack` render callbacks or `useFlow()` instead of introducing another router.
@@ -102,3 +102,11 @@
 - Night remains the default for new installs; Settings offers Day, Night, and Auto and retains explicit saved choices.
 
 - Unit discoverability: a compact split control pairs the unit + swap icon (tap to cycle) with a separate chevron (tap for all choices). Hold remains an optional shortcut with a subtle progress underline. Both targets stay at least 44px; focus returns to the activating control.
+
+## PilotSolve appearance update — Polished Aurum, September 24, 2026
+
+- This direction supersedes the prior cyan/blue avionics palette and fresh-install Night default. Use the deployed site's Polished Aurum tokens (`--g-*`) as the appearance authority, with matching local fallbacks for standalone use. Day canvas is `#E3E3E3`; Night canvas is `#414141`, with layered gray surfaces and reflective metallic yellow-gold accents.
+- Appearance choices are Day, Night, and System; new installs default to System. Preserve an explicitly saved Day or Night choice and all existing application data. System follows the device appearance.
+- Apply the shared `data-gold-mode` (`day`/`dark`) and `data-suarez-theme` (`light`/`dark`) root attributes when PilotSolve owns the page. App styles must consume shared tokens with standalone fallbacks and must not override host-wide tokens.
+- Primary and selected controls use the metallic gold gradient; secondary controls use neutral raised hardware; text actions remain quiet. Keep semantic warning, error, and success colors meaningful. Honor reduced motion and reduced transparency.
+- Keep all calculations, risk meaning, diagrams, navigation, layout behavior, offline operation, and attribution intact. The phone/mobile runtime remains protected; app-owned changes belong in `src/Prototype.tsx`, `src/PilotApp.tsx`, `src/prototype.css`, and app state modules only.
--- a/scripts/build-offline.mjs
+++ b/scripts/build-offline.mjs
@@ -2,16 +2,17 @@
 import {createHash} from 'node:crypto';
 import {resolve} from 'node:path';
 const root='dist/client';
+const siteRoot=resolve(process.env.SUAREZ_SITE_ROOT||resolve('..','..','site'));
 await copyFile(root+'/mobile.html',root+'/index.html');
 const names=(await readdir(root+'/assets')).filter(x=>!x.startsWith('.')&&/\.(js|css|woff2)$/.test(x));
 const shared=['/assets/appearance.js','/assets/avionics.css','/assets/tactile.js'];
 const files=['./index.html','./mobile.html','./manifest.webmanifest','./icon.svg','./icon-48.png','./apple-touch-icon.png','./icon-192.png','./icon-512.png',...names.map(n=>'./assets/'+n),...shared];
-const hash=createHash('sha256');for(const f of files)hash.update(await readFile(f.startsWith('/')?resolve('..','..','site',f.slice(1)):root+'/'+f));
-const version='pilotsolve-'+hash.digest('hex').slice(0,12);
-const worker=`const CACHE=${JSON.stringify(version)};const FILES=${JSON.stringify(files)};
-self.addEventListener('install',event=>{event.waitUntil(caches.open(CACHE).then(c=>c.addAll(FILES)).then(()=>self.skipWaiting()))});
-self.addEventListener('activate',event=>{event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k.startsWith('pilotsolve-')&&k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim()))});
-self.addEventListener('fetch',event=>{if(event.request.method!=='GET'||new URL(event.request.url).origin!==self.location.origin)return;event.respondWith(caches.match(event.request,{ignoreVary:true}).then(cached=>cached||fetch(event.request).catch(()=>event.request.mode==='navigate'?caches.match('./mobile.html'):Response.error())))});
-`;
+const hash=createHash('sha256');for(const f of files)hash.update(await readFile(f.startsWith('/')?resolve(siteRoot,f.slice(1)):root+'/'+f));
+// The host owns the shared-asset/offline update policy; retain it on rebuild.
+const hostWorker=await readFile(resolve(siteRoot,'pilotsolve/sw.js'),'utf8');
+hash.update(hostWorker.replace(/pilotsolve-site-[a-f0-9]+/g,'pilotsolve-site-VERSION'));
+const version='pilotsolve-site-'+hash.digest('hex').slice(0,12);
+const worker=hostWorker.replace(/const CACHE=.*?;const FILES=\[.*?\];/,
+ `const CACHE=${JSON.stringify(version)};const FILES=${JSON.stringify(files)};`);
 await writeFile(root+'/sw.js',worker);console.log('Offline app prepared:',files.length,'files',version);
 await writeFile(root+'/BUILD.json',JSON.stringify({app:'PilotSolve',cache:version,preparedAt:new Date().toISOString(),route:'/pilotsolve/',files},null,2)+'\n');
--- a/src/PilotApp.tsx
+++ b/src/PilotApp.tsx
@@ -12,6 +12,7 @@
 import type {Tool,Field,Dimension} from './engine/types';
 import {initialState,freshSession,isAppState,categories,type AppState,type Draft,type SavedCalculation} from './state';
 import {readSaved,writeSaved} from './features/storage';
+import {resolveAppearancePreference,type AppearancePreference} from './features/appearance';
 import {carryCalculatedOutputs} from './features/carryover';
 import {createTimer,timerValue,startTimer,pauseTimer,resetTimer,type TimerState} from './features/timer';
 import {evaluateExpression,formatSexagesimal} from './features/calculator';
@@ -23,11 +24,11 @@
 import {Icon} from './icons';
 const nativeInput=(p:InputHTMLAttributes<HTMLInputElement>)=><input {...p}/>;
 const nativeScroll=({children}:PropsWithChildren)=><div className="pilot-scroll" data-pilotsolve-scroll>{children}</div>;
-type AppearancePreference='dark'|'light'|'system';
 type SharedAppearance={getPreference:()=>AppearancePreference;setPreference:(value:AppearancePreference)=>void;getSolid:()=>boolean;setSolid:(value:boolean)=>void};
 const sharedAppearance=()=>((window as Window & {SuarezAppearance?:SharedAppearance}).SuarezAppearance);
 const validAppearance=(value:string|null):value is AppearancePreference=>value==='dark'||value==='light'||value==='system';
-const storedAppearance=()=>{try{const value=localStorage.getItem('suarez:appearance');return validAppearance(value)?value:null;}catch{return null;}};
+const canonicalAppearance=()=>{try{return localStorage.getItem('suarez-cfi-appearance');}catch{return null;}};
+const legacyAppearance=()=>{try{return localStorage.getItem('suarez:appearance');}catch{return null;}};
 const storedSolid=()=>{try{const value=localStorage.getItem('suarez:solid-controls');return value==='true'?true:value==='false'?false:null;}catch{return null;}};
 type Setter=Dispatch<SetStateAction<AppState>>;
 const findTool=(id:string)=>tools.find(t=>t.id===id);
@@ -36,10 +37,20 @@
 function calculate(t:Tool,s:AppState,visited:string[]=[]){const d=getDraft(t,s);const m=t.modes.find(m=>m.id===d.mode)||t.modes[0];const input:Record<string,number>={};for(const f of t.fields){const e=d.fields[f.id];if(e?.link){if(visited.includes(e.link.tool)||e.link.tool===t.id){input[f.id]=NaN;continue;}const source=findTool(e.link.tool);if(source){const r=calculate(source,s,[...visited,t.id]);input[f.id]=r.values[e.link.field];}}else if(e?.text?.trim()){try{input[f.id]=parseValue(e.text,f.dimension,e.unit);}catch{input[f.id]=NaN;}}}return {...solve(t.id,m.id,input),inputs:input,mode:m};}
 function toolIcon(t:Tool){return t.id==='wind-correction'?'navigation':categories.find(c=>c.id===t.category)?.icon||'calculator';}
 function friendly(t:Tool){return t.id==='fuel'?'Fuel required':t.id==='wind-component'?'Crosswind':t.id==='altitude'?'Density altitude':t.id==='wind-correction'?'Heading & groundspeed':t.name;}
+// Calculator drafts belong to the current app session, not a route mount or backup.
+function useCalculatorDraft(){
+ const expression=useState('');
+ const mode=useState<'number'|'time'|'angle'>('number');
+ const error=useState('');
+ const answer=useState<number|null>(null);
+ const reset=()=>{expression[1]('');mode[1]('number');error[1]('');answer[1](null);};
+ return {expression,mode,error,answer,reset};
+}
 export default function PilotApp({preview=false,Sheet=CalculationSheet,Input=nativeInput,Scroll=nativeScroll,dismissKeyboard=()=>{}}:{preview?:boolean;Sheet?:ComponentType<SheetProps>;Input?:ComponentType<InputHTMLAttributes<HTMLInputElement>>;Scroll?:ComponentType<PropsWithChildren>;dismissKeyboard?:()=>void}){
  const reduceMotion=useReducedMotion();
  const [menuOpen,setMenuOpen]=useState(false);
- const [state,setState]=useState<AppState>(()=>{const s=readSaved<unknown>('app',initialState);const session=freshSession(isAppState(s)?s:initialState);session.settings.theme=storedAppearance()||session.settings.theme;return session;});
+ const calculator=useCalculatorDraft();
+ const [state,setState]=useState<AppState>(()=>{const s=readSaved<unknown>('app',initialState);const session=freshSession(isAppState(s)?s:initialState);session.settings.theme=resolveAppearancePreference(sharedAppearance()?.getPreference(),canonicalAppearance(),legacyAppearance(),session.settings.theme);return session;});
  const [solid,setSolid]=useState(()=>storedSolid()??state.settings.glass===false);
  const [helperStack,setHelperStack]=useState<{parent:string;parentRoute:string;draft:Draft;field:Field;source:InputHelper;sourceDraft?:Draft}[]>([]);
  const [route,setRoute]=useState(()=>location.hash.slice(1)||'tools');const [search,setSearch]=useState('');const [editor,setEditor]=useState<Editor|null>(null);const [toast,setToast]=useState('');const [storageError,setStorageError]=useState(false);const [systemDark,setSystemDark]=useState(matchMedia('(prefers-color-scheme: dark)').matches);
@@ -49,21 +60,23 @@
  useLayoutEffect(()=>{setStorageError(!writeSaved('app',freshSession(state)));},[state.settings,state.favorites,state.history]);
  useEffect(()=>{const id=setInterval(()=>setNow(Date.now()),250);return()=>clearInterval(id);},[]);useEffect(()=>{if(toast){const id=setTimeout(()=>setToast(''),3500);return()=>clearTimeout(id);}},[toast]);
  useEffect(()=>{const q=matchMedia('(prefers-color-scheme: dark)');const h=()=>setSystemDark(q.matches);q.addEventListener('change',h);return()=>q.removeEventListener('change',h);},[]);
- useEffect(()=>{if(preview)return;const api=sharedAppearance();if(!storedAppearance())api?.setPreference(state.settings.theme);if(storedSolid()===null)api?.setSolid(solid);},[preview]);
+ useEffect(()=>{if(preview)return;const api=sharedAppearance();if(storedSolid()===null)api?.setSolid(solid);},[preview]);
  useEffect(()=>{if(preview)return;const api=sharedAppearance();if(api&&api.getPreference()!==state.settings.theme)api.setPreference(state.settings.theme);},[state.settings.theme,preview]);
  useEffect(()=>{if(preview)return;const api=sharedAppearance();if(api&&api.getSolid()!==solid)api.setSolid(solid);},[solid,preview]);
- useEffect(()=>{if(preview)return;const sync=(event:Event)=>{const detail=(event as CustomEvent<{preference?:AppearancePreference;solid?:boolean}>).detail;if(detail&&validAppearance(detail.preference||null)){setState(s=>s.settings.theme===detail.preference?s:{...s,settings:{...s.settings,theme:detail.preference!}});}if(typeof detail?.solid==='boolean')setSolid(detail.solid);};addEventListener('suarez:appearance',sync);return()=>removeEventListener('suarez:appearance',sync);},[preview]);
+ useEffect(()=>{if(preview)return;const sync=(event:Event)=>{const detail=(event as CustomEvent<{preference?:string;solid?:boolean}>).detail;if(detail?.preference!==undefined){const preference=validAppearance(detail.preference)?detail.preference:'system';setState(s=>s.settings.theme===preference?s:{...s,settings:{...s.settings,theme:preference}});}if(typeof detail?.solid==='boolean')setSolid(detail.solid);};addEventListener('suarez:appearance',sync);return()=>removeEventListener('suarez:appearance',sync);},[preview]);
  const dark=state.settings.theme==='dark'||state.settings.theme==='system'&&systemDark;
  useLayoutEffect(()=>{
   if(preview)return;
   const root=document.documentElement,meta=document.querySelector<HTMLMetaElement>('meta[name="theme-color"]');
-  const previous=root.getAttribute('data-pilot-theme'),previousColor=meta?.content;
+  const previous=root.getAttribute('data-pilot-theme'),previousGoldMode=root.getAttribute('data-gold-mode'),previousSuarezTheme=root.getAttribute('data-suarez-theme'),previousColor=meta?.content;
   root.setAttribute('data-pilot-theme',dark?'dark':'light');
-  if(meta)meta.content=dark?'#080b10':'#f4f6f9';
-  return()=>{if(previous===null)root.removeAttribute('data-pilot-theme');else root.setAttribute('data-pilot-theme',previous);if(meta&&previousColor!==undefined)meta.content=previousColor;};
+  root.setAttribute('data-gold-mode',dark?'dark':'day');
+  root.setAttribute('data-suarez-theme',dark?'dark':'light');
+  if(meta)meta.content=dark?'#414141':'#E3E3E3';
+  return()=>{if(previous===null)root.removeAttribute('data-pilot-theme');else root.setAttribute('data-pilot-theme',previous);if(previousGoldMode===null)root.removeAttribute('data-gold-mode');else root.setAttribute('data-gold-mode',previousGoldMode);if(previousSuarezTheme===null)root.removeAttribute('data-suarez-theme');else root.setAttribute('data-suarez-theme',previousSuarezTheme);if(meta&&previousColor!==undefined)meta.content=previousColor;};
  },[dark,solid,preview]);
  const navigate=(r:string,keepHelper=false)=>{setMenuOpen(false);if(!keepHelper)setHelperStack([]);dismissKeyboard();setEditor(null);setSearch('');setRoute(r);location.hash=r;};
- const resetSession=()=>{setState(s=>freshSession(s));setTimer(createTimer());navigate('tools');setToast('Session reset. Saved calculations kept.');};
+ const resetSession=()=>{calculator.reset();setState(s=>freshSession(s));setTimer(createTimer());navigate('tools');setToast('Session reset. Saved calculations kept.');};
  const edit=(e:Editor|null)=>{if(e)dismissKeyboard();setEditor(e);};
  const selectTool=(t:Tool)=>{setState(s=>s.drafts[t.id]?s:({...s,drafts:{...s.drafts,[t.id]:initialDraft(t,s.settings.unitSystem==='metric')}}));navigate('tool/'+t.id);};
  const activeTool=route.startsWith('tool/')?findTool(route.slice(5).split('?')[0]):undefined;
@@ -99,9 +112,9 @@
  {activeTool&&<ToolView key={activeTool.id+helperStack.length} openHelper={openHelper} tool={activeTool} state={state} setState={setState} notify={setToast} selectTool={selectTool} openTrip={()=>navigate('trip')}/>}
  {route==='trip'&&<TripView state={state} setState={setState}/>}{['aircraft','loading'].includes(route)&&<LoadingView state={state} setState={setState} openTool={id=>{const t=findTool(id);if(t)selectTool(t);}}/>}
  {route==='timer'&&<TimerView timer={timer} setTimer={setTimer} now={now}/>}
- {route==='calculator'&&<CalculatorView state={state} setState={setState} notify={setToast}/>}
+ {route==='calculator'&&<CalculatorView calculator={calculator} state={state} setState={setState} notify={setToast}/>}
  {route==='conversions'&&<ConversionView/>}
- {route==='settings'&&<SettingsView state={state} setState={setState} solid={solid} setSolid={setSolid} notify={setToast}/>}
+ {route==='settings'&&<SettingsView resetCalculator={calculator.reset} state={state} setState={setState} solid={solid} setSolid={setSolid} notify={setToast}/>}
  {route==='history'&&<><p className="quiet">Restore a copy to try a different scenario.</p>{!state.history.length?<div className="empty"><Icon name="history" size={40}/><h2>A little room for your working</h2><p>Save a result from any flight tool and it will appear here.</p></div>:state.history.map(h=><div className="history-row" key={h.id}><button onClick={()=>{const t=findTool(h.tool);if(t){setState(s=>({...s,drafts:{...s.drafts,[h.tool]:{mode:h.mode,fields:structuredClone(h.fields)}}}));navigate('tool/'+h.tool);}}}><strong>{findTool(h.tool)?.name||h.tool}</strong><small>{new Date(h.date).toLocaleString()}</small></button><button className="icon-button" aria-label="Delete saved calculation" onClick={()=>setState(s=>({...s,history:s.history.filter(x=>x.id!==h.id)}))}><Icon name="trash" size={20}/></button></div>)}</>}
  {!activeTool&&!selectedCategory&&!['tools','trip','aircraft','loading','timer','calculator','conversions','settings','history','rhumb-route'].includes(route)&&<div className="empty"><h1>Tool not found</h1><button className="primary" onClick={()=>navigate('tools')}>Browse tools</button></div>}
  </main></Scroll></div>
@@ -135,8 +148,8 @@
 }
 function ConversionView(){const [dimension,setDimension]=useState<Dimension>('distance');const [value,setValue]=useState('');const {edit}=useContext(UiContext);const dims:Dimension[]=['distance','speed','duration','temperature','pressure','volume','flow','weight','verticalSpeed','gradient','torque','angle'];const labels:Partial<Record<Dimension,string>>={verticalSpeed:'Rate of climb / descent',gradient:'Climb / descent gradient',flow:'Fuel flow',duration:'Duration',torque:'Torque'};return <><p className="quiet">One quantity. Every useful unit.</p><SelectionControl label="Conversion category" value={dimension} options={dims.map(d=>({value:d,label:labels[d]||d[0].toUpperCase()+d.slice(1)}))} onChange={next=>{edit(null);setDimension(next as Dimension);setValue('');}}/><NumberField label="Value" value={value} onChange={setValue} dimension={dimension}/><div className="conversion-list">{units[dimension].map(u=><div className="related-result" key={u.id}><span>{u.label}</span><strong>{value!==''?formatValue(Number(value),dimension,u.id):'—'}</strong><button className="icon-button" aria-label={'Enter value in '+u.label} onClick={()=>edit({id:'Value in '+u.id,label:'Value · '+u.label,text:value!==''?String(fromCanonical(Number(value),u.id,dimension)):'',dimension,unit:u.id,onChange:setValue})}><Icon name="calculator" size={18}/></button></div>)}</div><p className="quiet">Tap the calculator beside any unit to enter a value in that unit.</p></>;}
 function TimerView({timer,setTimer,now}:{timer:TimerState;setTimer:Dispatch<SetStateAction<TimerState>>;now:number}){const [duration,setDuration]=useState(String(timer.durationMs/3600000));const current=timerValue(timer,now);const capped=Math.min(Math.abs(current),359999000);return <><div className="segmented">{['stopwatch','countdown'].map(mode=><button key={mode} aria-pressed={timer.mode===mode} onClick={()=>setTimer(mode==='stopwatch'?createTimer():{...createTimer(Math.max(0,Number(duration)||0)*3600000),mode:'countdown'})}>{mode==='stopwatch'?'Stopwatch':'Countdown'}</button>)}</div>{timer.mode==='countdown'&&!timer.running&&<NumberField label="Countdown duration" value={duration} onChange={v=>{setDuration(v);setTimer({...createTimer(Math.max(0,Number(v)||0)*3600000),mode:'countdown'});}} dimension="duration"/>}<div className={'timer-face '+(current<0?'overrun':'')} role="timer"><span>{current<0?'−':''}{formatSexagesimal(Math.floor(capped/1000)/3600)}</span><small>{current<0?'Past zero':timer.running?'Running':timer.accumulatedMs?'Paused':'Ready'}</small></div><button className="primary wide" onClick={()=>setTimer(t=>t.running?pauseTimer(t,Date.now()):startTimer(t,Date.now()))}>{timer.running?'Pause':'Start'}</button><div className="action-row"><button className="secondary" onClick={()=>setTimer(t=>startTimer(resetTimer(t),Date.now()))}>Restart</button><button className="secondary" onClick={()=>setTimer(t=>resetTimer(t))}>Reset</button></div><p className="quiet">The timer continues while you switch tools in this session. Reloading or reopening starts a fresh timer. Sound and notifications are not guaranteed while the browser is suspended or the phone is locked.</p><p className="quiet">Display limit: 99:59:59. Countdown continues past zero.</p></>;}
-function CalculatorView({state,setState,notify}:{state:AppState;setState:Setter;notify:(s:string)=>void}){const [expression,setExpression]=useState('');const [mode,setMode]=useState<'number'|'time'|'angle'>('number');const [error,setError]=useState('');const [answer,setAnswer]=useState<number|null>(null);const memory=mode==='number'?state.memory:state.sexMemory;const {Input}=useContext(UiContext);const equals=()=>{try{const n=evaluateExpression(expression,mode);setAnswer(n);setError('');setState(s=>({...s,calcHistory:[`${expression} = ${mode==='number'?n:formatSexagesimal(n)}`,...s.calcHistory].slice(0,7)}));}catch(e){setError((e as Error).message);setAnswer(null);}};return <><div className="segmented">{(['number','time','angle'] as const).map(m=><button key={m} aria-pressed={mode===m} onClick={()=>{setMode(m);setExpression('');setAnswer(null);setError('');}}>{m==='number'?'Numbers':m==='time'?'Time':'Angles'}</button>)}</div><label className="expression"><span>{mode==='number'?'Expression':mode==='time'?'Hours : minutes : seconds':'Degrees : minutes : seconds'}</span><Input aria-label="Expression" value={expression} onChange={e=>{setExpression(e.target.value);setAnswer(null);setError('');}} onKeyDown={e=>{if(e.key==='Enter')equals();}} placeholder={mode==='number'?'0':'0:00:00'}/></label><div className="calc-answer" aria-label="Calculated answer" aria-live="polite" aria-atomic="true"><AnimatedValue value={answer==null?'—':mode==='number'?Number(answer.toPrecision(12)).toString():formatSexagesimal(answer)}/></div>{answer!=null&&mode!=='number'&&<p className="quiet">Decimal: {Number(answer.toFixed(8))} {mode==='time'?'hours':'degrees'}</p>}{error&&<p className="warning" role="alert">{error}</p>}<div className="memory-bar"><button onClick={()=>{if(answer!=null){setState(s=>({...s,[mode==='number'?'memory':'sexMemory']:answer}));notify('Stored in memory');}}} disabled={answer==null}>Store</button><button disabled={memory==null} onClick={()=>setExpression(e=>e+(mode==='number'?String(memory):formatSexagesimal(memory!)))}>Recall</button><button disabled={memory==null} onClick={()=>setState(s=>({...s,[mode==='number'?'memory':'sexMemory']:null}))}>Clear memory</button></div><div className="calculator-grid">{['C','(',')','÷','7','8','9','×','4','5','6','−','1','2','3','+','±','0','.', '=', '√',':','delete'].map(k=><button className={k==='='?'equals':''} key={k} onClick={()=>{if(k==='='){equals();return;}setAnswer(null);setError('');setExpression(e=>{const previous=answer===null?e:(mode==='number'?String(answer):formatSexagesimal(answer));const base=answer!==null&&!['+','−','×','÷','±','delete'].includes(k)?'':previous;return k==='C'?'':k==='delete'?base.slice(0,-1):k==='±'?(base?'-('+base+')':'-'):base+({'÷':'/','×':'*','−':'-','√':'sqrt('}[k]||k);});}} aria-label={k==='delete'?'Delete last character':k}>{k==='delete'?<Icon name="delete"/>:k}</button>)}</div><details className="explanation"><summary>Calculator history</summary>{state.calcHistory.length?state.calcHistory.map((h,i)=><p className="formula" key={i}>{h}</p>):<p>No calculations yet.</p>}</details><p className="quiet">Standard order of operations. Use parentheses when needed. For time, 1:30 means 1 hour 30 minutes; 1.5 is decimal hours.</p></>;}
-function SettingsView({state,setState,solid,setSolid,notify}:{state:AppState;setState:Setter;solid:boolean;setSolid:(value:boolean)=>void;notify:(s:string)=>void}){const [confirmReset,setConfirmReset]=useState(false);const settings=state.settings;const update=(v:Partial<typeof settings>)=>setState(s=>({...s,settings:{...s.settings,...v}}));const backup=()=>{const blob=new Blob([JSON.stringify(state,null,2)],{type:'application/json'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download='pilotsolve-backup-'+new Date().toISOString().slice(0,10)+'.json';a.click();URL.revokeObjectURL(url);};return <><h2>Make it yours</h2><p className="quiet">PilotSolve · E6B Flight Computer</p><section className="appearance-panel"><p className="eyebrow">APPEARANCE</p><div className="segmented appearance-modes" aria-label="Appearance">{(['dark','light','system'] as const).map(theme=><button key={theme} aria-pressed={settings.theme===theme} onClick={()=>update({theme})}>{theme==='dark'?'Dark':theme==='light'?'Day':'System'}</button>)}</div><GlassSwitch label="Solid controls" description="Replace translucent dock and menus with opaque surfaces" checked={solid} onChange={value=>{setSolid(value);update({glass:!value});}}/></section><SelectionControl label="Preferred units" value={settings.unitSystem} options={[{value:"us",label:"U.S. aviation"},{value:"metric",label:"Metric"}]} onChange={next=>update({unitSystem:next as 'us'|'metric'})}/><p className="quiet">Each calculation also has individual unit controls. Existing values retain their assigned units.</p><h2>Clocks</h2><NumberField label="Local UTC offset" value={String(settings.localOffset)} onChange={v=>{const n=Number(v);if(n>=-12&&n<=14)update({localOffset:n});}}/><NumberField label="Destination UTC offset" value={String(settings.destinationOffset)} onChange={v=>{const n=Number(v);if(n>=-12&&n<=14)update({destinationOffset:n});}}/><div className="clock-list">{[['UTC',0],['Local',settings.localOffset],['Destination',settings.destinationOffset]].map(([label,offset])=><div className="related-result" key={label}><span>{label}</span><strong>{new Date(Date.now()+Number(offset)*3600000).toISOString().slice(11,16)}</strong></div>)}</div><p className="quiet">Offsets are explicit. Update them for daylight saving time. Trip departures are entered in UTC.</p><h2>Your data</h2><button className="secondary wide" onClick={backup}><Icon name="download"/>Export backup</button><label className="file-import secondary"><Icon name="upload"/>Restore backup<input type="file" accept="application/json,.json" aria-label="Restore backup" onChange={async e=>{const file=e.target.files?.[0];if(!file)return;try{if(file.size>2_000_000)throw new Error('Backup is too large');const s=JSON.parse(await file.text());if(!isAppState(s))throw new Error('This is not a valid PilotSolve backup');setState(s);notify('Backup restored');}catch(error){notify((error as Error).message);}e.target.value='';}}/></label><p className="quiet">Restoring replaces this device’s saved calculations, trip and aircraft profile. Export first if you want to keep them.</p><details className="explanation"><summary>Install and use offline</summary><p>iPhone: open this app in Safari, choose Share, then Add to Home Screen. Android: use your browser’s Install app or Add to Home screen action.</p><p>The production app caches its tools after the first successful visit. Saved calculations stay on this device. Unsaved calculations, trip entries, loading entries, and timers clear when you reload. Export a backup to keep a working trip or aircraft profile.</p></details><details className="explanation"><summary>About PilotSolve</summary><p>Independent aviation calculator. No ASA affiliation. Original calculations implemented from documented aviation relationships, with unit-aware inputs and explicit solve directions.</p><p>Designed by CFI Diego Suarez.</p><p>Version 1.0 · Local-first · No account required</p><p>Use aircraft-specific data for loading. This app does not supply aircraft operating limits or assess a flight’s safety.</p></details><button className="text-button danger" onClick={()=>setConfirmReset(!confirmReset)}>Reset saved app data</button>{confirmReset&&<div className="warning"><p>Delete saved calculations, drafts, trip, profile, and preferences from this app?</p><button className="secondary" onClick={()=>{setState(structuredClone(initialState));setSolid(false);setConfirmReset(false);notify('App data reset');}}>Reset app data</button></div>}</>;}
+function CalculatorView({calculator,state,setState,notify}:{calculator:ReturnType<typeof useCalculatorDraft>;state:AppState;setState:Setter;notify:(s:string)=>void}){const {expression:[expression,setExpression],mode:[mode,setMode],error:[error,setError],answer:[answer,setAnswer]}=calculator;const memory=mode==='number'?state.memory:state.sexMemory;const {Input}=useContext(UiContext);const equals=()=>{try{const n=evaluateExpression(expression,mode);setAnswer(n);setError('');setState(s=>({...s,calcHistory:[`${expression} = ${mode==='number'?n:formatSexagesimal(n)}`,...s.calcHistory].slice(0,7)}));}catch(e){setError((e as Error).message);setAnswer(null);}};return <><div className="segmented">{(['number','time','angle'] as const).map(m=><button key={m} aria-pressed={mode===m} onClick={()=>{setMode(m);setExpression('');setAnswer(null);setError('');}}>{m==='number'?'Numbers':m==='time'?'Time':'Angles'}</button>)}</div><label className="expression"><span>{mode==='number'?'Expression':mode==='time'?'Hours : minutes : seconds':'Degrees : minutes : seconds'}</span><Input aria-label="Expression" value={expression} onChange={e=>{setExpression(e.target.value);setAnswer(null);setError('');}} onKeyDown={e=>{if(e.key==='Enter')equals();}} placeholder={mode==='number'?'0':'0:00:00'}/></label><div className="calc-answer" aria-label="Calculated answer" aria-live="polite" aria-atomic="true"><AnimatedValue value={answer==null?'—':mode==='number'?Number(answer.toPrecision(12)).toString():formatSexagesimal(answer)}/></div>{answer!=null&&mode!=='number'&&<p className="quiet">Decimal: {Number(answer.toFixed(8))} {mode==='time'?'hours':'degrees'}</p>}{error&&<p className="warning" role="alert">{error}</p>}<div className="memory-bar"><button onClick={()=>{if(answer!=null){setState(s=>({...s,[mode==='number'?'memory':'sexMemory']:answer}));notify('Stored in memory');}}} disabled={answer==null}>Store</button><button disabled={memory==null} onClick={()=>setExpression(e=>e+(mode==='number'?String(memory):formatSexagesimal(memory!)))}>Recall</button><button disabled={memory==null} onClick={()=>setState(s=>({...s,[mode==='number'?'memory':'sexMemory']:null}))}>Clear memory</button></div><div className="calculator-grid">{['C','(',')','÷','7','8','9','×','4','5','6','−','1','2','3','+','±','0','.', '=', '√',':','delete'].map(k=><button className={k==='='?'equals':''} key={k} onClick={()=>{if(k==='='){equals();return;}setAnswer(null);setError('');setExpression(e=>{const previous=answer===null?e:(mode==='number'?String(answer):formatSexagesimal(answer));const base=answer!==null&&!['+','−','×','÷','±','delete'].includes(k)?'':previous;return k==='C'?'':k==='delete'?base.slice(0,-1):k==='±'?(base?'-('+base+')':'-'):base+({'÷':'/','×':'*','−':'-','√':'sqrt('}[k]||k);});}} aria-label={k==='delete'?'Delete last character':k}>{k==='delete'?<Icon name="delete"/>:k}</button>)}</div><details className="explanation"><summary>Calculator history</summary>{state.calcHistory.length?state.calcHistory.map((h,i)=><p className="formula" key={i}>{h}</p>):<p>No calculations yet.</p>}</details><p className="quiet">Standard order of operations. Use parentheses when needed. For time, 1:30 means 1 hour 30 minutes; 1.5 is decimal hours.</p></>;}
+function SettingsView({resetCalculator,state,setState,solid,setSolid,notify}:{resetCalculator:()=>void;state:AppState;setState:Setter;solid:boolean;setSolid:(value:boolean)=>void;notify:(s:string)=>void}){const [confirmReset,setConfirmReset]=useState(false);const settings=state.settings;const update=(v:Partial<typeof settings>)=>setState(s=>({...s,settings:{...s.settings,...v}}));const backup=()=>{const blob=new Blob([JSON.stringify(state,null,2)],{type:'application/json'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download='pilotsolve-backup-'+new Date().toISOString().slice(0,10)+'.json';a.click();URL.revokeObjectURL(url);};return <><h2>Make it yours</h2><p className="quiet">PilotSolve · E6B Flight Computer</p><section className="appearance-panel"><p className="eyebrow">APPEARANCE</p><div className="segmented appearance-modes" aria-label="Appearance">{(['dark','light','system'] as const).map(theme=><button key={theme} aria-pressed={settings.theme===theme} onClick={()=>update({theme})}>{theme==='dark'?'Night':theme==='light'?'Day':'System'}</button>)}</div><GlassSwitch label="Solid controls" description="Replace translucent dock and menus with opaque surfaces" checked={solid} onChange={value=>{setSolid(value);update({glass:!value});}}/></section><SelectionControl label="Preferred units" value={settings.unitSystem} options={[{value:"us",label:"U.S. aviation"},{value:"metric",label:"Metric"}]} onChange={next=>update({unitSystem:next as 'us'|'metric'})}/><p className="quiet">Each calculation also has individual unit controls. Existing values retain their assigned units.</p><h2>Clocks</h2><NumberField label="Local UTC offset" value={String(settings.localOffset)} onChange={v=>{const n=Number(v);if(n>=-12&&n<=14)update({localOffset:n});}}/><NumberField label="Destination UTC offset" value={String(settings.destinationOffset)} onChange={v=>{const n=Number(v);if(n>=-12&&n<=14)update({destinationOffset:n});}}/><div className="clock-list">{[['UTC',0],['Local',settings.localOffset],['Destination',settings.destinationOffset]].map(([label,offset])=><div className="related-result" key={label}><span>{label}</span><strong>{new Date(Date.now()+Number(offset)*3600000).toISOString().slice(11,16)}</strong></div>)}</div><p className="quiet">Offsets are explicit. Update them for daylight saving time. Trip departures are entered in UTC.</p><h2>Your data</h2><button className="secondary wide" onClick={backup}><Icon name="download"/>Export backup</button><label className="file-import secondary"><Icon name="upload"/>Restore backup<input type="file" accept="application/json,.json" aria-label="Restore backup" onChange={async e=>{const file=e.target.files?.[0];if(!file)return;try{if(file.size>2_000_000)throw new Error('Backup is too large');const s=JSON.parse(await file.text());if(!isAppState(s))throw new Error('This is not a valid PilotSolve backup');setState(s);notify('Backup restored');}catch(error){notify((error as Error).message);}e.target.value='';}}/></label><p className="quiet">Restoring replaces this device’s saved calculations, trip and aircraft profile. Export first if you want to keep them.</p><details className="explanation"><summary>Install and use offline</summary><p>iPhone: open this app in Safari, choose Share, then Add to Home Screen. Android: use your browser’s Install app or Add to Home screen action.</p><p>The production app caches its tools after the first successful visit. Saved calculations stay on this device. Unsaved calculations, trip entries, loading entries, and timers clear when you reload. Export a backup to keep a working trip or aircraft profile.</p></details><details className="explanation"><summary>About PilotSolve</summary><p>Independent aviation calculator. No ASA affiliation. Original calculations implemented from documented aviation relationships, with unit-aware inputs and explicit solve directions.</p><p>Designed by CFI Diego Suarez.</p><p>Version 1.0 · Local-first · No account required</p><p>Use aircraft-specific data for loading. This app does not supply aircraft operating limits or assess a flight’s safety.</p></details><button className="text-button danger" onClick={()=>setConfirmReset(!confirmReset)}>Reset saved app data</button>{confirmReset&&<div className="warning"><p>Delete saved calculations, drafts, trip, profile, and preferences from this app?</p><button className="secondary" onClick={()=>{resetCalculator();setState(structuredClone(initialState));setSolid(false);setConfirmReset(false);notify('App data reset');}}>Reset app data</button></div>}</>;}
 
 function OfflineStatus(){
  const [status,setStatus]=useState(import.meta.env.PROD?'Preparing offline tools…':'Development preview · offline cache runs in production');
--- a/src/prototype.css
+++ b/src/prototype.css
@@ -1,6 +1,6 @@
 @font-face{font-family:'Inter Variable';font-style:normal;font-weight:100 900;font-display:swap;src:url('../node_modules/@fontsource-variable/inter/files/inter-latin-wght-normal.woff2') format('woff2');}
 @font-face{font-family:'Manrope Variable';font-style:normal;font-weight:200 800;font-display:swap;src:url('../node_modules/@fontsource-variable/manrope/files/manrope-latin-wght-normal.woff2') format('woff2');}
-:root{font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif;font-synthesis:none;text-rendering:optimizeLegibility;-webkit-font-smoothing:antialiased;color:#121b2d;background:#f5f6f8}*{box-sizing:border-box}html,body,#root{margin:0;width:100%;height:100%}body{min-width:320px}button,input,select,textarea{font:inherit}button,a,select{-webkit-tap-highlight-color:transparent}button{cursor:pointer}button:disabled{opacity:.42;cursor:default}button:focus-visible,a:focus-visible,summary:focus-visible,select:focus-visible,input:focus-visible{outline:3px solid #2488ff;outline-offset:3px}button{color:inherit}a{color:inherit}.pilot-app{--bg:#fff;--surface:#f3f5f8;--text:#101827;--muted:#747f91;--line:#e5e9f0;--blue:#0879ff;--soft:#eaf3ff;--danger:#ad352f;--warning:#fff0db;--shadow:0 4px 20px #122b5114;color:var(--text);background:var(--bg);font-size:15px;line-height:1.45;display:flex;flex-direction:column;overflow:hidden;height:100%;position:relative;color-scheme:light}.pilot-app[data-theme=dark]{--bg:#151a20;--surface:#20272f;--text:#f4f6fa;--muted:#a3adba;--line:#333c47;--blue:#42a5ff;--soft:#152f48;--danger:#ff9f95;--warning:#382b1e;color-scheme:dark}.pilot-app.standalone{max-width:500px;margin:0 auto;height:100dvh;box-shadow:0 0 0 1px #e6e9ee}.pilot-app.preview{padding-top:var(--mobile-status-bar-height,56px)}.pilot-header{flex-shrink:0;display:flex;align-items:center;justify-content:space-between;min-height:76px;padding:14px 22px 10px;gap:8px;padding-top:max(14px,env(safe-area-inset-top))}.preview .pilot-header{min-height:65px;padding-top:5px}.wordmark{text-decoration:none;letter-spacing:-1.4px;font-size:29px;line-height:1.1;font-weight:780}.wordmark span{color:var(--blue)}.header-actions{display:flex;gap:2px}.icon-button{border:0;background:transparent;display:inline-flex;align-items:center;justify-content:center;min-width:44px;min-height:48px;padding:8px;color:var(--muted)}.header-actions .icon-button{color:var(--blue)}.header-title{font-size:15px;font-weight:650;text-align:center;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.back-link{display:flex;gap:5px;align-items:center;background:none;border:0;color:var(--blue);padding:0;min-height:48px;font-size:15px}.scroll-slot{flex:1;min-height:0;position:relative;overflow:hidden}.pilot-scroll{height:100%;overflow:auto;overscroll-behavior:contain;scrollbar-width:thin}.pilot-mobile-scroll{height:100%;position:relative!important;inset:auto!important;background:var(--bg)}.pilot-content{padding:12px 22px 28px}.pilot-app h1{font-size:28px;letter-spacing:-.8px;line-height:1.15;margin:5px 0 10px;font-weight:730}.pilot-app h2{font-size:20px;letter-spacing:-.4px;margin:26px 0 12px;font-weight:680}.pilot-app p{margin:8px 0 16px}.page-intro p,.tool-intro p:not(.eyebrow){color:var(--muted);font-size:14px}.page-intro{margin-bottom:21px}.search-field{display:flex;align-items:center;gap:10px;min-height:51px;background:var(--surface);border-radius:12px;color:var(--muted);padding:0 13px;margin:0 0 18px}.search-field input{border:0;background:transparent;color:var(--text);width:100%;min-width:0;outline:none;font-size:16px;height:51px}.search-field input::placeholder{color:var(--muted)}.search-field .icon-button{min-width:28px;padding:0}.favorites-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin:20px 0 29px}.favorite-tile{border:1px solid transparent;border-radius:14px;background:var(--surface);min-height:122px;padding:19px 9px 15px;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:13px;line-height:1.25;font-size:14px;letter-spacing:-.35px;font-weight:560}.favorite-tile svg{color:var(--blue)}.favorite-tile:active{background:var(--soft);transform:scale(.98)}.tool-row{display:flex;align-items:center;width:100%;gap:14px;border:0;border-bottom:1px solid var(--line);background:transparent;text-align:left;min-height:72px;padding:13px 0}.tool-row>svg:first-child{color:var(--blue);flex-shrink:0}.tool-row>svg:last-child{margin-left:auto;color:var(--muted);flex-shrink:0}.tool-row span{flex:1;min-width:0}.tool-row strong{font-size:15px;font-weight:580;display:block}.tool-row small{display:block;font-size:12px;color:var(--muted);margin-top:2px}.quiet,.muted{color:var(--muted);font-size:13px}.footnote{margin-top:24px!important;line-height:1.6}.bottom-nav{flex-shrink:0;display:grid;grid-template-columns:repeat(5,1fr);border-top:1px solid var(--line);background:var(--bg);padding:9px 5px max(12px,env(safe-area-inset-bottom));min-height:78px}.preview .bottom-nav{padding-bottom:30px;min-height:92px}.bottom-nav button{display:flex;flex-direction:column;align-items:center;gap:5px;padding:2px 1px;border:0;background:transparent;color:var(--muted);font-size:10px;min-height:48px}.bottom-nav button[aria-current=page]{color:var(--blue)}.bottom-nav span{white-space:nowrap}.live-dot{display:inline-block;width:5px;height:5px;border-radius:50%;background:var(--blue);margin-right:3px}.eyebrow{text-transform:uppercase;font-size:10px;letter-spacing:1.5px;color:var(--muted);font-weight:650;margin-top:0!important}.tool-intro h1{font-size:27px}.tool-intro p{font-size:13px;line-height:1.5}.solve-select{display:flex;flex-direction:column;gap:5px;margin:20px 0 14px}.solve-select>span{font-size:10px;letter-spacing:1.7px;text-transform:uppercase;color:var(--muted);font-weight:650}.pilot-app select{background:var(--surface);border:1px solid var(--line);border-radius:10px;min-height:48px;padding:10px 30px 10px 12px;color:var(--text);max-width:100%}.solve-select select{color:var(--blue);font-weight:600;width:100%;font-size:15px}.result-panel{background:var(--surface);border-radius:14px;padding:17px 16px 15px;margin:15px 0 18px;text-align:center}.result-label{display:block;font-size:13px;color:var(--muted)}.result-number{font-size:47px;font-weight:680;line-height:1.3;letter-spacing:-1.7px;font-variant-numeric:tabular-nums;overflow-wrap:anywhere}.result-number>span{font-size:16px;letter-spacing:0;font-weight:450;color:var(--muted);margin-left:8px}.result-panel small{color:var(--muted);font-size:11px}.result-panel.pending .result-number{color:var(--muted)}.input-group{margin-bottom:15px}.field-row{display:flex;align-items:center;justify-content:space-between;gap:10px;min-height:64px;border-bottom:1px solid var(--line);padding:10px 0}.field-label{font-weight:560;font-size:14px;flex:1;min-width:0}.field-label small{display:block;font-weight:400;color:var(--muted);font-size:11px;margin-top:3px;line-height:1.4}.number-field{display:flex;align-items:baseline;justify-content:flex-end;gap:7px;border:1px solid transparent;background:var(--surface);border-radius:9px;padding:11px 10px;min-height:48px;min-width:92px;max-width:57%;font-variant-numeric:tabular-nums}.number-field strong{font-size:19px;font-weight:650;overflow-wrap:anywhere;min-width:0}.number-field span{font-size:10px;color:var(--muted);white-space:nowrap}.field-row.active .number-field{border-color:var(--blue);background:var(--soft)}.unit-options{display:flex;align-items:center;justify-content:flex-end;gap:10px;margin:2px 0 5px;font-size:10px;color:var(--muted)}.unit-options select{border:0;background:transparent;min-height:32px;font-size:11px;padding:4px 18px 4px 5px;color:var(--blue)}.unit-options button{background:none;border:0;color:var(--blue);min-height:32px;font-size:11px}.related-result{display:flex;gap:10px;align-items:center;justify-content:space-between;border-bottom:1px solid var(--line);padding:13px 0;min-height:48px;font-size:13px}.related-result strong{font-weight:600;font-variant-numeric:tabular-nums;text-align:right}.related-result small{font-weight:400;color:var(--muted)}.action-row{display:flex;gap:10px;margin:20px 0}.action-row>*{flex:1}.primary,.secondary{border:0;border-radius:10px;display:inline-flex;align-items:center;justify-content:center;gap:8px;min-height:48px;padding:12px 17px;font-size:14px;font-weight:600}.primary{background:var(--blue);color:#fff}.secondary{background:var(--soft);color:var(--blue)}.wide{width:100%;margin:8px 0}.text-button{border:0;background:transparent;color:var(--blue);min-height:48px;font-size:13px;padding:10px 0}.danger{color:var(--danger)}.explanation{border-top:1px solid var(--line);margin:20px 0 0;padding:6px 0;color:var(--muted);font-size:13px}.explanation>summary{display:flex;align-items:center;gap:10px;min-height:48px;font-size:14px;cursor:pointer;color:var(--text);list-style:none}.explanation summary>svg:last-child{margin-left:auto;color:var(--muted)}.explanation[open] summary>svg:last-child{transform:rotate(90deg)}.formula{padding:12px;background:var(--surface);border-radius:9px;overflow-wrap:anywhere;font-variant-numeric:tabular-nums}.warning{padding:13px 15px;background:var(--warning);color:var(--danger);font-size:13px;border-radius:9px;margin:12px 0}.empty{text-align:center;padding:40px 10px;color:var(--muted)}.empty svg{color:var(--blue)}.empty h2{color:var(--text)}.toast{position:absolute;bottom:100px;left:22px;right:22px;background:var(--text);color:var(--bg);border-radius:12px;padding:12px 15px;z-index:20;font-size:13px;text-align:center;box-shadow:var(--shadow)}.numeric-keypad{flex-shrink:0;background:var(--surface);padding:5px 9px max(10px,env(safe-area-inset-bottom));border-top:1px solid var(--line)}.preview .numeric-keypad{padding-bottom:28px}.keypad-caption{display:flex;align-items:center;gap:12px;padding:0 3px 4px;color:var(--muted);font-size:11px;min-height:30px}.keypad-caption>span{margin-right:auto}.keypad-caption button{background:none;border:0;padding:3px;color:var(--blue);font-size:11px;min-height:32px}.key-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:5px}.key-grid button{display:flex;align-items:center;justify-content:center;height:43px;background:var(--bg);border:1px solid var(--line);border-radius:8px;font-size:23px;font-weight:560;font-variant-numeric:tabular-nums}.keypad-actions{display:flex;gap:6px;margin-top:6px}.keypad-actions .primary{flex:1}.keypad-actions .secondary{min-width:48px;padding:10px}.keypad-actions>*{min-height:44px}.select-field{display:flex;gap:12px;align-items:center;justify-content:space-between;min-height:65px;border-bottom:1px solid var(--line);font-size:14px}.select-field span{font-weight:550}.select-field select{max-width:60%;font-size:13px}.text-field{display:flex;flex-direction:column;gap:6px;margin:13px 0;font-size:13px}.text-field span{font-weight:550}.text-field input{width:100%;min-height:48px;border:1px solid var(--line);border-radius:9px;background:var(--surface);color:var(--text);padding:12px;font-size:16px}.panel{margin:15px 0;border:1px solid var(--line);border-radius:12px;padding:12px 14px}.panel>summary{min-height:45px;cursor:pointer;font-weight:600}.panel h2{margin-top:8px}.panel p{font-size:13px}.totals,.trip-totals,.loading-totals{background:var(--surface);padding:15px;border-radius:12px;margin:15px 0}.totals h2{margin:0 0 10px}.history-row{display:flex;align-items:center;border-bottom:1px solid var(--line);padding:10px 0}.history-row>button:first-child{flex:1;border:0;background:none;text-align:left;min-height:55px}.history-row small{display:block;color:var(--muted);font-size:11px;margin-top:4px}.segmented{display:flex;border-radius:10px;background:var(--surface);padding:3px;margin:20px 0;gap:3px}.segmented>button{flex:1;min-height:43px;font-size:13px;background:none;border:0;border-radius:8px;color:var(--muted)}.segmented>button[aria-pressed=true]{background:var(--blue);color:#fff}.timer-face{padding:42px 0;text-align:center;font-variant-numeric:tabular-nums}.timer-face>span{font-size:45px;font-weight:600;letter-spacing:-2px}.timer-face small{display:block;color:var(--muted);font-size:13px;margin-top:9px}.timer-face.overrun{color:var(--danger)}.expression{display:block;margin:20px 0}.expression>span{font-size:12px;color:var(--muted)}.expression input{margin-top:6px;background:var(--surface);border:1px solid var(--line);border-radius:10px;min-height:56px;width:100%;color:var(--text);font-size:23px;text-align:right;padding:10px}.calc-answer{text-align:right;font-size:37px;font-weight:600;min-height:55px;overflow-wrap:anywhere;font-variant-numeric:tabular-nums}.memory-bar{display:flex;justify-content:space-between;margin:10px 0}.memory-bar button{border:0;background:none;color:var(--blue);min-height:44px;font-size:12px}.calculator-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:6px}.calculator-grid button{border:0;background:var(--surface);border-radius:9px;min-height:54px;font-size:23px}.calculator-grid .equals{background:var(--blue);color:white}.file-import{position:relative;width:100%;margin:8px 0;cursor:pointer}.file-import input{position:absolute;inset:0;opacity:0;cursor:pointer;width:100%}.reuse-panel{border:1px solid var(--line);border-radius:12px;padding:12px}.reuse-panel h2{margin:0 0 10px}.check-row{display:flex;gap:10px;align-items:center;min-height:48px;font-size:13px}.check-row input{width:20px;height:20px}.pilot-content details summary{cursor:pointer}.pilot-content dl{display:grid;grid-template-columns:1fr 1fr;gap:8px;font-size:13px}.pilot-content dd{margin:0;text-align:right;font-weight:600}.pilot-content dt{color:var(--muted)}@media(max-width:360px){.pilot-content{padding-left:16px;padding-right:16px}.pilot-header{padding-left:16px;padding-right:16px}.favorite-tile{font-size:12px}.wordmark{font-size:26px}.number-field strong{font-size:17px}.key-grid button{height:40px}}@media(prefers-reduced-motion:reduce){*{scroll-behavior:auto!important;transition:none!important;animation:none!important}}
+:root{font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif;font-synthesis:none;text-rendering:optimizeLegibility;-webkit-font-smoothing:antialiased;color:var(--g-ink,#272727);background:var(--g-bg,#E3E3E3)}html[data-pilot-theme=dark]{color:var(--g-ink,#f4f4f4);background:var(--g-bg,#414141)}*{box-sizing:border-box}html,body,#root{margin:0;width:100%;height:100%}body{min-width:320px}button,input,select,textarea{font:inherit}button,a,select{-webkit-tap-highlight-color:transparent}button{cursor:pointer}button:disabled{opacity:.42;cursor:default}button:focus-visible,a:focus-visible,summary:focus-visible,select:focus-visible,input:focus-visible{outline:3px solid var(--g-accent,#73551b);outline-offset:3px}button{color:inherit}a{color:inherit}.pilot-app{--bg:var(--g-bg,#E3E3E3);--surface:var(--g-surface,#efefef);--raised:var(--g-raised,#f7f7f7);--well:var(--g-well,#d6d6d6);--text:var(--g-ink,#272727);--muted:var(--g-muted,#595959);--line:var(--g-line,#b7b7b7);--blue:var(--g-accent,#73551b);--cyan:var(--g-accent,#73551b);--diagram-vector:#175ba9;--soft:color-mix(in srgb,var(--blue) 12%,var(--surface));--danger:#a5322e;--warning:#f4dfb6;--shadow:var(--g-shadow,0 3px 5px #0002,0 9px 20px #0000000b);color:var(--text);background:var(--bg);font-size:15px;line-height:1.45;display:flex;flex-direction:column;overflow:hidden;height:100%;position:relative;color-scheme:light}.pilot-app[data-theme=dark]{--bg:var(--g-bg,#414141);--surface:var(--g-surface,#4a4a4a);--raised:var(--g-raised,#545454);--well:var(--g-well,#393939);--text:var(--g-ink,#f4f4f4);--muted:var(--g-muted,#d0d0d0);--line:var(--g-line,#767676);--blue:var(--g-accent,#ecd39b);--cyan:var(--g-accent,#ecd39b);--diagram-vector:#2376dc;--danger:#ffaaa3;--warning:#685532;color-scheme:dark}.pilot-app.standalone{max-width:500px;margin:0 auto;height:100dvh;box-shadow:0 0 0 1px #e6e9ee}.pilot-app.preview{padding-top:var(--mobile-status-bar-height,56px)}.pilot-header{flex-shrink:0;display:flex;align-items:center;justify-content:space-between;min-height:76px;padding:14px 22px 10px;gap:8px;padding-top:max(14px,env(safe-area-inset-top))}.preview .pilot-header{min-height:65px;padding-top:5px}.wordmark{text-decoration:none;letter-spacing:-1.4px;font-size:29px;line-height:1.1;font-weight:780}.wordmark span{color:var(--blue)}.header-actions{display:flex;gap:2px}.icon-button{border:0;background:transparent;display:inline-flex;align-items:center;justify-content:center;min-width:44px;min-height:48px;padding:8px;color:var(--muted)}.header-actions .icon-button{color:var(--blue)}.header-title{font-size:15px;font-weight:650;text-align:center;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.back-link{display:flex;gap:5px;align-items:center;background:none;border:0;color:var(--blue);padding:0;min-height:48px;font-size:15px}.scroll-slot{flex:1;min-height:0;position:relative;overflow:hidden}.pilot-scroll{height:100%;overflow:auto;overscroll-behavior:contain;scrollbar-width:thin}.pilot-mobile-scroll{height:100%;position:relative!important;inset:auto!important;background:var(--bg)}.pilot-content{padding:12px 22px 28px}.pilot-app h1{font-size:28px;letter-spacing:-.8px;line-height:1.15;margin:5px 0 10px;font-weight:730}.pilot-app h2{font-size:20px;letter-spacing:-.4px;margin:26px 0 12px;font-weight:680}.pilot-app p{margin:8px 0 16px}.page-intro p,.tool-intro p:not(.eyebrow){color:var(--muted);font-size:14px}.page-intro{margin-bottom:21px}.search-field{display:flex;align-items:center;gap:10px;min-height:51px;background:var(--surface);border-radius:12px;color:var(--muted);padding:0 13px;margin:0 0 18px}.search-field input{border:0;background:transparent;color:var(--text);width:100%;min-width:0;outline:none;font-size:16px;height:51px}.search-field input::placeholder{color:var(--muted)}.search-field .icon-button{min-width:28px;padding:0}.favorites-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin:20px 0 29px}.favorite-tile{border:1px solid transparent;border-radius:14px;background:var(--surface);min-height:122px;padding:19px 9px 15px;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:13px;line-height:1.25;font-size:14px;letter-spacing:-.35px;font-weight:560}.favorite-tile svg{color:var(--blue)}.favorite-tile:active{background:var(--soft);transform:scale(.98)}.tool-row{display:flex;align-items:center;width:100%;gap:14px;border:0;border-bottom:1px solid var(--line);background:transparent;text-align:left;min-height:72px;padding:13px 0}.tool-row>svg:first-child{color:var(--blue);flex-shrink:0}.tool-row>svg:last-child{margin-left:auto;color:var(--muted);flex-shrink:0}.tool-row span{flex:1;min-width:0}.tool-row strong{font-size:15px;font-weight:580;display:block}.tool-row small{display:block;font-size:12px;color:var(--muted);margin-top:2px}.quiet,.muted{color:var(--muted);font-size:13px}.footnote{margin-top:24px!important;line-height:1.6}.bottom-nav{flex-shrink:0;display:grid;grid-template-columns:repeat(5,1fr);border-top:1px solid var(--line);background:var(--bg);padding:9px 5px max(12px,env(safe-area-inset-bottom));min-height:78px}.preview .bottom-nav{padding-bottom:30px;min-height:92px}.bottom-nav button{display:flex;flex-direction:column;align-items:center;gap:5px;padding:2px 1px;border:0;background:transparent;color:var(--muted);font-size:10px;min-height:48px}.bottom-nav button[aria-current=page]{color:var(--blue)}.bottom-nav span{white-space:nowrap}.live-dot{display:inline-block;width:5px;height:5px;border-radius:50%;background:var(--blue);margin-right:3px}.eyebrow{text-transform:uppercase;font-size:10px;letter-spacing:1.5px;color:var(--muted);font-weight:650;margin-top:0!important}.tool-intro h1{font-size:27px}.tool-intro p{font-size:13px;line-height:1.5}.solve-select{display:flex;flex-direction:column;gap:5px;margin:20px 0 14px}.solve-select>span{font-size:10px;letter-spacing:1.7px;text-transform:uppercase;color:var(--muted);font-weight:650}.pilot-app select{background:var(--surface);border:1px solid var(--line);border-radius:10px;min-height:48px;padding:10px 30px 10px 12px;color:var(--text);max-width:100%}.solve-select select{color:var(--blue);font-weight:600;width:100%;font-size:15px}.result-panel{background:var(--surface);border-radius:14px;padding:17px 16px 15px;margin:15px 0 18px;text-align:center}.result-label{display:block;font-size:13px;color:var(--muted)}.result-number{font-size:47px;font-weight:680;line-height:1.3;letter-spacing:-1.7px;font-variant-numeric:tabular-nums;overflow-wrap:anywhere}.result-number>span{font-size:16px;letter-spacing:0;font-weight:450;color:var(--muted);margin-left:8px}.result-panel small{color:var(--muted);font-size:11px}.result-panel.pending .result-number{color:var(--muted)}.input-group{margin-bottom:15px}.field-row{display:flex;align-items:center;justify-content:space-between;gap:10px;min-height:64px;border-bottom:1px solid var(--line);padding:10px 0}.field-label{font-weight:560;font-size:14px;flex:1;min-width:0}.field-label small{display:block;font-weight:400;color:var(--muted);font-size:11px;margin-top:3px;line-height:1.4}.number-field{display:flex;align-items:baseline;justify-content:flex-end;gap:7px;border:1px solid transparent;background:var(--surface);border-radius:9px;padding:11px 10px;min-height:48px;min-width:92px;max-width:57%;font-variant-numeric:tabular-nums}.number-field strong{font-size:19px;font-weight:650;overflow-wrap:anywhere;min-width:0}.number-field span{font-size:10px;color:var(--muted);white-space:nowrap}.field-row.active .number-field{border-color:var(--blue);background:var(--soft)}.unit-options{display:flex;align-items:center;justify-content:flex-end;gap:10px;margin:2px 0 5px;font-size:10px;color:var(--muted)}.unit-options select{border:0;background:transparent;min-height:32px;font-size:11px;padding:4px 18px 4px 5px;color:var(--blue)}.unit-options button{background:none;border:0;color:var(--blue);min-height:32px;font-size:11px}.related-result{display:flex;gap:10px;align-items:center;justify-content:space-between;border-bottom:1px solid var(--line);padding:13px 0;min-height:48px;font-size:13px}.related-result strong{font-weight:600;font-variant-numeric:tabular-nums;text-align:right}.related-result small{font-weight:400;color:var(--muted)}.action-row{display:flex;gap:10px;margin:20px 0}.action-row>*{flex:1}.primary,.secondary{border:0;border-radius:10px;display:inline-flex;align-items:center;justify-content:center;gap:8px;min-height:48px;padding:12px 17px;font-size:14px;font-weight:600}.primary{background:var(--blue);color:#fff}.secondary{background:var(--soft);color:var(--blue)}.wide{width:100%;margin:8px 0}.text-button{border:0;background:transparent;color:var(--blue);min-height:48px;font-size:13px;padding:10px 0}.danger{color:var(--danger)}.explanation{border-top:1px solid var(--line);margin:20px 0 0;padding:6px 0;color:var(--muted);font-size:13px}.explanation>summary{display:flex;align-items:center;gap:10px;min-height:48px;font-size:14px;cursor:pointer;color:var(--text);list-style:none}.explanation summary>svg:last-child{margin-left:auto;color:var(--muted)}.explanation[open] summary>svg:last-child{transform:rotate(90deg)}.formula{padding:12px;background:var(--surface);border-radius:9px;overflow-wrap:anywhere;font-variant-numeric:tabular-nums}.warning{padding:13px 15px;background:var(--warning);color:var(--danger);font-size:13px;border-radius:9px;margin:12px 0}.empty{text-align:center;padding:40px 10px;color:var(--muted)}.empty svg{color:var(--blue)}.empty h2{color:var(--text)}.toast{position:absolute;bottom:100px;left:22px;right:22px;background:var(--text);color:var(--bg);border-radius:12px;padding:12px 15px;z-index:20;font-size:13px;text-align:center;box-shadow:var(--shadow)}.numeric-keypad{flex-shrink:0;background:var(--surface);padding:5px 9px max(10px,env(safe-area-inset-bottom));border-top:1px solid var(--line)}.preview .numeric-keypad{padding-bottom:28px}.keypad-caption{display:flex;align-items:center;gap:12px;padding:0 3px 4px;color:var(--muted);font-size:11px;min-height:30px}.keypad-caption>span{margin-right:auto}.keypad-caption button{background:none;border:0;padding:3px;color:var(--blue);font-size:11px;min-height:32px}.key-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:5px}.key-grid button{display:flex;align-items:center;justify-content:center;height:43px;background:var(--bg);border:1px solid var(--line);border-radius:8px;font-size:23px;font-weight:560;font-variant-numeric:tabular-nums}.keypad-actions{display:flex;gap:6px;margin-top:6px}.keypad-actions .primary{flex:1}.keypad-actions .secondary{min-width:48px;padding:10px}.keypad-actions>*{min-height:44px}.select-field{display:flex;gap:12px;align-items:center;justify-content:space-between;min-height:65px;border-bottom:1px solid var(--line);font-size:14px}.select-field span{font-weight:550}.select-field select{max-width:60%;font-size:13px}.text-field{display:flex;flex-direction:column;gap:6px;margin:13px 0;font-size:13px}.text-field span{font-weight:550}.text-field input{width:100%;min-height:48px;border:1px solid var(--line);border-radius:9px;background:var(--surface);color:var(--text);padding:12px;font-size:16px}.panel{margin:15px 0;border:1px solid var(--line);border-radius:12px;padding:12px 14px}.panel>summary{min-height:45px;cursor:pointer;font-weight:600}.panel h2{margin-top:8px}.panel p{font-size:13px}.totals,.trip-totals,.loading-totals{background:var(--surface);padding:15px;border-radius:12px;margin:15px 0}.totals h2{margin:0 0 10px}.history-row{display:flex;align-items:center;border-bottom:1px solid var(--line);padding:10px 0}.history-row>button:first-child{flex:1;border:0;background:none;text-align:left;min-height:55px}.history-row small{display:block;color:var(--muted);font-size:11px;margin-top:4px}.segmented{display:flex;border-radius:10px;background:var(--surface);padding:3px;margin:20px 0;gap:3px}.segmented>button{flex:1;min-height:43px;font-size:13px;background:none;border:0;border-radius:8px;color:var(--muted)}.segmented>button[aria-pressed=true]{background:var(--blue);color:#fff}.timer-face{padding:42px 0;text-align:center;font-variant-numeric:tabular-nums}.timer-face>span{font-size:45px;font-weight:600;letter-spacing:-2px}.timer-face small{display:block;color:var(--muted);font-size:13px;margin-top:9px}.timer-face.overrun{color:var(--danger)}.expression{display:block;margin:20px 0}.expression>span{font-size:12px;color:var(--muted)}.expression input{margin-top:6px;background:var(--surface);border:1px solid var(--line);border-radius:10px;min-height:56px;width:100%;color:var(--text);font-size:23px;text-align:right;padding:10px}.calc-answer{text-align:right;font-size:37px;font-weight:600;min-height:55px;overflow-wrap:anywhere;font-variant-numeric:tabular-nums}.memory-bar{display:flex;justify-content:space-between;margin:10px 0}.memory-bar button{border:0;background:none;color:var(--blue);min-height:44px;font-size:12px}.calculator-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:6px}.calculator-grid button{border:0;background:var(--surface);border-radius:9px;min-height:54px;font-size:23px}.calculator-grid .equals{background:var(--blue);color:white}.file-import{position:relative;width:100%;margin:8px 0;cursor:pointer}.file-import input{position:absolute;inset:0;opacity:0;cursor:pointer;width:100%}.reuse-panel{border:1px solid var(--line);border-radius:12px;padding:12px}.reuse-panel h2{margin:0 0 10px}.check-row{display:flex;gap:10px;align-items:center;min-height:48px;font-size:13px}.check-row input{width:20px;height:20px}.pilot-content details summary{cursor:pointer}.pilot-content dl{display:grid;grid-template-columns:1fr 1fr;gap:8px;font-size:13px}.pilot-content dd{margin:0;text-align:right;font-weight:600}.pilot-content dt{color:var(--muted)}@media(max-width:360px){.pilot-content{padding-left:16px;padding-right:16px}.pilot-header{padding-left:16px;padding-right:16px}.favorite-tile{font-size:12px}.wordmark{font-size:26px}.number-field strong{font-size:17px}.key-grid button{height:40px}}@media(prefers-reduced-motion:reduce){*{scroll-behavior:auto!important;transition:none!important;animation:none!important}}
 
 .editing .tool-intro{display:none}.editing .pilot-content{padding-top:4px}.editing .result-panel{padding:12px 16px;margin:10px 0}.editing .result-number{font-size:42px}.key-grid button{min-height:48px}.bottom-nav button span{font-size:11px}.card-title{display:flex;flex-direction:column;gap:4px}.card-title small{font-size:12px;font-weight:400;color:var(--muted)}
 .editing .result-panel{position:sticky;top:0;z-index:2;background:var(--surface);box-shadow:0 4px 12px var(--bg)}
@@ -8,25 +8,25 @@
 
 .result-number.entry-result{font-size:clamp(32px,9vw,48px)}
 
-.wind-diagram{margin:16px 0;display:flex;align-items:center;gap:16px;padding:12px;background:var(--surface);border-radius:18px}.wind-plot{position:relative;width:140px;height:140px;flex-shrink:0;border:1px solid var(--line);border-radius:50%}.runway-label{position:absolute;top:6px;left:0;width:100%;text-align:center;font-size:10px;color:var(--muted)}.runway-line{position:absolute;top:40px;bottom:28px;left:calc(50% - 6px);width:12px;border:1px solid var(--muted);border-radius:2px}.runway-line:after{content:'';position:absolute;top:5px;bottom:5px;left:5px;border-left:1px dashed var(--muted)}.wind-vector{position:absolute;left:calc(50% - 1px);top:calc(50% - 45px);height:45px;width:2px;background:var(--blue);transform-origin:50% 100%}.wind-vector i{position:absolute;bottom:-2px;left:-4px;border-left:5px solid transparent;border-right:5px solid transparent;border-top:9px solid var(--blue)}.wind-diagram figcaption{font-size:13px;font-weight:600}.wind-diagram small{display:block;color:var(--muted);font-weight:400;font-size:11px;margin-top:6px}
+.wind-diagram{margin:16px 0;display:flex;align-items:center;gap:16px;padding:12px;background:var(--surface);border-radius:18px}.wind-plot{position:relative;width:140px;height:140px;flex-shrink:0;border:1px solid var(--line);border-radius:50%}.runway-label{position:absolute;top:6px;left:0;width:100%;text-align:center;font-size:10px;color:var(--muted)}.runway-line{position:absolute;top:40px;bottom:28px;left:calc(50% - 6px);width:12px;border:1px solid var(--muted);border-radius:2px}.runway-line:after{content:'';position:absolute;top:5px;bottom:5px;left:5px;border-left:1px dashed var(--muted)}.wind-vector{position:absolute;left:calc(50% - 1px);top:calc(50% - 45px);height:45px;width:2px;background:var(--diagram-vector,var(--blue));transform-origin:50% 100%}.wind-vector i{position:absolute;bottom:-2px;left:-4px;border-left:5px solid transparent;border-right:5px solid transparent;border-top:9px solid var(--diagram-vector,var(--blue))}.wind-diagram figcaption{font-size:13px;font-weight:600}.wind-diagram small{display:block;color:var(--muted);font-weight:400;font-size:11px;margin-top:6px}
 
 /* PilotSolve glass material system. Controls float; flight data stays high contrast. */
 .pilot-app {
-  --bg: #f6f9fe; --surface: #f0f5fc; --text: #14223b; --muted: #607089;
-  --blue: #0968de; --soft: #e6f0ff; --line: #d9e3f1;
-  --glass: rgba(255,255,255,.73); --glass-edge: rgba(255,255,255,.96);
-  --glass-shadow: 0 8px 24px #24529c0b, 0 1px 3px #2448750a;
-  --press-ease: cubic-bezier(.22,1,.36,1);
-  font-family: 'Inter Variable', -apple-system, BlinkMacSystemFont, sans-serif;
-  background: radial-gradient(ellipse at 90% 0%,#dcecff 0,transparent 45%),radial-gradient(ellipse at 0% 80%,#e8e7fb 0,transparent 50%),var(--bg);
+  --bg:var(--g-bg,#E3E3E3);--surface:var(--g-surface,#efefef);--text:var(--g-ink,#272727);--muted:var(--g-muted,#595959);
+  --blue:var(--g-accent,#73551b);--cyan:var(--g-accent,#73551b);--soft:color-mix(in srgb,var(--blue) 12%,var(--surface));--line:var(--g-line,#b7b7b7);
+  --glass:var(--g-glass,rgba(241,241,241,.86));--glass-edge:var(--g-glass-edge,rgba(255,255,255,.8));
+  --glass-shadow:var(--g-shadow,0 3px 5px #0002,0 9px 20px #0000000b);
+  --press-ease:cubic-bezier(.22,1,.36,1);
+  font-family:'Inter Variable',-apple-system,BlinkMacSystemFont,sans-serif;
+  background:var(--bg);
   isolation: isolate;
 }
 .pilot-app[data-theme=dark] {
-  --bg: #0d1625; --surface: #18273b; --text: #eff5ff; --muted: #a5b7ce;
-  --blue: #76baff; --soft: #203b5d; --line: #30445f;
-  --glass: rgba(27,43,64,.82); --glass-edge: rgba(178,210,255,.19);
-  --glass-shadow: 0 8px 28px #0003, 0 1px 2px #0002;
-  background: radial-gradient(ellipse at 95% 0%,#1d3957 0,transparent 45%),radial-gradient(ellipse at 0% 80%,#1b2040 0,transparent 55%),var(--bg);
+  --bg:var(--g-bg,#414141);--surface:var(--g-surface,#4a4a4a);--text:var(--g-ink,#f4f4f4);--muted:var(--g-muted,#d0d0d0);
+  --blue:var(--g-accent,#ecd39b);--cyan:var(--g-accent,#ecd39b);--soft:color-mix(in srgb,var(--blue) 12%,var(--surface));--line:var(--g-line,#767676);
+  --glass:var(--g-glass,rgba(73,73,73,.89));--glass-edge:var(--g-glass-edge,rgba(255,255,255,.23));
+  --glass-shadow:var(--g-shadow,0 3px 6px #0003,0 10px 24px #0002);
+  background:var(--bg);
 }
 .pilot-app h1,.pilot-app h2,.pilot-app .wordmark {font-family:'Manrope Variable',sans-serif}
 .pilot-app h1 {font-weight:760;letter-spacing:-1.1px;font-size:29px;line-height:1.2}
@@ -51,66 +51,64 @@
 .favorites-grid {gap:12px;margin-top:18px;margin-bottom:30px}
 .favorite-tile {border-radius:24px;min-height:128px;font-size:13px;font-weight:590;gap:14px;position:relative;overflow:hidden}
 .favorite-tile:before {content:'';position:absolute;inset:0;pointer-events:none;background:linear-gradient(135deg,#fff5,transparent 55%);opacity:.5}
-.favorite-tile svg {filter:drop-shadow(0 2px 3px #3a89ff1a);stroke-width:1.7}
+.favorite-tile svg {filter:drop-shadow(0 2px 3px #0002);stroke-width:1.7}
 .tool-row {min-height:76px;gap:13px;border-color:var(--line);padding:14px 1px;transition:background .18s,transform .18s var(--press-ease)}
 .tool-row strong {font-size:14px;font-weight:620;letter-spacing:-.25px}
 .tool-row small {font-size:11px;margin-top:4px;line-height:1.4}
 .bottom-nav {
   position:absolute;z-index:8;bottom:max(12px,env(safe-area-inset-bottom));left:12px;right:12px;
   border:1px solid var(--glass-edge);border-radius:28px;padding:6px;min-height:72px;
-  background:color-mix(in srgb,var(--bg) 94%,transparent);box-shadow:0 12px 36px #143e711c,inset 0 1px 0 var(--glass-edge),0 0 0 1px #56799b0a;
+  background:color-mix(in srgb,var(--bg) 94%,transparent);box-shadow:0 12px 36px #0002,inset 0 1px 0 var(--glass-edge),0 0 0 1px #0001;
   backdrop-filter:blur(28px) saturate(170%);-webkit-backdrop-filter:blur(28px) saturate(170%);
 }
 .preview .bottom-nav {min-height:72px;padding:6px;bottom:26px}
 .bottom-nav button {border-radius:21px;gap:4px;padding:7px 0;min-width:0;min-height:58px;position:relative;transition:background .24s var(--press-ease),color .2s,transform .2s var(--press-ease)}
-.bottom-nav button[aria-current=page] {background:linear-gradient(145deg,#cde6ffbf,#eaf4ffbd);box-shadow:inset 0 1px 0 #fff9,0 2px 5px #2b6cb21a;color:#0864c8}
-[data-theme=dark] .bottom-nav button[aria-current=page] {background:linear-gradient(145deg,#346493aa,#24486bbd);box-shadow:inset 0 1px 0 #a4d3ff35;color:#b5dcff}
 .bottom-nav button span {font-size:10px;font-weight:550;letter-spacing:-.2px}
 .bottom-nav svg {width:22px;height:22px}
 .pilot-app button {touch-action:manipulation;transition:transform .18s var(--press-ease),box-shadow .2s,background .2s,color .2s;transform-origin:center}
 .pilot-app button:not(:disabled):active {transform:scale(.965)}
-.pilot-app .key-grid button:not(:disabled):active,.pilot-app .calculator-grid button:not(:disabled):active {transform:scale(.94);background:var(--soft);box-shadow:inset 0 2px 5px #12334b1c}
+.pilot-app .key-grid button:not(:disabled):active,.pilot-app .calculator-grid button:not(:disabled):active {transform:scale(.94);background:var(--soft);box-shadow:inset 0 2px 5px #0002}
 .primary,.secondary {border-radius:18px;min-height:50px;font-size:13px;font-weight:650;border:1px solid transparent}
-.primary,.calculator-grid .equals {color:#fff;background:linear-gradient(155deg,#3297ff,#0869e9 80%);border-color:#1876ea;box-shadow:inset 0 1px 0 #a0d4ff9c,0 5px 12px #0875ee26}
+.primary,.calculator-grid .equals {color:var(--g-key-ink,#2b2316);background:var(--g-gold,linear-gradient(165deg,#fff0b4,#d8a735));border-color:var(--g-edge,#9a701b);box-shadow:inset 0 1px #fff8,var(--shadow)}
 .secondary {background:var(--glass);border-color:var(--glass-edge);box-shadow:var(--glass-shadow),inset 0 1px 0 var(--glass-edge);color:var(--blue)}
 .pilot-app select {border-radius:15px;background:var(--glass);box-shadow:inset 0 1px 0 var(--glass-edge);border-color:var(--line);font-size:13px}
 .solve-select {margin-top:17px}.solve-select select{font-size:13px}
-.result-panel {border:1px solid var(--glass-edge);border-radius:25px;padding:22px 16px 20px;background:linear-gradient(145deg,#fafdff,#e5f0ff);box-shadow:var(--glass-shadow),inset 0 1px 0 #fff;position:relative}
-[data-theme=dark] .result-panel {background:linear-gradient(145deg,#263e5c,#1b2d45);box-shadow:var(--glass-shadow),inset 0 1px 0 #aad0ff21}
+.result-panel {border:1px solid var(--glass-edge);border-radius:25px;padding:22px 16px 20px;background:var(--surface);box-shadow:var(--glass-shadow),inset 0 1px 0 #fff4;position:relative}
+[data-theme=dark] .result-panel {background:var(--surface);box-shadow:var(--glass-shadow),inset 0 1px 0 #fff2}
 .result-number {font-size:53px;font-weight:650;letter-spacing:-2px;font-feature-settings:'tnum' 1,'cv02' 1;line-height:1.25;margin:5px 0}
 .result-number>.result-value {font:inherit;font-feature-settings:inherit;color:var(--text);letter-spacing:inherit;margin:0}
 .result-number>span:not(.result-value) {font-size:15px;font-weight:550;color:var(--blue)}
 .result-label {font-size:12px;font-weight:550}.result-panel small {font-size:10px}
-.number-field {background:var(--glass);border:1px solid var(--glass-edge);border-radius:15px;padding:11px 12px;box-shadow:inset 0 1px 0 var(--glass-edge),0 2px 6px #25446e06}
+.number-field {background:var(--glass);border:1px solid var(--glass-edge);border-radius:15px;padding:11px 12px;box-shadow:inset 0 1px 0 var(--glass-edge),0 2px 6px #0001}
 .number-field strong {font-size:20px;font-weight:620;letter-spacing:-.6px}
-.field-row.active .number-field {box-shadow:0 0 0 3px #2d8dff17,inset 0 1px 0 var(--glass-edge);border-color:var(--blue);background:var(--soft)}
+.field-row.active .number-field {box-shadow:0 0 0 3px color-mix(in srgb,var(--g-edge,#9a701b) 18%,transparent),inset 0 1px 0 var(--glass-edge);border-color:var(--g-edge,#9a701b);background:var(--soft)}
 .field-label{font-size:13px;font-weight:580}.field-label small{font-size:10px;line-height:1.5}
 .unit-options select {background:transparent;box-shadow:none;border:0;border-radius:10px;font-size:11px;min-height:38px}
 .unit-options button {min-height:38px}.unit-options{font-size:10px}
 .segmented {padding:4px;gap:3px;border:1px solid var(--glass-edge);border-radius:19px;background:var(--glass);box-shadow:inset 0 1px 0 var(--glass-edge),var(--glass-shadow)}
 .segmented>button {min-height:44px;border-radius:14px;font-size:12px;font-weight:600}
-.segmented>button[aria-pressed=true] {background:linear-gradient(155deg,#399aff,#0869e9);box-shadow:inset 0 1px 0 #b9dfff80,0 3px 8px #176ed633;color:#fff}
-.numeric-keypad {border:1px solid var(--glass-edge);border-bottom:0;border-radius:26px 26px 0 0;background:var(--glass);box-shadow:0 -8px 30px #17304d0c;backdrop-filter:blur(25px) saturate(150%);-webkit-backdrop-filter:blur(25px) saturate(150%);padding-top:7px;position:relative;z-index:9}
-.key-grid{gap:6px}.key-grid button{border-radius:13px;background:var(--glass);border-color:var(--glass-edge);box-shadow:0 2px 2px #102c4d0b,inset 0 1px 0 var(--glass-edge);font-size:24px;font-weight:540;min-height:48px}
+.segmented>button[aria-pressed=true] {background:var(--g-gold,linear-gradient(165deg,#fff0b4,#d8a735));box-shadow:inset 0 1px #fff8,var(--shadow);color:var(--g-key-ink,#2b2316)}
+.numeric-keypad {border:1px solid var(--glass-edge);border-bottom:0;border-radius:26px 26px 0 0;background:var(--glass);box-shadow:0 -8px 30px #0001;backdrop-filter:blur(25px) saturate(150%);-webkit-backdrop-filter:blur(25px) saturate(150%);padding-top:7px;position:relative;z-index:9}
+.key-grid{gap:6px}.key-grid button{border-radius:13px;background:var(--glass);border-color:var(--glass-edge);box-shadow:0 2px 2px #0001,inset 0 1px 0 var(--glass-edge);font-size:24px;font-weight:540;min-height:48px}
 .keypad-actions .primary,.keypad-actions .secondary{border-radius:14px;min-height:45px}
 .keypad-caption{font-size:11px}.keypad-caption button{min-width:34px}
 .editing .pilot-content{padding-bottom:20px}.editing .result-panel{background:var(--surface);box-shadow:0 7px 18px var(--bg);border-radius:20px;padding:12px}
 .panel,.reuse-panel,.appearance-panel{border-radius:22px}.appearance-panel{padding:15px;margin:20px 0}.appearance-panel .segmented{margin:8px 0 6px;box-shadow:none}
 .pilot-app .text-field input,.expression input{background:var(--glass);border-radius:15px;border-color:var(--line);box-shadow:inset 0 1px 0 var(--glass-edge)}
 .calculator-grid{gap:9px}.calculator-grid button{border-radius:18px;background:var(--glass);box-shadow:var(--glass-shadow),inset 0 1px 0 var(--glass-edge);border:1px solid var(--glass-edge);min-height:57px}
-.calculator-grid .equals{background:linear-gradient(155deg,#3297ff,#0869e9 80%)}
+.calculator-grid .equals{background:var(--g-gold,linear-gradient(165deg,#fff0b4,#d8a735))}
 .calc-answer{font-weight:620;font-size:43px;letter-spacing:-1.5px;padding-bottom:8px}
 .glass-switch-row{display:flex;align-items:center;justify-content:space-between;gap:14px;min-height:68px;padding:10px 0;cursor:pointer}
 .switch-copy{min-width:0;flex:1}.switch-copy strong{display:block;font-size:13px;font-weight:590}.switch-copy small{display:block;font-size:11px;line-height:1.5;color:var(--muted);margin-top:4px}
 .glass-switch{position:relative;display:block;width:51px;height:42px;flex-shrink:0}.glass-switch input{position:absolute;inset:0;width:100%;height:100%;margin:0;opacity:0;cursor:pointer;z-index:2}
-.switch-track{display:block;position:absolute;left:0;right:0;top:5px;height:32px;border-radius:20px;background:#9cadc44d;border:1px solid #90a4bf4d;box-shadow:inset 0 1px 3px #17304d13;transition:background .2s,border-color .2s}
-.switch-thumb{display:block;width:26px;height:26px;border-radius:50%;position:absolute;top:2px;left:2px;background:linear-gradient(145deg,#fff,#eaf0f8);box-shadow:0 2px 5px #18345435,inset 0 1px 0 white;transition:transform .25s cubic-bezier(.2,1.5,.45,1),width .15s}
-.glass-switch input:checked+.switch-track{background:#0877ed;border-color:#0871df}.glass-switch input:checked+.switch-track .switch-thumb{transform:translateX(19px)}
+.switch-track{display:block;position:absolute;left:0;right:0;top:5px;height:32px;border-radius:20px;background:var(--g-well);border:1px solid var(--g-line);box-shadow:inset 0 2px 4px #0003;transition:background .2s,border-color .2s}
+.switch-thumb{display:block;width:26px;height:26px;border-radius:50%;position:absolute;top:2px;left:2px;background:linear-gradient(145deg,var(--g-raised),var(--g-surface));box-shadow:0 2px 5px #0003,inset 0 1px 0 #ffffff66;transition:transform .25s cubic-bezier(.2,1.5,.45,1),width .15s}
+.glass-switch input:checked+.switch-track{background:var(--g-gold);border-color:var(--g-edge)}.glass-switch input:checked+.switch-track .switch-thumb{transform:translateX(19px)}
 .glass-switch input:active+.switch-track .switch-thumb{width:29px}.glass-switch input:checked:active+.switch-track .switch-thumb{transform:translateX(16px)}
 .glass-switch input:focus-visible+.switch-track{outline:3px solid var(--blue);outline-offset:3px}.glass-switch-row.is-disabled{opacity:.5}
-.toast{bottom:102px;background:var(--text);border:1px solid var(--glass-edge);box-shadow:0 8px 25px #12334b24;border-radius:20px;animation:toast-arrive .22s var(--press-ease)}
+.toast{bottom:102px;background:var(--text);border:1px solid var(--glass-edge);box-shadow:0 8px 25px #0003;border-radius:20px;animation:toast-arrive .22s var(--press-ease)}
 @keyframes toast-arrive{from{opacity:0;transform:translateY(8px)}to{opacity:1;transform:translateY(0)}}
-@media(hover:hover){.pilot-app button:not(:disabled):hover{filter:brightness(1.035)}.favorite-tile:hover{box-shadow:0 10px 28px #1c5cb519,inset 0 1px 0 var(--glass-edge)}.tool-row:hover{background:var(--glass);border-radius:12px}}
+@media(hover:hover){.pilot-app button:not(:disabled):hover{filter:brightness(1.035)}.favorite-tile:hover{box-shadow:0 10px 28px #0002,inset 0 1px 0 var(--glass-edge)}.tool-row:hover{background:var(--glass);border-radius:12px}}
 @media(max-width:360px){.pilot-app .wordmark{font-size:25px}.designer-credit{font-size:9px}.header-actions{gap:3px}.header-actions .icon-button{width:36px;min-width:36px;height:40px;min-height:40px}.pilot-app h1{font-size:27px}.favorites-grid{gap:10px}.favorite-tile{font-size:12px;min-height:121px}.bottom-nav{left:8px;right:8px;padding:4px;border-radius:25px}.bottom-nav button span{font-size:9px}.result-number{font-size:47px}.field-label{font-size:12px}}
 .pilot-app[data-glass=off]{--glass:var(--surface);--glass-edge:var(--line)}
 .pilot-app[data-glass=off] .bottom-nav{background:var(--surface)}
@@ -123,17 +121,14 @@
 .header-actions .icon-button{min-height:44px;height:44px;min-width:44px;width:44px}
 .pilot-header{background:transparent}
 
-.bottom-nav button[aria-current=page],.pilot-app[data-theme=dark] .bottom-nav button[aria-current=page]{background:transparent;box-shadow:none}
-.bottom-nav .nav-selection{position:absolute;inset:0;border-radius:21px;background:linear-gradient(145deg,#cde6ffbf,#eaf4ffbd);box-shadow:inset 0 1px 0 #fff9,0 2px 5px #2b6cb21a;z-index:0}
-[data-theme=dark] .bottom-nav .nav-selection{background:linear-gradient(145deg,#346493aa,#24486bbd);box-shadow:inset 0 1px 0 #a4d3ff35}
 .bottom-nav button>svg,.bottom-nav button>span:not(.nav-selection){position:relative;z-index:1}
 
 .phase-inputs{padding:16px;margin:18px 0}.phase-inputs>.glass-switch-row{padding:0}.phase-inputs .segmented{margin:12px 0}.phase-overview{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin:16px 0}.phase-overview>div{display:flex;flex-direction:column;gap:5px;padding:15px 12px;background:var(--soft);border:1px solid var(--glass-edge);border-radius:20px}.phase-overview span,.phase-overview small{font-size:11px;color:var(--muted)}.phase-overview strong{font-size:18px;color:var(--blue)}.nav-segment{padding:16px;margin:12px 0}.nav-segment-title{display:flex;align-items:center;gap:9px;flex-wrap:wrap;font-size:14px;margin-bottom:12px}.phase-badge{font-size:11px;padding:5px 9px;border-radius:12px;background:var(--soft);color:var(--blue)}.nav-segment .result-grid{grid-template-columns:1fr 1fr}.nav-segment .result-grid b{font-size:12px;overflow-wrap:anywhere}.nav-log .totals-card{padding:18px}
 .nav-segment .result-grid{display:grid;gap:16px 12px}.nav-segment .result-grid>span{display:flex;flex-direction:column;gap:5px;font-size:10px;line-height:1.5;color:var(--muted)}.nav-segment .result-grid b{color:var(--text);font-weight:600;font-variant-numeric:tabular-nums}
 
 /* Safari samples the document surface outside the app's dynamic viewport. */
-html[data-pilot-theme=dark],html[data-pilot-theme=dark] body,html[data-pilot-theme=dark] #root{background-color:#0d1625;color-scheme:dark}
-html[data-pilot-theme=light],html[data-pilot-theme=light] body,html[data-pilot-theme=light] #root{background-color:#f6f9fe;color-scheme:light}
+html[data-pilot-theme=dark],html[data-pilot-theme=dark] body,html[data-pilot-theme=dark] #root{background-color:var(--g-bg,#414141);color-scheme:dark}
+html[data-pilot-theme=light],html[data-pilot-theme=light] body,html[data-pilot-theme=light] #root{background-color:var(--g-bg,#E3E3E3);color-scheme:light}
 html[data-pilot-theme] body{overscroll-behavior-y:none}
 /* Compact chrome: 56px working header, plus only the actual device safe area. */
 .pilot-app .pilot-header{min-height:56px;height:56px;padding:6px 16px;gap:8px;flex-shrink:0}
@@ -205,10 +200,10 @@
 .helper-picker{border:1px solid var(--line);border-radius:14px;padding:10px;background:var(--surface);margin:8px 0}.helper-picker-title{display:flex;align-items:center;justify-content:space-between;gap:8px}.pilot-app .helper-picker h2{margin:0;font-size:15px}.helper-picker .tool-row{padding:8px 0;min-height:52px}.helper-picker .tool-row strong{font-size:12px}.helper-picker .tool-row small{font-size:11px}.helper-picker .quiet{font-size:11px}
 
 /* Calm compact system: content hierarchy replaces decorative containers. */
-.pilot-app{background:var(--bg);--glass-shadow:0 2px 8px #17304d08}
+.pilot-app{background:var(--bg);--glass-shadow:0 2px 8px #0001}
 .pilot-app .pilot-content{padding:8px 16px 104px}
 .pilot-app.editing .pilot-content{padding-bottom:16px}
-.pilot-app .bottom-nav{grid-template-columns:repeat(3,1fr);min-height:64px;padding:4px;border-radius:22px;box-shadow:0 3px 14px #17304d0b}
+.pilot-app .bottom-nav{grid-template-columns:repeat(3,1fr);min-height:64px;padding:4px;border-radius:22px;box-shadow:0 3px 14px #0002}
 .pilot-app .bottom-nav button{min-height:54px;border-radius:18px}.pilot-app .bottom-nav button span{font-size:12px}
 .pilot-app .nav-selection{border-radius:18px;box-shadow:none}
 .pilot-app .favorite-tile{min-height:104px;padding:12px;box-shadow:none;background:var(--surface);border-radius:18px}
@@ -242,8 +237,8 @@
 .pilot-app .helper-return{padding:4px 12px}.pilot-app .helper-return small{margin-top:0}
 .pilot-app .helper-picker{border:0;padding:0;margin:0;background:none}.pilot-app .helper-picker .tool-row{width:100%;text-align:left}
 .pilot-app .helper-picker .tool-row strong{font-size:14px}.pilot-app .helper-picker .tool-row small{font-size:12px}
-.calculation-sheet{padding:0;position:fixed;inset:auto 0 0;margin:0 auto;width:min(100%,500px);max-width:100%;max-height:75dvh;border:1px solid var(--line);border-radius:22px 22px 0 0;background:var(--bg);color:var(--text);overflow:auto;animation:sheet-enter .2s ease-out}
-.calculation-sheet::backdrop{background:#08172b55}.sheet-inner{padding:16px 16px max(16px,env(safe-area-inset-bottom))}.sheet-heading{display:flex;align-items:center;justify-content:space-between;gap:8px}.pilot-app .sheet-heading h2{margin:0;font-size:18px}.sheet-heading button{min-width:44px;min-height:44px;border:0;background:transparent;color:var(--muted);font-size:26px}
+.calculation-sheet{padding:0;position:fixed;inset:auto 0 0;margin:0 auto;width:min(100%,500px);max-width:100%;max-height:75dvh;border:1px solid var(--line);border-radius:22px 22px 0 0;background:var(--bg);color:var(--text);font-size:15px;line-height:1.45;letter-spacing:normal;overflow:auto;animation:sheet-enter .2s ease-out}
+.calculation-sheet::backdrop{background:#0009}.sheet-inner{padding:16px 16px max(16px,env(safe-area-inset-bottom))}.sheet-heading{display:flex;align-items:center;justify-content:space-between;gap:8px}.pilot-app .sheet-heading h2{margin:0;font-size:18px}.sheet-heading button{min-width:44px;min-height:44px;border:0;background:transparent;color:var(--muted);font-size:26px}
 .pilot-app .running-timer{position:relative}.running-timer .live-dot{position:absolute;right:6px;top:6px}
 .pilot-app .toast{pointer-events:none;font-size:13px}.pilot-app.editing .toast{bottom:calc(212px + env(safe-area-inset-bottom))}
 @keyframes sheet-enter{from{transform:translateY(16px);opacity:0}to{transform:translateY(0);opacity:1}}
@@ -253,10 +248,10 @@
 
 /* Pista avionics system — final app-owned visual layer. */
 .pilot-app{
---bg:#f5f7fa;--surface:#e8edf3;--raised:#f8fafc;--text:#0d1c33;--muted:#526276;--line:#bdcad8;--blue:#2465d8;--cyan:#087989;--green:#18a56b;--soft:#e6f1ff;--shadow:0 1px 2px #14233a16,0 5px 14px #14233a0d;
+--bg:var(--g-bg,#E3E3E3);--surface:var(--g-surface,#efefef);--raised:var(--g-raised,#f7f7f7);--text:var(--g-ink,#272727);--muted:var(--g-muted,#595959);--line:var(--g-line,#b7b7b7);--blue:var(--g-accent,#73551b);--cyan:var(--g-accent,#73551b);--green:#397a43;--soft:color-mix(in srgb,var(--blue) 12%,var(--surface));--shadow:var(--g-shadow,0 3px 5px #0002,0 9px 20px #0000000b);
 font-family:'Inter Variable',-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif;background:var(--bg);color:var(--text)
 }
-.pilot-app[data-theme=dark]{--bg:#080b10;--surface:#171c23;--raised:#252c35;--text:#f3f7fb;--muted:#a9b6c6;--line:#3a4553;--blue:#2465d8;--cyan:#67ddeb;--green:#45d985;--soft:#102536;--shadow:0 1px 1px #0009,0 7px 18px #0005;color-scheme:dark}
+.pilot-app[data-theme=dark]{--bg:var(--g-bg,#414141);--surface:var(--g-surface,#4a4a4a);--raised:var(--g-raised,#545454);--text:var(--g-ink,#f4f4f4);--muted:var(--g-muted,#d0d0d0);--line:var(--g-line,#767676);--blue:var(--g-accent,#ecd39b);--cyan:var(--g-accent,#ecd39b);--green:#9ed890;--soft:color-mix(in srgb,var(--blue) 12%,var(--surface));--shadow:var(--g-shadow,0 3px 6px #0003,0 10px 24px #0002);color-scheme:dark}
 .pilot-app :is(h1,h2,h3,.wordmark,.result-number,.calc-answer){font-family:'Manrope Variable','Inter Variable',sans-serif}
 .pilot-app :is(button,input,select){font-family:'Inter Variable',-apple-system,sans-serif}
 .pilot-app button:focus-visible,.pilot-app input:focus-visible,.pilot-app select:focus-visible,.pilot-app summary:focus-visible{outline:2px solid var(--cyan);outline-offset:2px}
@@ -282,25 +277,25 @@
 .pilot-app .bottom-nav{position:static;left:auto;right:auto;bottom:auto;flex-shrink:0;display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:6px;min-height:68px;padding:6px max(8px,env(safe-area-inset-right)) max(7px,env(safe-area-inset-bottom)) max(8px,env(safe-area-inset-left));border-top:1px solid var(--line);border-radius:0;background:var(--bg);box-shadow:none}
 .pilot-app .bottom-nav button{position:relative;min-height:52px;border:1px solid var(--line);border-radius:7px;background:var(--surface);color:var(--muted);overflow:hidden}.pilot-app .bottom-nav button[aria-current=page]{color:var(--cyan);border-color:var(--cyan);background:var(--raised)}.pilot-app .bottom-nav button[aria-current=page]::after{content:'';position:absolute;width:29px;height:3px;bottom:4px;left:50%;transform:translateX(-50%);border-radius:2px;background:var(--green)}.pilot-app .nav-selection{display:none}
 .pilot-app .result-panel{position:static;background:var(--surface);border:1px solid var(--line);border-radius:7px;box-shadow:inset 0 1px 0 #ffffff12,var(--shadow);padding:11px 12px;margin:8px 0 11px}.pilot-app .result-label{font-size:10px;letter-spacing:1.2px;font-weight:720;text-transform:uppercase}.pilot-app .result-number{font-size:38px;line-height:1.15;color:var(--cyan)}.pilot-app .result-panel.pending .result-number{color:var(--muted)}.pilot-app .result-panel progress{accent-color:var(--green);height:4px}.pilot-app .result-panel small{color:var(--muted)}
-.pilot-app .field-row{padding:5px 0;min-height:58px;border:0;gap:8px}.pilot-app .field-label{font-size:13px;font-weight:650;overflow-wrap:anywhere}.pilot-app .field-value{max-width:64%}.pilot-app .number-field{min-height:46px;background:var(--surface);border:1px solid var(--line);border-radius:7px;box-shadow:inset 0 1px 0 #ffffff0d}.pilot-app .number-field strong{color:var(--cyan)}.pilot-app .field-row.active .number-field{border-color:var(--cyan);background:#0e2633;box-shadow:0 0 0 1px var(--cyan),inset 0 1px 0 #ffffff12}.pilot-app[data-theme=light] .field-row.active .number-field{background:#e0f6f7}.pilot-app .field-value .unit-cycle,.pilot-app .unit-options .unit-cycle,.pilot-app .result-number .unit-cycle{min-height:44px;border:1px solid var(--line);border-radius:6px;background:var(--raised);color:var(--text)}
-.pilot-app .choice-trigger{width:100%;min-height:48px;padding:8px 11px;display:flex;align-items:center;justify-content:space-between;gap:10px;text-align:left;border:1px solid var(--line);border-radius:7px;background:var(--surface);color:var(--text);box-shadow:inset 0 1px 0 #ffffff12}.pilot-app .choice-trigger>span{display:flex;flex-direction:column;min-width:0}.pilot-app .choice-trigger span span:first-child{font-size:10px;letter-spacing:1px;text-transform:uppercase;color:var(--muted)}.pilot-app .choice-trigger span span:last-child{font-weight:700;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.pilot-app .choice-trigger svg{color:var(--cyan);flex-shrink:0}.pilot-app .choice-list{display:flex;flex-direction:column;gap:6px}.pilot-app .choice-option{width:100%;min-height:48px;padding:9px 10px;display:flex;align-items:center;gap:9px;text-align:left;background:var(--surface);border:1px solid var(--line);border-radius:7px;color:var(--text)}.pilot-app .choice-option[aria-pressed=true]{border-color:var(--cyan);background:var(--soft);box-shadow:inset 3px 0 var(--green)}.pilot-app .choice-copy{display:flex;flex-direction:column;min-width:0;flex:1}.pilot-app .choice-copy small{color:var(--muted);font-size:11px}
-.pilot-app .primary,.pilot-app .secondary{min-height:46px;border-radius:7px;font-weight:700;box-shadow:inset 0 1px 0 #ffffff1c}.pilot-app .primary{background:#2465d8;color:#fff;border:1px solid #3d7cf0}.pilot-app .secondary{background:var(--raised);color:var(--text);border:1px solid var(--line)}.pilot-app .result-actions{display:grid;grid-template-columns:1fr 1.25fr;gap:8px;margin:11px 0}.pilot-app .result-actions .secondary{background:var(--surface)}
-.pilot-app .numeric-keypad{background:var(--surface);border:1px solid var(--line);border-bottom:0;border-radius:8px 8px 0 0;box-shadow:0 -5px 15px #0003,inset 0 1px 0 #ffffff12}.pilot-app .keypad-caption{min-height:40px;color:var(--muted);border-bottom:1px solid var(--line)}.pilot-app .keypad-caption>span{font-size:11px}.pilot-app .keypad-caption button{color:var(--cyan);border-radius:6px}.pilot-app .keypad-caption .keypad-done{background:var(--blue);color:#fff}.pilot-app .key-grid button{min-height:44px;height:44px;background:var(--raised);border:1px solid var(--line);border-radius:6px;box-shadow:inset 0 1px 0 #ffffff12;color:var(--text)}.pilot-app .key-grid .key-action{background:var(--surface);color:var(--cyan)}.pilot-app .key-grid .key-done{background:var(--blue);color:#fff;border-color:#3d7cf0}
-.pilot-app .calculate-input{color:var(--text)}.pilot-app .calculate-input svg{color:var(--cyan)}.pilot-app .helper-return{background:var(--surface);border-top:1px solid var(--line);border-bottom:1px solid var(--line)}.pilot-app .helper-picker .tool-row{margin:5px 0}.pilot-app .explanation{border-color:var(--line)}.pilot-app .explanation>summary{color:var(--text)}.pilot-app .formula,.pilot-app .warning{background:var(--raised);border:1px solid var(--line);border-radius:7px}.pilot-app .segmented{background:var(--surface);border:1px solid var(--line);border-radius:7px}.pilot-app .segmented>button{border-radius:5px}.pilot-app .segmented>button[aria-pressed=true]{background:var(--blue);box-shadow:none}
+.pilot-app .field-row{padding:5px 0;min-height:58px;border:0;gap:8px}.pilot-app .field-label{font-size:13px;font-weight:650;overflow-wrap:anywhere}.pilot-app .field-value{max-width:64%}.pilot-app .number-field{min-height:46px;background:var(--surface);border:1px solid var(--line);border-radius:7px;box-shadow:inset 0 1px 0 #ffffff0d}.pilot-app .number-field strong{color:var(--cyan)}.pilot-app .field-row.active .number-field{border-color:var(--g-edge,#9a701b);background:color-mix(in srgb,var(--g-mid,#e3b741) 18%,var(--surface));box-shadow:0 0 0 1px var(--g-edge,#9a701b),inset 0 1px 0 #ffffff30}.pilot-app .field-value .unit-cycle,.pilot-app .unit-options .unit-cycle,.pilot-app .result-number .unit-cycle{min-height:44px;border:1px solid var(--line);border-radius:6px;background:var(--raised);color:var(--text)}
+.pilot-app .choice-trigger{width:100%;min-height:48px;padding:8px 11px;display:flex;align-items:center;justify-content:space-between;gap:10px;text-align:left;border:1px solid var(--line);border-radius:7px;background:var(--surface);color:var(--text);box-shadow:inset 0 1px 0 #ffffff12}.pilot-app .choice-trigger>span{display:flex;flex-direction:column;min-width:0}.pilot-app .choice-trigger span span:first-child{font-size:10px;letter-spacing:1px;text-transform:uppercase;color:var(--muted)}.pilot-app .choice-trigger span span:last-child{font-weight:700;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.pilot-app .choice-trigger svg{color:var(--cyan);flex-shrink:0}.pilot-app .choice-list{display:flex;flex-direction:column;gap:6px}.pilot-app .choice-option{width:100%;min-height:48px;padding:9px 10px;display:flex;align-items:center;gap:9px;text-align:left;background:var(--surface);border:1px solid var(--line);border-radius:7px;color:var(--text)}.pilot-app .choice-option[aria-pressed=true]{border-color:var(--g-edge,#9a701b);background:var(--g-gold,linear-gradient(165deg,#fff0b4 0%,#ebc55b 23%,#d8a735 40%,#ffdf80 45%,#f4d16a 52%,#d2a23a 79%,#e8c361 100%));color:var(--g-key-ink,#2b2316);box-shadow:inset 0 1px #fff8,0 2px 3px #0002}.pilot-app .choice-copy{display:flex;flex-direction:column;min-width:0;flex:1}.pilot-app .choice-copy small{color:var(--muted);font-size:11px}
+.pilot-app .primary,.pilot-app .secondary{min-height:46px;border-radius:7px;font-weight:700;box-shadow:inset 0 1px 0 #ffffff1c}.pilot-app .secondary{background:var(--raised);color:var(--text);border:1px solid var(--line)}.pilot-app .result-actions{display:grid;grid-template-columns:1fr 1.25fr;gap:8px;margin:11px 0}.pilot-app .result-actions .secondary{background:var(--surface)}
+.pilot-app .numeric-keypad{background:var(--surface);border:1px solid var(--line);border-bottom:0;border-radius:8px 8px 0 0;box-shadow:0 -5px 15px #0003,inset 0 1px 0 #ffffff12}.pilot-app .keypad-caption{min-height:40px;color:var(--muted);border-bottom:1px solid var(--line)}.pilot-app .keypad-caption>span{font-size:11px}.pilot-app .keypad-caption button{color:var(--cyan);border-radius:6px}.pilot-app .keypad-caption .keypad-done{background:var(--blue);color:#fff}.pilot-app .key-grid button{min-height:44px;height:44px;background:var(--raised);border:1px solid var(--line);border-radius:6px;box-shadow:inset 0 1px 0 #ffffff12;color:var(--text)}.pilot-app .key-grid .key-action{background:var(--surface);color:var(--cyan)}.pilot-app .key-grid .key-done{background:var(--blue);color:#fff;border-color:var(--g-edge,#9a701b)}
+.pilot-app .calculate-input{color:var(--text)}.pilot-app .calculate-input svg{color:var(--cyan)}.pilot-app .helper-return{background:var(--surface);border-top:1px solid var(--line);border-bottom:1px solid var(--line)}.pilot-app .helper-picker .tool-row{margin:5px 0}.pilot-app .explanation{border-color:var(--line)}.pilot-app .explanation>summary{color:var(--text)}.pilot-app .formula,.pilot-app .warning{background:var(--raised);border:1px solid var(--line);border-radius:7px}.pilot-app .segmented{background:var(--surface);border:1px solid var(--line);border-radius:7px}.pilot-app .segmented>button{border-radius:5px}.pilot-app .segmented>button[aria-pressed=true]{background:var(--g-gold,linear-gradient(165deg,#fff0b4 0%,#ebc55b 23%,#d8a735 40%,#ffdf80 45%,#f4d16a 52%,#d2a23a 79%,#e8c361 100%));color:var(--g-key-ink,#2b2316);border:1px solid var(--g-edge,#9a701b);box-shadow:inset 0 1px #fff8,0 2px 3px #0002}
 /* Sheet styling is deliberately restricted to PilotSolve-owned surfaces. */
 .pilot-app .bottom-sheet,.pilot-app .calculation-sheet{background:var(--bg);color:var(--text);border-color:var(--line);box-shadow:0 -10px 28px #0006}.pilot-app .sheet-handle{background:var(--line)}.pilot-app .sheet-header{border-color:var(--line)}.pilot-app .sheet-title{color:var(--text)}.pilot-app .sheet-description{color:var(--muted)}.calculation-sheet{background:var(--bg);border-color:var(--line);border-radius:8px 8px 0 0;box-shadow:0 -10px 28px #0006}.calculation-sheet::backdrop{background:#0008}
 @media(max-width:350px){.pilot-app .field-label{font-size:12px}.pilot-app .field-value{max-width:66%}.pilot-app .number-field{min-width:52px;padding:6px}.pilot-app .number-field strong{font-size:16px}.pilot-app .bottom-nav button span{font-size:10px}.pilot-app .result-number{font-size:34px}}
 
 /* BottomSheet portals beside the app inherit this only when PilotSolve owns the screen. */
-.device-screen:has(.pilot-app) .bottom-sheet{--bg:#f5f7fa;--surface:#e8edf3;--raised:#f8fafc;--text:#0d1c33;--muted:#526276;--line:#bdcad8;--blue:#2465d8;--cyan:#087989;--green:#18a56b;--soft:#e6f1ff;font-family:'Inter Variable',-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif;background:var(--bg);color:var(--text);border-color:var(--line);box-shadow:0 -10px 28px #0005}
-.device-screen:has(.pilot-app[data-theme=dark]) .bottom-sheet{--bg:#080b10;--surface:#171c23;--raised:#252c35;--text:#f3f7fb;--muted:#a9b6c6;--line:#3a4553;--blue:#2465d8;--cyan:#67ddeb;--green:#45d985;--soft:#102536;background:var(--bg);color:var(--text);color-scheme:dark}
+.device-screen:has(.pilot-app) .bottom-sheet{--bg:var(--g-bg,#E3E3E3);--surface:var(--g-surface,#efefef);--raised:var(--g-raised,#f7f7f7);--text:var(--g-ink,#272727);--muted:var(--g-muted,#595959);--line:var(--g-line,#b7b7b7);--blue:var(--g-accent,#73551b);--cyan:var(--g-accent,#73551b);--diagram-vector:#175ba9;--green:#397a43;--soft:color-mix(in srgb,var(--blue) 12%,var(--surface));font-family:'Inter Variable',-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif;background:var(--bg);color:var(--text);border-color:var(--line);box-shadow:0 -10px 28px #0005}
+.device-screen:has(.pilot-app[data-theme=dark]) .bottom-sheet{--bg:var(--g-bg,#414141);--surface:var(--g-surface,#4a4a4a);--raised:var(--g-raised,#545454);--text:var(--g-ink,#f4f4f4);--muted:var(--g-muted,#d0d0d0);--line:var(--g-line,#767676);--blue:var(--g-accent,#ecd39b);--cyan:var(--g-accent,#ecd39b);--diagram-vector:#2376dc;--green:#9ed890;--soft:color-mix(in srgb,var(--blue) 12%,var(--surface));background:var(--bg);color:var(--text);color-scheme:dark}
 :is(.pilot-app,.device-screen:has(.pilot-app) .bottom-sheet) .sheet-handle{background:var(--line)}
 :is(.pilot-app,.device-screen:has(.pilot-app) .bottom-sheet) .sheet-header{border-color:var(--line)}
 :is(.pilot-app,.device-screen:has(.pilot-app) .bottom-sheet) :is(.sheet-title,.sheet-heading h2){font-family:'Manrope Variable','Inter Variable',sans-serif;color:var(--text)}
 :is(.pilot-app,.device-screen:has(.pilot-app) .bottom-sheet) .sheet-description{color:var(--muted)}
 :is(.pilot-app,.device-screen:has(.pilot-app) .bottom-sheet) .choice-list{display:flex;flex-direction:column;gap:6px}
 :is(.pilot-app,.device-screen:has(.pilot-app) .bottom-sheet) .choice-option{width:100%;min-height:48px;padding:9px 10px;display:flex;align-items:center;gap:9px;text-align:left;background:var(--surface);border:1px solid var(--line);border-radius:7px;color:var(--text)}
-:is(.pilot-app,.device-screen:has(.pilot-app) .bottom-sheet) .choice-option[aria-pressed=true]{border-color:var(--cyan);background:var(--soft);box-shadow:inset 3px 0 var(--green)}
+:is(.pilot-app,.device-screen:has(.pilot-app) .bottom-sheet) .choice-option[aria-pressed=true]{border-color:var(--g-edge,#9a701b);background:var(--g-gold,linear-gradient(165deg,#fff0b4 0%,#ebc55b 23%,#d8a735 40%,#ffdf80 45%,#f4d16a 52%,#d2a23a 79%,#e8c361 100%));color:var(--g-key-ink,#2b2316);box-shadow:inset 0 1px #fff8,0 2px 3px #0002}
 :is(.pilot-app,.device-screen:has(.pilot-app) .bottom-sheet) .choice-copy{display:flex;flex-direction:column;min-width:0;flex:1}.device-screen:has(.pilot-app) .bottom-sheet .choice-copy small{font-size:11px;color:var(--muted)}.device-screen:has(.pilot-app) .bottom-sheet .choice-copy strong{color:var(--text)}
 :is(.pilot-app,.device-screen:has(.pilot-app) .bottom-sheet) .helper-picker{margin:0;padding:0;border:0;background:none}.device-screen:has(.pilot-app) .bottom-sheet .helper-picker .tool-row,.device-screen:has(.pilot-app) .bottom-sheet .app-menu .tool-row{margin:6px 0;min-height:58px;border:1px solid var(--line);border-radius:7px;background:var(--surface);color:var(--text);box-shadow:inset 0 1px 0 #ffffff10}.device-screen:has(.pilot-app) .bottom-sheet .tool-row small{color:var(--muted)}.device-screen:has(.pilot-app) .bottom-sheet .tool-row>svg:first-child{color:var(--cyan)}
 
@@ -310,6 +305,7 @@
 .pilot-app .result-number{flex-wrap:nowrap;min-width:0}.pilot-app .result-value{min-width:0;overflow-wrap:anywhere;color:var(--cyan)}.pilot-app .result-panel.pending .result-value{color:var(--muted)}
 .pilot-app .header-title{font-size:15px;line-height:1.2;margin:0;flex:1;min-width:0}
 .pilot-app .choice-trigger .choice-copy small,.device-screen:has(.pilot-app) .bottom-sheet .choice-copy small{font-size:10px;letter-spacing:1px;font-weight:720;text-transform:uppercase;color:var(--muted)}.pilot-app .choice-trigger .choice-copy strong,.device-screen:has(.pilot-app) .bottom-sheet .choice-copy strong{font-size:14px;font-weight:720;line-height:1.25;color:var(--text)}
+.calculation-sheet .choice-copy strong{font-size:14px;line-height:1.25;letter-spacing:normal}
 .pilot-app.preview .bottom-nav{padding-bottom:calc(var(--mobile-safe-area-height,24px) + 6px)}.pilot-app.preview .numeric-keypad{padding-bottom:calc(var(--mobile-safe-area-height,24px) + 8px)}
 .pilot-app[data-glass=off] :is(.search-field,.favorite-tile,.panel,.reuse-panel,.appearance-panel,.totals,.trip-totals,.loading-totals,.phase-overview>div,.tool-row,.result-panel,.numeric-keypad,.key-grid button,.bottom-nav button){box-shadow:none!important;backdrop-filter:none!important;-webkit-backdrop-filter:none!important}
 .device-screen:has(.pilot-app) .bottom-sheet :is(.secondary,.text-button,.quiet){min-height:44px}.device-screen:has(.pilot-app) .bottom-sheet .secondary{background:var(--raised);border:1px solid var(--line);border-radius:7px;color:var(--text);font-weight:700}.device-screen:has(.pilot-app) .bottom-sheet .text-button{color:var(--cyan);background:transparent;border:0}.device-screen:has(.pilot-app) .bottom-sheet .quiet{color:var(--muted)}
@@ -344,21 +340,21 @@
 .pilot-app .unit-split .unit-cycle.is-holding::after{transform:scaleX(1);transition:transform 500ms linear}
 @media(prefers-reduced-motion:reduce){.pilot-app .unit-split .unit-cycle.is-holding::after{transition:none}}
 
-/* Suarez CFI avionics integration: matte working surfaces, glass navigation only. */
+/* Suarez CFI PilotSolve integration: shared Polished Aurum materials with original layout geometry. */
 .pilot-app.standalone{max-width:1120px;width:100%;box-shadow:none;border-left:1px solid var(--line);border-right:1px solid var(--line)}
-.pilot-app[data-theme=dark]{--bg:#090d13;--surface:#151b23;--raised:#202935;--text:#eef5f8;--muted:#a8b8c3;--line:#344451;--blue:#2376dc;--cyan:#79d9e7;--green:#65cb98;--soft:#122b39;--shadow:inset 0 1px 0 #ffffff0c,0 2px 6px #0002}
-.pilot-app[data-theme=light]{--bg:#e9eef2;--surface:#f5f8fa;--raised:#fff;--text:#152432;--muted:#4c626f;--line:#aebdc6;--blue:#175ba9;--cyan:#006f84;--green:#168358;--soft:#dcecf4;--shadow:inset 0 1px 0 #ffffffa8,0 2px 6px #19344312}
+.pilot-app[data-theme=dark]{--bg:var(--g-bg,#414141);--surface:var(--g-surface,#4a4a4a);--raised:var(--g-raised,#545454);--well:var(--g-well,#393939);--text:var(--g-ink,#f4f4f4);--muted:var(--g-muted,#d0d0d0);--line:var(--g-line,#767676);--blue:var(--g-accent,#ecd39b);--cyan:var(--g-accent,#ecd39b);--green:#9ed890;--soft:color-mix(in srgb,var(--blue) 12%,var(--surface));--shadow:var(--g-shadow,0 3px 6px #0003,0 10px 24px #0002)}
+.pilot-app[data-theme=light]{--bg:var(--g-bg,#E3E3E3);--surface:var(--g-surface,#efefef);--raised:var(--g-raised,#f7f7f7);--well:var(--g-well,#d6d6d6);--text:var(--g-ink,#272727);--muted:var(--g-muted,#595959);--line:var(--g-line,#b7b7b7);--blue:var(--g-accent,#73551b);--cyan:var(--g-accent,#73551b);--green:#397a43;--soft:color-mix(in srgb,var(--blue) 12%,var(--surface));--shadow:var(--g-shadow,0 3px 5px #0002,0 9px 20px #0000000b)}
 .pilot-app .pilot-header{background:var(--bg);border-bottom:1px solid var(--line)}
 .pilot-app .pilot-content{width:min(100%,960px);margin-inline:auto;padding:16px clamp(16px,3vw,30px) 26px}
 .pilot-app :is(.search-field,.favorite-tile,.panel,.reuse-panel,.appearance-panel,.totals,.trip-totals,.loading-totals,.phase-overview>div,.tool-row,.result-panel,.numeric-keypad,.key-grid button,.calculator-grid button,.number-field,.choice-trigger,.choice-option,.secondary,.segmented,.text-field input,.expression input,select){background:var(--surface);border-color:var(--line);border-radius:7px;box-shadow:var(--shadow);backdrop-filter:none;-webkit-backdrop-filter:none}
-.pilot-app .primary{background:var(--blue);border-color:color-mix(in srgb,var(--blue),white 16%);border-radius:7px;box-shadow:inset 0 1px 0 #ffffff2d}
+.pilot-app :is(.primary,.key-done,.keypad-done,.calculator-grid .equals){background:var(--g-gold,linear-gradient(165deg,#fff0b4 0%,#ebc55b 23%,#d8a735 40%,#ffdf80 45%,#f4d16a 52%,#d2a23a 79%,#e8c361 100%));color:var(--g-key-ink,#2b2316);border:1px solid var(--g-edge,#9a701b);border-radius:7px;box-shadow:inset 0 1px #fff8,0 2px 4px #0002}
 .pilot-app .result-panel{background:var(--surface);box-shadow:inset 0 1px 0 #ffffff10,0 2px 7px #0002}
 .pilot-app .number-field strong,.pilot-app .result-value{color:var(--cyan)}
 .pilot-app .bottom-nav{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:5px;flex:0 0 auto;min-height:64px;margin:7px 10px max(7px,env(safe-area-inset-bottom));padding:5px;border:1px solid color-mix(in srgb,var(--line),white 10%);border-radius:12px;background:color-mix(in srgb,var(--raised) 86%,transparent);box-shadow:0 8px 22px #0006,inset 0 1px 0 #ffffff20;backdrop-filter:blur(20px) saturate(115%);-webkit-backdrop-filter:blur(20px) saturate(115%)}
 .pilot-app .bottom-nav button{min-width:0;min-height:52px;padding:4px 2px;gap:2px;background:transparent;border:1px solid transparent;border-radius:7px;color:var(--text);overflow:visible}
 .pilot-app .bottom-nav button span{font-size:11px;font-weight:700;line-height:1.1;white-space:normal}
-.pilot-app .bottom-nav button[aria-current=page]{background:color-mix(in srgb,var(--blue) 21%,transparent);border-color:var(--cyan);color:var(--text)}
-.pilot-app .bottom-nav button[aria-current=page]::after{background:var(--green);bottom:2px;height:2px;width:30px}
+.pilot-app .bottom-nav button[aria-current=page]{background:var(--g-gold,linear-gradient(165deg,#fff0b4 0%,#ebc55b 23%,#d8a735 40%,#ffdf80 45%,#f4d16a 52%,#d2a23a 79%,#e8c361 100%));border-color:var(--g-edge,#9a701b);color:var(--g-key-ink,#2b2316)}
+.pilot-app .bottom-nav button[aria-current=page]::after{content:none}
 .pilot-app .bottom-nav button:focus-visible{outline:2px solid var(--cyan);outline-offset:1px}
 .pilot-app .site-menu-links{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:6px;padding:14px 0;border-top:1px solid var(--line);margin-top:12px}
 .pilot-app .site-menu-links .eyebrow{grid-column:1/-1;margin:0 0 2px}
@@ -398,3 +394,8 @@
 @media (min-width:700px){.pilot-app [data-tactile-dock]{--dock-expanded:60px}}
 @media (min-width:700px){.pilot-app [data-tactile-dock]{width:calc(100% - 20px);max-width:600px;align-self:center}}
 @media (max-width:699px){.pilot-app [data-tactile-dock]{--dock-expanded:56px}.pilot-app .angle-adjustments{gap:4px}}
+
+/* Keep calculated loading values distinct from their labels, including at narrow widths. */
+.pilot-app .calculated-field{display:flex;align-items:center;justify-content:space-between;flex-wrap:wrap;gap:8px 16px;min-height:58px;padding:5px 0;color:var(--text)}
+.pilot-app .calculated-field small{color:var(--muted)}
+.pilot-app .calculated-field strong{font-variant-numeric:tabular-nums;overflow-wrap:anywhere}
--- a/src/state.ts
+++ b/src/state.ts
@@ -13,7 +13,7 @@
 export const blankPhases:FlightPhases={enabled:false,departureAltitude:'',cruiseAltitude:'',arrivalAltitude:'',climbBasis:'rate',descentBasis:'rate',climbRate:'',descentRate:'',climbTime:'',descentTime:'',climbTas:'',descentTas:'',climbFlow:'',descentFlow:''};
 export type AppState = {version:1;flightPhases?:FlightPhases;rhumbPoints?:RhumbRoutePoint[];settings:Settings;favorites:string[];drafts:Record<string,Draft>;history:SavedCalculation[];legs:Leg[];departure:string;tripFlow:string;stations:Station[];reduction:string;profile:Profile;calcHistory:string[];memory:number|null;sexMemory:number|null};
 export const blankProfile:Profile={name:'My aircraft',emptyWeight:'',emptyArm:'',fuelArm:'',fuelType:0,flow:'',reduction:'1',pilotArm:'',passengerArm:'',cargoArm:'',customArm:''};
-export const initialState:AppState={version:1,settings:{theme:'dark',unitSystem:'us',localOffset:-4,destinationOffset:0},favorites:['wind-component','altitude','fuel','wind-correction'],drafts:{},history:[],legs:[],departure:'',tripFlow:'',stations:[],reduction:'1',profile:blankProfile,calcHistory:[],memory:null,sexMemory:null};
+export const initialState:AppState={version:1,settings:{theme:'system',unitSystem:'us',localOffset:-4,destinationOffset:0},favorites:['wind-component','altitude','fuel','wind-correction'],drafts:{},history:[],legs:[],departure:'',tripFlow:'',stations:[],reduction:'1',profile:blankProfile,calcHistory:[],memory:null,sexMemory:null};
 export const newLeg=():Leg=>({id:crypto.randomUUID(),name:'',distance:'',course:'',tas:'',windDirection:'',windSpeed:'',variation:'0',deviation:'0'});
 export const newStation=(name=''):Station=>({id:crypto.randomUUID(),name,weight:'',arm:'',moment:'',solve:'moment'});
 const record = (v:unknown):v is Record<string,unknown> => !!v && typeof v==='object' && !Array.isArray(v);
--- a/tests/session.test.ts
+++ b/tests/session.test.ts
@@ -1,13 +1,14 @@
 import test from 'node:test';
 import assert from 'node:assert/strict';
 import {initialState,freshSession} from '../src/state';
+import {resolveAppearancePreference} from '../src/features/appearance';
 test('fresh session clears unsaved work but retains explicit saves and preferences',()=>{
  const s=structuredClone(initialState);s.settings.theme='dark';s.memory=42;s.sexMemory=3;s.departure='12:00';s.tripFlow='9';s.profile.name='Temporary';s.calcHistory=['2+2 = 4'];s.drafts.fuel={mode:'volume',fields:{flow:{text:'9',unit:'usgph'}}};s.history=[{id:'saved',tool:'fuel',mode:'volume',fields:{flow:{text:'9',unit:'usgph'}},values:{volume:13.5},date:'2026-09-14'}];
  const next=freshSession(s);assert.deepEqual(next.drafts,{});assert.equal(next.memory,null);assert.equal(next.sexMemory,null);assert.equal(next.departure,'');assert.equal(next.tripFlow,'');assert.deepEqual(next.profile,initialState.profile);assert.deepEqual(next.calcHistory,[]);assert.deepEqual(next.history,s.history);assert.equal(next.settings.theme,'dark');assert.notEqual(next.history,s.history);assert.equal(s.memory,42);
 });
 
-test('fresh-install night theme does not override saved Day, Night, or Auto preferences',()=>{
- assert.equal(initialState.settings.theme,'dark');
+test('fresh installs follow System and preserve saved Day, Night, or System preferences',()=>{
+ assert.equal(initialState.settings.theme,'system');
  for(const theme of ['light','dark','system'] as const){
   const saved=structuredClone(initialState);saved.settings.theme=theme;
   const reopened=freshSession(JSON.parse(JSON.stringify(saved)));
@@ -15,3 +16,16 @@
   assert.equal(reopened.version,1);
  }
 });
+
+test('shared appearance is authoritative, including System',()=>{
+ assert.equal(resolveAppearancePreference('system','dark','light','dark'),'system');
+ assert.equal(resolveAppearancePreference('light','day','dark','system'),'light');
+});
+
+test('canonical appearance maps Day and invalid values before standalone fallbacks',()=>{
+ assert.equal(resolveAppearancePreference(undefined,'day','dark','dark'),'light');
+ assert.equal(resolveAppearancePreference(undefined,'invalid','dark','light'),'system');
+ assert.equal(resolveAppearancePreference(undefined,null,'dark','light'),'dark');
+ assert.equal(resolveAppearancePreference(undefined,null,'invalid','light'),'light');
+ assert.equal(resolveAppearancePreference(undefined,null,null,'system'),'system');
+});
