# Actual-source visual and interaction observations

Preview: http://127.0.0.1:8898/ in the Codex in-app browser. No production publication. These observations are bounded; they do not approve every route or every viewport. The structural route audit records all523 routes separately.

| Surface | Observed mode / viewport | Evidence and outcome |
|---|---|---|
| Homepage hero | Day and Night,390x844 | Sky retained; gold discovery action; secondary path action corrected from pale-on-pale Night text to gray with readable text/icon. |
| Blog directory | Day/Night desktop; Night390x844 | Filters, card wrapping and reading surfaces inspected. Selected Written Test filter is metallic gold and returns2 matching articles. |
| Blog article | Night390x844 | Heading, byline and reading surface inspected; category marker retained. Not a factual-content audit. |
| Tools directory | Night390x844 | Gold current category and Open PilotSolve action; original app icons and orbit controls retained. |
| Learn library | Day834x1112 | Search fields, training/topic list and gold selected Topics state inspected; document width834 matches viewport834. |
| PilotSolve | Night390x844 | Main tools, crosswind inputs, numeric keypad, results, saved-result list and unit dialog inspected.10kt from90deg on heading0deg produces10kt crosswind/0kt headwind; save/reload/restore preserves the result. Done key corrected to gold. Unit-sheet inherited38px type/-2px tracking fixed and rebuilt; screenshot confirms normal readable labels. Diagram blue remains semantic. |
| Certificate Generator | Night390x844 | Selection, names, photo optionality, review and original canvas artwork inspected using Preview Student/CFI Sample Instructor. CFII selected control gold verified. Export action creates a rendered1080x1920 image and success message. Download event timed out; native file arrival not claimed. JS and templates have no diff against baseline. |
| FOI Cards | Day834x1112; Night appearance menu | Entry, active question/answer, rating actions and CTA inspected. Added viewport dock clearance. Review/Memorized retain semantic colors. Primary CTA now gold. Final phone study pass remains pending. |
| Shared menu | Day and Night834x1112 | Gold selected section, readable dark label, appearance controls, gold solid-controls switch. Solid menu background computedrgb(74,74,74), backdrop-filter none. |
| Cross-tab appearance | Two actual-site tabs | Night choice changes second tab to data-theme=dark, resolveddark, meta#414141. System restores second tab to preferenceSystem, resolvedDay, meta#E3E3E3 under current OS. Solid controls restoredoff. No live OS-toggle claim. |
| Crank & Core | Prior continuation, desktop Day/Night | Collection, live Rotax model, View panel and appearance menu inspected; gray dock corrected; pinned simulator runtime unchanged. |
| Simply Endorsed First Solo | Day390x844 | Deep task detail showed gold selected navigation/Search, gray secondary controls and retained orange category header. Rendered solid-control text scan found no pair below 4.5:1; checklist outcomes were not changed. |
| Aero Lab Geometry Studio | Day/Night desktop and 1024x768; Night390x844 handoff | Modal selected mode/object and Save use gold with dark ink; secondary editor/Open/Export controls stay gray. At 1024px the internally scrolled footer was reachable, and selecting flap updated the selected row and Name field. Document width matched viewport. Phone kept the existing desktop handoff. No geometry edit or export delivery was attempted. |
| FOI Cards isolated phone pass | Day/Night390x844 | Flipped answer and Review/Next/Memorized controls retain gold/gray/status hierarchy. Marking Review advanced to card 2 with one review later. Night reset dialog focused Keep progress and showed gold confirmation. Confirming reset returned to 238 remaining/0 review after reload on a separate 8899 origin; the main preview's progress was untouched. |
| CertPath synthetic populated result | Day/Night desktop; Night390; Day/Night320 | Separate 8899 origin, normal goal/background/zero-hour inputs, and test-tab-only save-hook disconnection rendered the existing calculation result without submitting contact data. Solid result cards, gold regulatory accents/selected dock, gray edit controls and expanded training detail remained legible. At320px document width matched viewport and footer cleared the dock at the true scroll bottom. Normal contact save and native zoom remain unverified. |

## Remaining acceptance

Complete the18 source families across required viewport/appearance combinations; inspect remaining advanced Aero Lab/Crank panels and Simply Endorsed workflows, CertPath populated workflow after selected-control refinement, final FOI phone flow, actual keyboard/zoom/touch and OS appearance/accessibility settings. Check print/share outcomes and full text/control contrast. Do not equate source groups, test counts or these spot checks with full-site acceptance.

## CertPath populated-form continuation

At 834×1112, inspected Private goal selection in Day and Night, then entered a sample 20 hours of total/powered/airplane/ASEL and dual time. Empty fields correctly produced an alert and focused the missing input; group zero-fill completed the remaining fields. The Night inputs, focus ring, alert, category markers, and final save form were visually reviewed. Building the plan reached the existing contact-submission gate; no contact details were submitted and the service-backed results screen remains unverified.

Forced-color material aliases now use ButtonFace/ButtonText so more-specific selected-label adapters inherit the system palette. This is a source correction; OS high-contrast rendering still needs browser acceptance.

## Simply Endorsed and FOI phone continuation

- Simply Endorsed First Solo/A.3 detail: reviewed at desktop 1280×720 in Day, and phone 390×844 in Day and Night. Selected subcategory and mobile Tasks navigation now use shared metallic gold; category stripe/icon/badge remain category-colored. The model-text copy action reports success. No endorsement or external record was issued.
- FOI at 390×844 in Night: resumed Card 1, flipped to its answer, inspected Review/Next/Memorized controls and dock clearance, advanced with Next, then reloaded. Resume correctly reports Card 2 with no ratings changed. Day phone and long-card/section-overlay checks remain.
- Legacy mobile category surfaces now derive their gray bases from shared tokens rather than hard-coded green-black colors. Category accent variables are retained.

## FlightRisk mobile continuation

At 390×844, the shared high-specificity touch-size rule was found overriding FlightRisk's 168px certificate-card minimum with 44px, squeezing all five cards into the viewport. The shared minimum-size defaults now use zero specificity so application geometry wins. After reload, all five cards compute to 168px and the existing horizontal swipe layout is restored. Reviewed the corrected Day and Night profile screens.

The instrument-rating selected control was still gray because its app background utility overrode the selected material. The explicit FlightRisk shared adapter now applies the gold background and edge with the same precedence as its label correction. Night screenshot confirms gold VFR, Private Pilot, and Not rated selections. Guide introduction and first illustration reviewed on phone in Day and Night; illustration artwork remains unchanged. Full assessment completion, print, and lower guide/methodology sections remain pending.

## Aero Lab advanced-material continuation

At desktop 1280×720, found remaining green decorative surfaces in the airflow toolbar and Geometry Studio. Replaced app-owned CSS declarations with shared gray/gold tokens, rebuilt TypeScript/Vite, and packaged only Aero Lab through the new --app option. Geometry Studio reviewed in Day and Night: selected object is gold, surfaces gray, canvas geometry/flow colors unchanged. Selected flap object and Preview motion; pressed state confirmed. Night Pressure view and its selected teaching controls reviewed after rebuild.

All 14 Aero Lab tests pass, including studio model and flow/reference tests. Updated source patch includes geometry-studio.css. Six superseded generated bundles archived after dependency-closure and SHA verification; current entry is index-C1fiNUI2.js with index-Ct90X-y3.css. Remaining work includes reference cases, source/help dialogs, other result surfaces, export delivery, and accessible viewport/zoom acceptance.

## Aero Lab reference and source continuation

Reviewed reference charts at 1280×720 in Day/Night and changed Ladson series to 120 grit through the select control. Converted reference panel, technical disclosure, probe/result strip, and source-link surfaces to shared tokens. Night inspection exposed default-black SVG labels and low-contrast green source captions; corrected SVG fill, used lighter versions of existing green/orange chart series in Night, and removed the later competing caption override. Final Night source dialog screenshot confirms readable titles and captions on gray cards. Scientific data, canvas and model code unchanged.

Current package: index-LsKSwjRY.js and index-B90QPMlo.css. The refreshed source patch includes ReferenceCases.css. Archived 21 superseded/intermediate bundles with SHA verification; cumulative generated archive count is 230. Remaining accessibility/zoom/export and cross-template gates are not completed by this check.

## Public-template continuation

- Flight training: desktop 1280×720 Day hero and Student Pilot journey, then Night journey. Corrected selected journey stages to gold and bounded the CTA SVG to 18px (previous bare SVG stretched the button). Phone 390×844 Night Private Pilot selection and panel reviewed. Commercial label wraps mid-word at this width; retains current three-column geometry, further responsive polish pending.
- Ground-school service and learn-to-fly local landing templates: phone Night hero, primary/secondary actions and first reading section reviewed. No new color defects in these views.
- What Is Simply Endorsed article: phone Night heading/body, lower CTA, FAQ and footer reviewed; lower CTA/FAQ compared in Day. Shared gold action and solid gray reading surfaces confirmed. These observations do not certify unseen lower content or the other viewport combinations.

## Crank & Core advanced continuation

At 1280×720, opened Rotax study panel and Moving internals. Corrected selected-study material precedence; reopened panel confirms gold selection. Enabled switch track now uses gold. Opened ignition comparison, exposed internals (UI changed to Internals exposed), and returned to the engine. Night review found the lazy ignition dialog still used fixed light surfaces. Added explicit overlay adapters for its headings, reading cards, clock/navigation, comparison table, and captions. Final Night opening and Comparison section screenshots show gray surfaces, readable text and gold active controls. Model canvases, spark-state colors, meters and pinned runtime unchanged. Remaining engine mobile, keyboard, animation and model-specific flow coverage is still open.

## Learn and control-inventory continuation

At 1280×720, reviewed Private Pilot category hero/actions in Day, followed a card into the checkride-tips article, and used its Know Your Airplane anchor. Night reading view confirmed at the target heading. Existing blue table-of-contents/breadcrumb dots derive from category-accent variables and were preserved as category identity. This does not establish all Learn article content or viewport acceptance.

The structural audit now inventories button elements and button-class anchors per registered route: 54 static control families across 523 routes. It excludes unregistered PDF-build outputs and explicitly does not infer dynamic app control coverage. All route wiring checks still pass.

## Canonical contrast verification

Added scripts/audit-aurum-contrast.py and contrast-coverage.json, tied to the current stylesheet SHA. Checks cover normal/muted/accent text on four opaque surfaces, input borders on their raised surface, dark key labels over 101 sRGB samples per gold-gradient segment, and glass text over black/white backdrop bounds. The original Night glass opacity .89 allowed muted text to reach 4.27:1 over white. Increased shared Night glass to .94 and Day glass to .90; 34/34 checks pass, minimum normal-size text ratio 4.75:1. Visually reviewed the resulting Night appearance menu at desktop.

This verifies canonical palette pairs, not every rendered text node, alpha overlay, semantic color, canvas label, or application override. Full accessibility acceptance remains open.

## FOI phone dialog and keyboard continuation

At 390×844 in Day, reviewed Card 2 answer/rating controls and the section chooser. Found Shift+Tab from the dialog Close button escaped to the background Memorized action. Fixed the modal boundary: background controls become inert while open, Tab/Shift+Tab wrap within the chooser, and closing restores prior inert state and focus to Sections. Browser verification after reload confirms both wrap directions and Escape focus return. Resume remains Card 2, 238 unmemorized, zero review; no rating was changed. Added a passing DOM regression test. System appearance and default viewport restored. Long-card overflow, Night section chooser and 200% zoom remain separate acceptance work.

## FOI tablet materials continuation

At 834×1112, reviewed Night section chooser and Card 2 answer/rating controls plus the lower conversion CTA in both Night and Day. Removed obsolete blue/navy/cyan decorative literals and fallback aliases from FOI's own stylesheet, replacing them with canonical g tokens. Gold primary/CTA, neutral secondary surfaces, amber Review and green Memorized/answer meanings remain. No ratings changed. Browser zoom keyboard shortcuts did not alter innerWidth/innerHeight/devicePixelRatio, so this is not a 200% zoom verification. Restored System and default viewport.

## CertPath source consolidation and validation continuation

Replaced remaining prominent cream/olive decorative declarations in part61-redesign.css and storage.css with shared neutral/gold tokens; preserved layout and operational/category rules. Primary and selected rules now specify the canonical metallic gradient and dark labels in their own stylesheet. Added content-bound URLs for both CSS sources to sync-avionics.

At 1280×720, reviewed Night goal controls and progressed through background to blank experience validation. Generic shared rules had suppressed error text and invalid borders; explicit semantic adapters now restore them. Final rendered invalid input uses border rgb(255,195,192) on rgb(84,84,84); screenshot confirms text/borders and focused field. At 390×844 in Day, reviewed alert, error messages, inputs, and dock. System/default viewport restored; no contact submission or backend write.

Expanded contrast audit includes shared error text on all four opaque surfaces. Night red changed to #FFC3C0 and Day red to #A82724 after detecting inadequate contrast on raised Night and recessed Day surfaces. 42/42 canonical pairs now pass, minimum 4.75:1. Other semantic colors and complete rendered accessibility remain outside this audit. CertPath storage regression and 18 appearance/navigation tests pass. Saved-plan results, exports and full viewport acceptance remain open.

## Shared tool core and tablet follow-up

Migrated tool-core neutral surfaces and icon-button normal/hover/pressed backgrounds from legacy white/navy literals to canonical gray tokens. Kept category/identity and print colors. Removed self-referencing shadow aliases so shared shadow definitions resolve, and added content-bound tool-core CSS URLs.

At 834×1112, Certificate Generator CFI selection exposed a red title and pale subtitle over gold. Corrected both selected labels to --g-key-ink in app-owned and shared styles; reloaded and confirmed both compute rgb(43,35,22). Reviewed tablet Day and Night selected tiles; category dots and generated certificate artwork remain unchanged.

Simply Endorsed DPE question detail reviewed at tablet size in Day/Night. Reviewed toggle now uses gold when aria-pressed=true; reload correctly clears this transient state. Lower About/reference disclosure icons used near-black inline colors in Night. Migrated those inline styles to shared tokens; opened About and verified icon rgb(244,244,244) with readable expanded content. Reference links now use shared accent rgb(236,211,155). System/default viewport restored. This is presentation verification, not a new aviation-content audit.

Workspace regression passes 1,678 assertions; 10 shared navigation tests pass. Route wiring 523/523; canonical contrast 42/42. Remaining distinct-template/viewport, zoom, result/export and live OS gates remain open.

## Article discovery and mobile footer continuation

Migrated site-discovery.css neutral card and source-guide materials to canonical g tokens; app identity accent remains available. Added content-bound URLs across 518 referring HTML files. Related-tool links now use the shared gold text accent with underlines.

At 390×844 on the checkride-tips Learn article, visually reviewed the related-tool panel and adjacent training CTA in Night and Day. Tab from Plan training requirements moves to All pilot tools with a visible focus ring in both appearances. Reviewed Day Explore footer expansion and Night Flight instruction expansion, including bottom social links above the dock. Restored collapsed disclosures, System appearance and default viewport. Footer/catalog regression: 14/14 pass; route wiring 523/523. These observations do not substitute for the remaining full template/viewport matrix.

## FlightRisk completed-assessment and results continuation

Completed a synthetic local VFR/Private profile with experience values 250 total, 100 type, 100 PIC, 10 last-30-days, 30 last-90-days and 10 night. Selected only Night or twilight flight; reviewed remaining factors with section completion. UI moved from Incomplete to LOW, score 3, and results identified one selected hazard. Reload after packaging retained this state. This is UI/regression evidence for synthetic data, not operational flight advice.

Desktop results were compressed to 640×360 because the mobile bottom/right offsets remained active alongside desktop centering. Corrected desktop constraints to right/bottom auto and width calc(100%-3rem), retaining the existing 920px/88vh maximums. Confirmed final bounds 920×633.59 at x180/y43.20 on 1280×720. Raised backdrop/dialog to z160/161 above the shared z100 dock; phone result action row now remains unobstructed.

Night gauge labels and pointer used fixed dark colors. Updated only neutral pointer/label fills/strokes to shared ink/muted/surface tokens; risk arcs, ticks and score math unchanged. At 390×844, final labels compute rgb(208,208,208), pointer rgb(244,244,244), and modal bottom is844. Keyboard Shift+Tab/Tab wraps between Close and New assessment. Escape returns to Results for a phone-opened dialog. Resizing from desktop to phone while open leaves focus on body after dismissal because the original desktop trigger becomes hidden; this responsive focus edge case remains open. Native print delivery still unverified.

Current package index-eVnrbulL.js / index-BNK65EUo.css. Also migrated keyboard skip-link metal to shared tokens. TypeScript/Vite build and 26 regression tests pass; matched source patch refreshed (CSS, Tailwind config, Layout, ResultsOverlay, Gauge). Packaged with base /flight-risk-assessment/. Archived29 replaced/intermediate bundles with dependency closure and SHA checks, 259 cumulative. System/default viewport restored; synthetic state remains local in this preview.

## FlightRisk responsive focus continuation

Resolved the previously open result-dialog resize edge case. Marked equivalent result triggers and return focus to the first visible, enabled trigger when the original is hidden/disconnected. Added a regression that changes the layout while the dialog is open. All27 app tests and TypeScript/Vite build pass.

Actual browser: opened via keyboard on desktop, resized to390×844, dismissed with Escape; focus reached the visible44px Open assessment results button. Reopened on phone, restored desktop size, dismissed; focus reached the44px View Results button. This closes the observed responsive focus defect. Exercised Print summary; the embedded browser exposed no print-preview or delivered-file evidence, so native print acceptance remains open.

Current package index-Dj7sEDnv.js / index-BNK65EUo.css; source patch refreshed. Archived9 replaced JS bundles with dependency/SHA checks (268 cumulative). Default viewport retained and appearance remained System. Synthetic assessment remains in local preview storage.

## Service FAQ and intake follow-up

Ground-school service route reviewed at 1280x720 and 390x844. Found a literal translucent white FAQ fill that reduced Night reading contrast; replaced it with the canonical raised solid surface and shared border in learn/style.css. The stylesheet now receives content-bound URLs from sync-avionics. Opened the other-school and cost disclosures and reviewed expanded content in phone Day/Night; desktop Night FAQ also reviewed.

Empty intake submission stays on-page and shows its validation alert. Fixed shared cascade precedence that hid error text and invalid-input borders. After reload, Night computed error text/borders are #ffc3c0 on #4a4a4a alert surface; Day #a82724. Phone Day/Night screenshots show readable alert and metallic primary/gray secondary actions; document width equals390px. No contact details entered, email client opened, or message sent.

Contact regression suite4/4; route integration523/523; canonical contrast42/42. This is bounded service-template evidence, not full-page/all-viewport or real message-delivery acceptance.

## Learn source-token and tablet navigation follow-up

Consolidated remaining decorative blue grid/shadows, flat-yellow link underlines, TOC white fill, service-link underlines, and footer border in learn/style.css into shared material tokens or neutral shadows. Meaningful category palettes retained. Contact email-handoff notice now uses a solid raised surface and canonical accent border; handoff behavior unchanged and not activated in the browser.

Reviewed /learn/aircraft-ownership/understanding-aircraft-insurance-costs/ at834x1112 Day/Night (header, TOC, reading surface, selected Learn dock). Tab focus on Liability Coverage visibly uses the shared gold ring. Enter navigates to #liability-coverage with heading landing23.8px from viewport top after smooth scrolling settles. Day phone390x844 header/TOC reviewed; all four navigation links44px tall, document width390px. This verifies materials/navigation, not the article's substantive claims.

Sync updated479 HTML sources. Route audit523/523; no calculation/content/navigation logic changed. Broader full-template matrix remains incomplete.

## Certificate Generator phone workflow continuation

At390x844, selected First Solo and continued to Names. Empty Continue set aria-invalid on Student first name but generic shared input chrome hid the semantic border. Added a shared explicit aria-invalid rule with sufficient specificity and updated generator source error borders to canonical av-error. After reload, Day invalid border computed#a82724 with gold focus ring; Night invalid border visibly present.

Entered synthetic Sample Student with existing local sample instructor, skipped optional photo, reached Review. Phone Night summary/preview/download controls reviewed. Opened caption editor and reviewed Night and Day: gold Done/Download, gray Reset/Share, readable textarea and focus ring. Returned Done without changing caption. Entered values and summary survived appearance changes; corrected field no longer aria-invalid. Document width390px. Template artwork and generator JS show no Git diff. No sharing or publication performed; this turn does not establish download-to-disk/export delivery.

Route integration523/523, canonical contrast42/42. Remaining full-tool/viewport/OS/zoom/export gates remain open.

## Certificate Generator source and cropper continuation

Consolidated generator app-owned neutral text, surfaces, borders, primary/rating gradients, step markers, mobile action surface, cropper, and footer into shared g tokens. Removed prior decorative navy/teal literals; retained white crop grid over photographs, neutral shadows, and semantic error halo. No template/artwork/canvas logic edits.

Loaded the existing public og-certificate-generator.png as a local test image through Upload photo. Day cropper reviewed at1280x720 and390x844; Night cropper at390x844. Surface follows requested Day/Night base, primary Use photo is gold, cancel/zoom/reset controls gray, image content unchanged. Zoom in changes slider0 to30 and image transform; Reset returns0. Tab from Use photo wraps to Cancel and close. Use photo returns to Photo ready and Adjust crop reopens the accepted image. Phone controls and crop region fit viewport above the bottom edge without dock overlap. No publication or external sharing.

523/523 route wiring passes. This closes bounded cropper visual/control checks, not native touch pinch, full export delivery, or complete application acceptance.

## Tools directory source and interaction continuation

Removed old navy/light-blue appearance literals from tool-catalog.css in favor of canonical g tokens; gold launch and filter controls now declared at source. Changed selected orbit-app ring from per-app brand color to shared gold, preserving icons/identity colors and subtle app-stage tint.

Desktop Day Training and instruction filter/Next reached Simply Endorsed; reload retained category/selection. Phone Day selected ring and launcher reviewed; phone Night Aircraft systems filter selected Aero Lab with gold filter/launcher/ring. Expanded All apps shows all seven direct links. At834x1112 Night, ArrowRight from Aero Lab selects Crank & Core with gold focus ring and final selected-icon ring computed#ecd39b; document width834px. Two-column expanded app list fits. Restored System/default viewport and collapsed list.

Eight catalog tests pass, including filtering, direct links, session state, blocked storage, swipe, enlarged text, and reduced-motion logic. These are not live OS/reduced-motion or native200%zoom proof. Route integration523/523, shared references fresh.

## Simply Endorsed selected-hover continuation

At1280x720 Night, clicking the current Tasks navigation link left its pointer-hover state with dark gold-button ink#2b2316 on gray#545454. Restricted the neutral navigation hover fill to non-current links. After reload, browser computed :hover=true while the selected Tasks background remains the canonical metallic gradient with dark key ink.

Selected endorsement row edges now use shared g-accent instead of per-category bright color; category badge colors stay intact. Opened Library, all96 endorsements, A.3 detail; Night selected border/inset marker computed#ecd39b. Day detail and selection also reviewed. Content/data logic unchanged. Restored System appearance.

## Shared base-material continuation

Consolidated design-system.css legacy glass/paper/border aliases into canonical g tokens with opaque Day fallbacks; neutralized blue shadows/grid/ambient washes and gold glow. Homepage sky artwork unaffected. Shared social hover now uses raised surface. Unified text-selection declarations in base/premium/avionics to solid g-mid background and g-key-ink text.

Homepage1280x720 Day/Night hero visually reviewed after base changes: preserved sky, gold primary action/selected Home dock, gray secondary action with readable icon/label. Browser pseudo-style inspection after final reload confirms text selection #e3b741/#2b2316 in both modes. Contrast script now explicitly checks text-selection pairs,44/44 pass; footer suite6/6. Route wiring523/523; no full-site acceptance claim.

## Solid-surface and PilotSolve cache continuation

Solid controls already made the shared dock/menu opaque, but g-glass itself remained translucent. Quiet mode now sets g-glass to g-surface and g-blur to0, with backdrop-filter disabled on elements/pseudo-elements. Reduced-transparency, higher-contrast and unsupported-backdrop CSS paths receive opaque material-token fallbacks. Removed an unused liquid-menu::before material rule (computed content:none).

Homepage solid mode persisted reload; Day token#efefef, Night token#4a4a4a, dock/menu backdrop-filter:none. PilotSolve first visits/reload served older cached HTML/theme(version d4bcdf4284). Its worker now requests documents/shared appearance assets network-first with offline fallbacks, retains immutable bundles cache-first, prefers exact versioned matches offline, and caches newly fetched bundles. Cache revision includes normalized worker policy to invalidate on future policy edits. Failed cache writes do not hide a usable network response.

After update, browser reload showed current CSS19a3eccd8c and opaque#4a4a4a. PilotSolve settings reflected Solid controls enabled and Night preference; restored System and toggled Solid controls off, confirming normal rgba(241,241,241,.90) material. Four new worker tests pass (online stale refresh, offline exact-version/shell fallbacks, immutable bundles, scope isolation); appearance8/8 and canonical contrast44/44. Browser-offline emulation and OS-level transparency changes remain unverified.

An earlier PilotSolve settings build said Dark. The current preserved Aurum source, patch and packaged build say Night; current-browser verification is recorded below.

## PilotSolve Night-label rebuild

Updated app-owned Settings label Dark to Night without changing stored theme values. Protected runtime28/28 and app regression36/36 pass; TypeScript/Vite build succeeded. Offline preparation initially failed because its hardcoded sibling site path did not exist in the isolated source workspace. Added SUAREZ_SITE_ROOT override and reuse of the canonical host worker policy, then successfully prepared/package-integrated the build. Source patch regenerated including build script.

Current JS mobile-ByfunKI1.js, prototype-CIkk57bl.js, preview-DEAI0vXg.js; CSS unchanged. Three superseded JS bundles archived with SHA verification (271 cumulative). Browser settings immediately show Night/Day/System; selecting Night gives dark root and gold selected control, reload retains Night. Restored System. Cache tests4/4; route wiring523/523. This resolves the prior wording item; broader acceptance remains open.

## Endorsement guide material correction

Reviewed `/simply-endorsed/blog/cfi-endorsements/` in the local preview. The Night card background was a fixed pale cream alpha fill with light text. Replaced guide card, ID, note, and action styling in `simply-endorsed/css/seo.css` with shared material tokens. Added this stylesheet to content-version synchronization and normalized relative stylesheet URLs across 18 guide documents.

Browser observations: at 1280×720 Night, core cluster cards, lower reference note, and primary/secondary actions are legible on solid gray surfaces; the primary action is metallic gold. At 390×844 Day and Night, core cards wrap within the viewport; Day document width measured 390px. Restored System appearance and reset viewport. This is a bounded template observation, not substantive endorsement-content review or full route acceptance.

## FOI desktop rating contrast correction

At 1280×720, reviewed the Day welcome, resumed Card 2 question/answer, and Day/Night ratings and lower CTA. The floating dock allows controls to scroll clear; no layout changes were made. Night semantic-label contrast was 4.30:1 for Review and 3.60:1 for Memorized. Changed only these labels to shared `--g-ink`, preserving amber/green borders and fills. Reloaded and inspected both appearances; all four resulting computed rating pairs pass 4.5:1 (see `foi-rating-contrast.json`). The gold Flip and CTA treatments remain intact. Reload retained Card 2, 238 unmemorized, and zero review-later cards; no ratings/reset actions used. This is not full long-card/zoom acceptance.

## Blog article material consolidation

Reviewed `/blog/private-pilot-cost-louisville-ky/` locally. Found legacy teal header/hover fills in the article table. Replaced those with `--g-raised`; replaced decorative blue grid/shadow, fixed dark metadata, old yellow link underline, and fixed footer-line declarations with shared neutral/accent tokens. Meaningful category palettes remain. Added `blog/style.css` to content-version synchronization and normalized relative blog stylesheet references; 42 HTML sources updated.

Browser evidence: desktop 1280×720 Day intro/table and Night table; tablet 834×1112 table in both appearances (Night header computed rgb(84,84,84), document width 834). Tablet Day FAQ expanded successfully. Phone 390×844 Day reading and FAQ collapse/wrapping inspected; document width 390. Restored System and normal viewport. Only visual/interaction review performed, not cost or aviation claim validation. Route audit remains 523/523; full article variants and remaining acceptance gates remain open.

## Learn library and category responsive review

Current local `/learn/`: desktop 1280×720 Day search `weather` returned 66 results. Selecting Weather and Safety reduced this to 42; switching to Night retained both query and topic. Reviewed solid result cards, gray Clear filters, and readable input/metadata. Tablet 834×1112 Night results wrap into the intended two-column filter layout; unmatched test query produced 0 results and a readable explanatory empty state. Clear filters restored the 459-entry library. At phone 390×844, Topics switched to the gold pressed state and displayed collection links in Day and Night; Day document width measured 390.

Followed the visible FAA Written Test Study Guides collection link. Reviewed its hero/actions on phone in both appearances, tablet hero and two-column cards in both appearances, and desktop Night hero/actions. Primary prep action remains metallic gold, secondary ground instruction remains gray, and category identity artwork stays unchanged. No content or logic edits needed in this review. Restored System and normal viewport. Remaining checks are explicitly retained in the acceptance matrix; these observations do not establish complete route or accessibility acceptance.

## FlightRisk methodology matrix correction

Reviewed local `/flight-risk-assessment/methodology/`: desktop Day introduction, table jump to IFR, and Day/Night scoring tables; phone Night disclosure for Night or twilight flight (`aria-expanded=true`, document width 390), plus phone risk-band cards in both appearances; tablet 834×1112 full band matrix in both appearances. Found `odd:bg-white` on desktop/tablet matrix rows: Night profile text was effectively invisible on white. Replaced only row material classes with `odd:bg-surface-card even:bg-surface-soft` in source `src/components/methodology/BandingMatrix.tsx`. No scoring/data changes.

TypeScript and Vite build passed. Packaged `index-BY8Vt2up.js` / `index-Bp18znhh.css`; refreshed route/cache wiring (523/523). Source patch now includes BandingMatrix. Archived 10 superseded generated assets with the existing hash-checked archive tool to `flightrisk-matrix-followup`, outside deployment. After reload, Night matrix row backgrounds computed rgb(74,74,74) with rgb(244,244,244) profile text; settled red/amber/green band fills remain visible. Day rebuild also visually checked. Full guide/reference/export/zoom acceptance remains open; this is UI verification, not scoring or aviation-content certification.

## FlightRisk guide timeline material correction

Source audit of guide/methodology fixed-color states located hard-coded white/teal timeline marker states in `src/components/guide/StepTimeline.tsx`. Browser Night review confirmed the teal active marker. Replaced the marker constants with shared gray idle and metallic gold active tokens, using CSS variable background values so live appearance changes resolve without reinitializing the timeline. Preserved all instructional SVG artwork, geometry, scroll behavior, and content. The expanded INCOMPLETE FAQ was also visually readable in desktop Night; its existing shared override already handles its white source class.

TypeScript and Vite build passed. Packaged `index-C3Bpn_gU.js` / unchanged `index-Bp18znhh.css`; updated source patch, refreshed wiring, and route audit passed 523/523. Archived nine superseded JS bundles in `flightrisk-timeline-followup` outside deployable files. After reload, inspected desktop Night Step 02 gold marker and live switch to Day: marker remains gold with dark readable text. Restored System. Remaining guide checks remain in the acceptance matrix.

## Training journey label wrapping correction

At 390×844 Day, the selected Commercial tab split its word into `Commerci` / `al`. Adjusted the journey's existing responsive rule: gap 10→6px, prevent dot shrinking, wrap labels at word boundaries, and use two columns below 381px. This preserves the six stages, content, navigation, and selection behavior while keeping narrow labels legible. The normal three-column tablet/phone layout and desktop route remain unchanged above that threshold.

Reload verification: 390px Day Commercial fits on one line. At 320×740 Night, six tabs fit in two columns, document width equals 320, and ArrowRight Commercial→CFI then ArrowLeft CFI→Commercial updated selected state/panel with visible gold focus. At 834×1112, Day journey controls/panel and Night panel/planning details were reviewed. Shared materials remain intact. Sync/cache refresh and 523/523 route audit passed; no data or form submissions. Remaining lower-page/enlarged-text checks retained.

## Aero Lab flow-status contrast correction

Desktop browser review found the Day `Below critical angle` label using pale rgb(167,199,183) text over an almost transparent green fill. Updated `.flow-status` source rules to solid shared surface/ink, with green default and amber warning borders/dots. Preserved scientific flow colors, instrument arcs, simulation logic, geometry, and labels.

TypeScript/Vite web build passed. Packaged `index-BRFyvVe9.js` / `index-BrlkFKEs.css`, updated source patch and cache/wiring; 523/523 route audit passed. Five superseded generated assets archived to `aero-status-followup` outside deployment. Reloaded browser Day badge computes text rgb(39,39,39) on rgb(239,239,239); warning at 30-degree angle uses amber border. Night warning computes text rgb(244,244,244) on rgb(74,74,74), with amber rgb(255,202,114) border. Both are canonical contrast-tested opaque text pairs. The initial exact-input/slider reads preceded simulation updates; final DOM confirmed 30 degrees and Beyond critical angle. Reset lesson restored Below critical angle and System appearance. This bounded check does not close responsive/help/export gates.

## Aero Lab responsive handoff and help

Reviewed supported narrow-device handoff at 390×844 and 834×1112 in Day/Night. Its primary Copy link action was still gray. Added shared `gold-primary` classification and gold source tokens in DesktopGate.tsx/css. Geometry, device requirements, URLs, and clipboard logic unchanged. Clicked the rebuilt phone action; UI displayed Link copied and the live status confirmed the Aero Lab URL was copied. Both appearances show gold action and readable gray surfaces.

Desktop 1280×720 How to explore overlay visually reviewed in both appearances; Escape dismissed the Day dialog and the Night close action worked. Restored System and default viewport. TypeScript/Vite passed; package now `index-C980s7W8.js` / `index-B2Pyi143.css`. Updated source patch, synced assets/cache, route audit 523/523. Five superseded assets archived outside deployment in `aero-handoff-followup`. Export and remaining desktop-state acceptance still open.

## Crank & Core phone/tablet study controls

Reviewed Rotax collection and Study at 390×844 Day, then Moving internals in Night. Appearance switching retained the selected study view. Reviewed Lycoming collection/loading and Whole engine Study at 834×1112 Night and Day. Gold selected controls, gray secondary controls and the existing model artwork remained visible.

Tablet review exposed the pinned runtime's cream enabled Propeller switch surface. Added a scoped checked-switch rule in `engine-explorer/app/assets/avionics-overlay.css` using shared raised gray/ink and gold edge; the existing gold rocker track remains. Reloaded and visually verified both appearances. Toggling Propeller off removed its checked state; toggling back restored it. No model/runtime logic changes. System appearance and default viewport restored. Sync/cache refresh and route wiring audit passed 523/523. Remaining model-specific controls, enlarged text and cross-cutting acceptance remain open.

## PilotSolve tablet switch correction

At 834×1112 Day reviewed Fuel numeric keypad, disabled actions before input, and a synthetic 10 US gal/hr × 2:00:00 example returning 20 US gal. Settings Night review exposed the blue enabled Solid controls switch. Replaced switch track/thumb declarations in app-owned prototype.css with canonical gray/gold tokens; unchanged dimensions, semantics and checked behavior.

Runtime guard passed all 28 protected files; TypeScript/Vite production build passed. Packaged prototype-BB03Ca4u.css / prototype-BwLH_lGl.js / mobile-D-j-Bjkv.js; updated source patch, refreshed worker cache pilotsolve-site-7310c4bc6ec3, route audit 523/523. Four superseded bundles archived outside deployment in pilotsolve-switch-followup. Reload verified gold enabled switch in Night and Day; disabled it again and restored System/default viewport. Tablet settings and backup action surfaces reviewed, but no backup restore/export delivery performed. Unsaved synthetic calculation cleared by expected reload behavior. Remaining calculation helpers, trip/aircraft and native backup acceptance stay open.

## PilotSolve trip and aircraft surfaces

On the current switch-corrected build, desktop 1280×720 Day trip review enabled Plan climb & descent and added one blank leg. Reviewed phase fields, gold Average rate selection, gray Known time option and leg entry surfaces. Opened Aircraft profile and added a blank Custom 1 station; desktop Day solve dialog shows gold Moment selection with gray alternatives. At 390×844 Night the station dialog fits above the viewport edge with readable solid gray surface and gold selection. Escape dismissed it and restored focus to the station solve trigger. Reopening and selecting Weight changed the form to calculated Weight with Arm/Moment inputs and returned focus to the updated trigger.

No code changes were needed for these bounded surfaces. Restored System/default viewport and reloaded to discard unsaved review entries, retaining saved results/preferences. These observations do not certify populated trip/aircraft calculations, all responsive combinations, native backup delivery or enlarged-text acceptance.

## Homepage lower-section and contact review

Current local homepage at 834×1112 Day: inspected instruction rows/Book action, instructor card/disclosures, tool links with retained identities, and expanded Draft an email form. Gold primary actions, gray reading/entry surfaces and secondary actions are visible. Empty Open email draft attempt stayed on-page and produced the required-name/contact alert; no data entered and no email sent. Tablet Night form/error/footer reviewed after appearance switch.

At 390×844 Night and Day, reviewed focused empty Name field, required-field borders, wrapped error message, and expanded Where do lessons take place FAQ. Gold focus ring and primary action, semantic error colors and gray surfaces remain legible. FAQ appearance changes preserved expansion. Day document width equals viewport 390px. Restored System/default viewport and reloaded. No code correction was needed for these surfaces. Desktop lower sections, complete pathway-card states and enlarged-text verification remain open; no pricing or aviation-content accuracy claim is made.

## Supporting document coverage and factory-reference correction

Expanded structural audit beyond 523 SEO-registered routes to six supporting documents: Simply Endorsed and FRAT redirects, PilotSolve mobile entry, embedded engine app, engine factory-reference shell, and Aero Lab native-flow frame. All six pass current-byte appearance/material URLs and first-script checks. Source templates, HTML partials, test fixtures and PDF artwork previews remain outside this browser-entry count. This is wiring evidence only.

Found standalone factory-reference/reference.css still declaring cyan/navy appearance tokens. Replaced them with shared g tokens, gold selected camera buttons/animated-parts action, gray shells, and canonical focus/checkbox/spinner colors. Added its CSS to content-hash sync. Desktop Day and Night loading shell and Night return action visually verified; hosted external model remained loading, so enabled camera controls/model delivery are not accepted. Model/provider code and imagery unchanged. Restored System. Audit now passes 523 routes plus 6 supporting documents; diff check passes.

## Explicit supporting-page inventory and reference cleanup

Moved six supporting documents into config/theme-supporting-documents.json with explicit reasoned exclusions for source templates, partials, print artwork and the text-size test fixture. Audit now scans all repository HTML and fails on unclassified documents; it also verifies supporting stylesheets exist and shared authority is last in the head. A temporary self-created unregistered HTML probe caused the expected audit failure; removing the probe restored a passing report. Current report has zero unclassified HTML.

Confirmed original factory-reference/reference.css is present in styles-dfd653c.tar.gz. Removed remaining superseded literal palette declarations from the active reference stylesheet and replaced with shared tokens. Phone 390×844 Day controls/loading/disclosure reviewed, document width 390px. Tablet 834×1112 Day columns, expanded disclosure and gold animated-parts action reviewed. Hosted model stayed loading, so enabled model controls remain unverified. System remained selected; viewport restored. Route audit 523/523 plus supporting 6/6, sync check zero stale HTML, diff check passed.

## Local landing template review

Reviewed /learn-to-fly-louisville/ desktop 1280×720 Day hero and three service cards; keyboard Tab from Plan the full path moved to Start ground school with visible gold focus. Desktop Night FAQ expansion displayed readable gray panels. At 834×1112, contact panel and focus reviewed in Night, then Day; expanded FAQ persisted. At 390×844 Day, stacked contact fields/action fit and document width equals 390. No contact data entered or submitted.

The same style group contains /southern-indiana-flight-training/ and /flight-instructor-louisville/. Reviewed Southern Indiana phone hero/actions/badges in Day/Night, including longer heading wrapping. Reviewed instructor tablet Night hero, gold phone action, gray secondary action and services reading panel. Existing identity icons retained. No material correction needed for observed surfaces. Restored System/default viewport. These bounded observations do not claim every lower section/viewport, text zoom, aviation-content validation or complete template acceptance.

## Blog directory responsive filters and expansion

Reviewed /blog/ 834×1112 Day/Night featured card, filters and For Instructors result. Selection remained gold and retained one matching article across appearance changes. At 390×844 Night, All articles filters wrap without clipping; Show more reveals subsequent cards. At phone Day, Aircraft returned one article. Keyboard Tab from Aircraft to Airport & Airspace then Return selected it and announced three matching articles. Document width equals viewport 390px. Returning to All articles and activating Show more twice reached the final article and removed the exhausted Show more control. Gray cards, gold selected controls/links and focus border reviewed.

No free-text search exists on this directory, and available categories contain results; no empty-results test claimed. No code correction needed. Restored System/default viewport. These observations close the previously missing tablet and phone Day baseline coverage, but enlarged text and the separate endorsement directory remain to verify. Content/aviation claims were not audited.

## Endorsement introduction and linked FAQ correction

Reviewed What Is Simply Endorsed CFI desktop Day reading/cards and Night cards, plus tablet 834×1112 Night CTA/expanded FAQ. Found automatically linked CFR text became separate flex items in FAQ summaries, spacing a single question into disconnected fragments. Updated shared blog summary layout to normal inline text flow with an absolutely positioned disclosure chevron and reserved end padding. Content, destinations and details behavior unchanged.

Reload verified contiguous linked question at tablet Night. Phone 390×844 Day screenshot verifies natural two-line question wrapping, chevrons, solid reading surfaces and gold app action. Disclosures still toggle. Restored System/default viewport. Refreshed 42 blog-family CSS references; wiring audit passes 523 routes plus 6 supporting documents. Broader guide variants, tablet Day and enlarged-text acceptance remain open.

## Simply Endorsed search and category dialogs

Current workspace desktop Day unmatched query zzzzthemeprobe returned 0 matches with a readable gray recovery panel. Phone 390×844 Night retained the query across appearance change; Clear search recovered, and IPC returned seven matches (one endorsement, one checklist, five guidance results). Phone Night category dialog retained semantic category icons and gray surfaces; expanded Recurrent & aircraft displayed its subcategories. Escape closed the dialog and DOM focus returned to Browse categories.

Tablet 834×1112 Day populated search and expanded category dialog visually reviewed. No material correction needed for these observations. Cleared test search and restored System/default viewport. No checklist progress or user data changed. This does not close full guided-workflow, zoom or all viewport acceptance.

## Tools directory remaining baseline views

Current /tools/ desktop 1280×720 Night: Previous app changed Aircraft systems selection from Crank & Core to Aero Lab, showing gold launch action and selected app rim. Expanded All apps displays all seven app identities and quiet launch links. Tablet 834×1112 Day Flight planning shows gold selected filter, PilotSolve launch action and gray directional controls; expanded list uses two columns. Keyboard Enter on Next app changed the announced stage to 2 of 2 FlightRisk. Phone 390×844 Day stage/list and visible focus ring reviewed, document width equals 390.

No correction needed for these observed surfaces. Restored original Aircraft systems/Crank & Core selection, System appearance and default viewport. Together with earlier observations, each baseline viewport/appearance now has bounded directory evidence; enlarged text, reduced effects and full pointer/touch interaction acceptance remain open.

## Certificate Generator desktop Night review/export interface

Selected First Solo, entered synthetic Sample Student, retained pre-existing Sample Instructor/CFII example, and reviewed desktop Night name fields, selected rating, photo-optional step, review/download controls and caption editor. Gold actions/selected steps and gray inputs/editor remain readable; certificate artwork retains its own original colors.

The first download observation failed because the review contains two Download JPEG buttons and the pending observation timed out during an ambiguous action. Retried once using the explicit preview button #cgDownloadBtn with an immediately handled observation. UI announced FirstSolo_Sample_Student.jpg and rendered the finished-save fallback image #cgSaveImg as a blob with natural dimensions 1080×1920. The browser download event still timed out, so native saved-file delivery remains unverified; do not count the UI announcement as file proof. Caption editor closed without edits. System restored; sample preview remains available for review. No source changes were needed for these surfaces.

## Learn phone search and native clear-control correction

Reviewed populated weather search with Weather and Safety selected at 390×844 in Day and Night. The 42-result state and selected topic persisted across appearance changes. Found WebKit's native blue search-clear icon; scoped its existing pseudo-element to the shared gold accent with a masked x, retaining native clearing behavior. Day/Night screenshots confirm the gold icon and focus ring. Clicking the icon cleared the query while retaining the topic.

Keyboard Tab moved from search to Topic, Training stage, then Clear filters; Return reset all three fields and restored the 459-item library status. Document width equals 390. Restored System and default viewport. Shared asset URLs refreshed across 545 HTML sources and standalone cache digests refreshed. Wiring audit passes 523 registered routes plus six supporting documents; all 44 canonical contrast pairs pass (minimum 4.75). Forced-colors fallback is implemented but not natively exercised. Full-page, zoom, OS-setting and export acceptance remain open.

## FlightRisk tablet assessment and guide warning correction

At confirmed 834×1112, reviewed restored synthetic VFR/Private assessment profile and LOW 3 results in Day/Night. Native Escape closed Night results and focus returned to the responsive Results button; document width 834. The initial resize before full navigation reset to desktop, so tablet evidence begins only after reapplied size and DOM confirmation. Synthetic assessment values were not changed.

Guide worked example reviewed in Day/Night, then found the OVERRIDE → HIGH badge retained translucent white behind light red text in Night. Replaced that app-owned background with the existing solid semantic high-risk tint. An intermediate gray-surface candidate measured only 4.17:1 and was rejected. Final browser-computed Day/Night pairs and contrast are in flightrisk-override-contrast.json; screenshots confirm warning semantics and readable label. All 27 FlightRisk tests and TypeScript/Vite build passed. Packaged final index-BG7_EtUz.js / Guide-C2BBwHsc.js with unchanged index-Bp18znhh.css, updated source patch, archived 18 superseded bundles with dependency/hash verification, and passed 523-route plus six-supporting-document audit.

System/default viewport restored. This is material and interaction evidence, not a validation of aviation guidance or native printing. Full guide lower CTA, other assessment bands/IFR and enlarged-text checks remain open.

## FlightRisk override family and guide lower actions

Searched remaining translucent-white utility use in app-owned FlightRisk components after the guide correction. Found the same warning-surface issue in FlightProfileCard's IFR badge and OverrideActions' methodology icon/badge. Replaced those three backgrounds with the existing semantic high-risk tint; kept colors, wording, calculations, layout and export styles. Other white-opacity matches are switch hardware, gold glints, or button count overlays and were not treated as the same warning defect.

Rebuilt and packaged index-DttITNAe.js, Assessment-DEPPpDDC.js and Methodology-DyNQGesM.js; source patch regenerated. All 27 tests plus TypeScript/Vite build pass; wiring audit 523+6 passes. Archived nine superseded bundles with dependency/hash verification. Tablet Night methodology and synthetic IFR/not-rated warning screenshots confirm the corrected label/icon; computed badge colors match the previously measured 6.442:1 pair. Phone Day warning wraps and fits, with semantic red retained. Returned synthetic profile to VFR and reconfirmed it; experience inputs unchanged.

Guide phone Day/Night lower CTA reviewed, gold primary and gray secondary visible; keyboard Tab from Open FlightRisk reached Review the methodology with a gold focus ring. The link navigated correctly, document width 390. Save/share FAQ expanded and displayed its local-storage/print answer. Restored System/default viewport. These checks do not close the full IFR workflow, native print, reduced-motion or zoom gates.

## Service template tablet coverage

Reviewed Discovery Flight at confirmed 834×1112: Day hero/details, contact anchor, empty form validation, then Night form/error/actions and expanded FAQs. No contact information entered and no email draft or call launched. Keyboard Tab from gift FAQ reached What happens after the flight; Return opened it, confirmed via native details open state. Document width equals 834.

Ground-school service variant reviewed at the same tablet size: Night hero and three training cards; Day cards, expanded remote-session FAQ and footer. Existing category identity icon retained, gold primary/gray secondary/quiet reading links remain consistent. No new visual defect found in these observed surfaces. Restored System/default viewport. This fills missing tablet baseline observations but does not establish complete page/variant, zoom, motion or message-delivery acceptance.

## Endorsement content missing baseline views

CFI endorsement cluster guide: reviewed desktop Day cards, tablet 834×1112 Day cards/note/CTA and Night note/CTA/footer. Gold primary and gray secondary actions remain distinct. Keyboard Tab from Open the Simply Endorsed CFI tool reached Read the CFI initial guide with visible gold focus. Primary launch reached the actual Simply Endorsed task workspace. Tablet document width equals 834.

Introduction article: reviewed tablet Day audience/coverage cards, reading and gold app CTA; native FAQ opened and its details state was confirmed. No visual correction required in these observed views. Restored System/default viewport. This adds missing baseline combinations, not complete article/guide-variant, enlarged-text or content-accuracy acceptance.

## PilotSolve phone Day and calculator appearance persistence

At 390×844, repeated keypad input 125 × 4 produced 500 with readable Day keypad, solid result area, gold equals/selected controls and disabled memory actions. Found calculator expression/answer were lost when visiting Settings to change appearance and reopening Calculator, because draft hooks belonged to the unmounted route. Lifted calculator expression, mode, error and answer into a parent-owned in-memory session hook. No AppState, backup format or calculation engine changes. Explicit Reset session and Reset app data also clear the draft.

Rebuilt preview reproduced 125*4 = 500, visited Settings, changed Night to Day, returned through Tools and reopened Calculator: expression and answer retained. Explicit Reset session then restored empty expression, no answer and disabled Store. Full existing PilotSolve test command, 28-file protected runtime check and TypeScript/Vite build pass. Packaged prototype-DOuNOj3r.js / mobile-Bya7Y8qO.js / preview-C6OAzi0g.js with unchanged prototype-BB03Ca4u.css; source patch regenerated. Cache now pilotsolve-site-8b1f80f13020, wiring audit 523+6 passes, old bundles archived. System/default viewport restored.

This verifies the observed appearance-related state-loss fix and phone Day keypad; it does not close populated trip/aircraft, native backup delivery, zoom or all helper workflows.

## FOI longest-answer responsive review

Read current card inventory to choose the longest answer (card119, 472 characters), then navigated through Flight instruction via native section/Next controls without assigning ratings. At 390×844 Day, the answer's internal scroll area reaches its final line and Tap to return; Night appearance retains the same card and scroll position with readable answer/rating controls. At 834×1112 Day/Night, the entire long answer and three rating controls fit in the reviewed study surface. Existing semantic answer/rating colors retained.

No material correction needed for these observations. Restored All remaining cards to Card2 / pass2of238, confirmed238 left and0review later; no ratings changed. Restored System/default viewport. This closes the previously unreviewed long-answer surface, while enlarged-text, native reduced-motion and full interaction matrix remain open. Content was not audited in this visual pass.

## Endorsement directory variant

Reviewed /simply-endorsed/blog/ desktop Day three-column listing, tablet834 Day/Night two-column listing, and phone390 Day/Night lower app CTA/footer. The last Logbook Audit card navigated to its guide; returned to directory. Phone Tab from Open Simply Endorsed CFI reached Search the app with gold focus; document width390. No material fix needed for observed surfaces. System/default viewport restored.

Focused source search across blog/style.css, learn/style.css, simply-endorsed/css/seo.css and assets/contact-form.css found no literal background/text fills except intentional semantic error fallbacks in contact-form.css. This is a scoped source check, not proof about all computed styles. Directory desktop Night and complete phone listing/zoom remain to review.

## Endorsement directory Night and phone listing continuation

Reviewed desktop Night hero/listing, then phone390 Night listing in sequential scroll screenshots through the final article and CTA. Long checkride/recreational headings wrap; all19 cards have350px client/scroll widths and document width390. Day phone checkride cards also reviewed; all19 card text colors compute to canonical ink, link colors to canonical accent/hover ink, with zero card overflow. All19 post-card hrefs resolve to existing local article files. Initial source selector matched0 because cards use post-card, then corrected selector asserted19 before validating targets.

No new correction needed. System/default viewport restored. Baseline directory appearance coverage is now recorded across all sizes, while enlarged text and complete interaction/accessibility acceptance remain open. Local target presence does not prove remote publication or article-content accuracy.

## Blog table word-wrapping correction

Private-pilot-cost article at390×844 exposed words splitting into fragments in table cells (for example Louisville and planning), caused by inherited shared overflow-wrap:anywhere. Set overflow-wrap:normal on .prose table in blog/style.css, preserving its width/layout and existing table-wrap horizontal-overflow container. Reload confirmed intact words,348px table/wrapper width and390px document width. This particular table fits without horizontal overflow after correction; horizontal gesture behavior is therefore not claimed.

Reviewed corrected phone Day top rows and Night middle rows, plus tablet834 Night header/rows. Gray reading surfaces and text remain readable. Refreshed42 blog-family stylesheet URLs; cache refresh and523+6 wiring audit pass. Restored System/default viewport. Wider table variants, lower article CTA, zoom and full-page acceptance remain open. No article data or claims changed.

## Wide blog table keyboard access

The timeline article has the widest current blog table (four columns). At390×844 its table is the actual block scroll container (599px content/348px viewport), while the outer wrapper remains348px. Touch-style horizontal scroll reached the last columns and the page stayed390px. Found no keyboard focus target on the scrollable table. Added tabindex=0 and a descriptive aria-label to all three current blog tables (timeline, cost, aircraft comparison), plus an inset canonical-gold focus ring in blog/style.css. No data, column widths or scrolling implementation changed.

Reloaded timeline: ArrowRight focused the table and moved scrollLeft to40px; further ArrowRight input reached251.5px and the final column. Day/Night screenshots confirm focus ring and readable cells. Phone Day/Night lower article CTA also reviewed: gold booking action and gray message action fit. No external action submitted. Refreshed42 CSS references, cache refresh and523+6 route audit pass. System/default viewport restored. Other table content, zoom and full article acceptance remain bounded; current evidence verifies native table scrolling and focus.

## Training lower-page materials

Reviewed /flight-training-louisville-ky/ desktop Day service cards and empty contact form validation; tablet834 Night form/error/actions; phone390 Night reading/FAQ/final-next-step panel and Day final contact actions. No contact data entered, no email/call launched. Expanded ground-instruction FAQ and validation state persisted across appearance changes; phone document width390. Gold primary, gray secondary and solid reading surfaces remained consistent in observed sections. No new correction needed. Restored System/default viewport. These observations supplement journey checks; full-page combinations and enlarged-text/native reduced-effects acceptance remain open.

## CertPath desktop Day and phone Night form coverage

Reviewed desktop1280 Day intro, selected Private goal, blank logbook form and expanded cost panel. At390x844 Night reviewed form fields, category accents, cost panel and empty Build my plan validation. Validation moved focus to Total flight time, with gold focus ring and semantic error border/text; alert explained all18 missing fields. Document scroll width remained390px. Default aircraft185/instructor45 values and open cost disclosure survived the appearance change. No contact information entered or external submission made.

No source correction needed. Restored System appearance and1280x720 viewport, then returned to Goal; no saved user progress deleted. Contact-gated results, complete workflow combinations, enlarged text and native reduced-effects acceptance remain open. These checks establish visual/interaction evidence only, not regulatory or calculation accuracy.

## CertPath residual decorative-token consolidation

Replaced31 literal decorative colors in assets/tool-system/part61-redesign.css with existing g-surface, g-raised, g-well, g-ink, g-muted, g-accent, g-line and g-edge tokens. This includes hero secondary text, reference/ledger reading surfaces and links, goal labels, focus outlines and no-time review controls. Existing selectors, geometry and semantic green completion values remain unchanged. Many earlier literals had broad downstream overrides; removing them also prevents latent states from restoring cream/olive surfaces. Shadow/backdrop alpha colors remain material effects.

Refreshed the one affected HTML stylesheet hash and app cache manifests; audit confirms523 registered routes and6 supporting documents. Browser reloaded current build and reviewed zero-hour summary at1280x720 Day and390x844 Night. All18 zero hour values retained across appearance switching; Review or edit totals reopened the form. Visible Night numeric fields computed ink244/surface84, document width390. No contact or result-gate submission. Restored System/default viewport and reloaded to clean intro; no saved user progress deleted. Result details remain source-integrated but not rendered-accepted behind the contact gate. No calculation or data code changed.

## CertPath base-control material consolidation

Replaced remaining primary-action rule families in part61.css (default, hover, panel-next and late calculator override) with g-gold/g-key-ink/g-edge/g-shadow. Default secondary controls, hover and text/numeric focus surfaces now use g-raised; legacy blue/orange focus glows use the shared gold accent. Header-action shadows also use g-shadow. Changed only targeted interactive declarations and neutral material variables; semantic invalid/completion styles, print rules, geometry and calculation logic were not modified. Other historical base declarations still require scoped review.

Added part61.css to sync-avionics.py content-hash assets, replacing stale v=1. Sync/check, cache refresh and523+6 wiring audit pass. Current browser reload verified desktop Day selected Private and Continue controls, then desktop Night numeric focus with gold ring and gray fill. No values or contact information entered. System appearance restored. Result/print/native gates remain open; these observations do not close full acceptance.

## CertPath selected credentials and legacy branding

Removed hardcoded orange brand declarations and old blue/orange decorative glows from part61.css. The legacy body.theme-orange class remains for compatibility but its material aliases now resolve to shared gold tokens. Selected option buttons use g-gold, g-key-ink and g-edge. Per-group category palettes, semantic completion/error colors, geometry, data and calculation logic remain untouched. Updated stale comments describing the superseded brand.

Sync/check, cache refresh and523+6 wiring audit pass. Current browser reload: selected Student Pilot on desktop Day renders gold; phone390 Night search for student retained the selection and showed gold focus/native clear, selected credential, gray removable chip and gold Continue. Removing the synthetic credential restored Student Pilot Add. Cleared query, restored No certificate yet, System and default viewport, reloaded to intro. No contact submission. These checks cover selection/search/removal and materials, not contact-gated results or full workflow acceptance.

## Refreshed regression checkpoint after material consolidation

Current shared Node61, Python10, Aero Lab14, FlightRisk27, PilotSolve36 and Part61 wizard98 tests all pass (246 total). Workspace1678 assertions pass; CertPath storage fresh/gate/failure/retry/save/reopen/update checks pass; PilotSolve28-file runtime guard passes. Canonical contrast44/44 (minimum4.75), route wiring523+6 and unreferenced generated assets0. All23 packaged PilotSolve/Aero/FlightRisk JS/CSS files match their local dist outputs by SHA256.

Archived the earlier checkpoint under regression-history and refreshed regression-checkpoint.json with current material, package and test hashes plus source-tree digests. This resolves stale checkpoint hashes after recent PilotSolve/FlightRisk and shared/CertPath stylesheet updates. It does not establish native exports, live OS effects, actual200% zoom, full visual acceptance or content-source accuracy. Production remains unpublished.

## Learn CFI lower collection and native-settings boundary

Attempted to open macOS System Settings for live appearance/reduced-motion acceptance. Computer-use tool reported the Mac locked and automatic unlock unavailable; no OS settings were changed. This is an environmental limitation for native checks, not a failure of the web appearance logic. Other local work remains available.

Reviewed /learn/cfi/ lower related collections at desktop1280 Day/Night and phone390 Night; reviewed final related card and shared footer at phone Day. Night keyboard Tab from Pilot Career Guides reached Checkride Prep Guides with visible gold focus. Phone document width390 equals viewport. All three related-card titles and descriptions wrap without clipping in observed views. No source fix required. Restored System/default viewport. Other category variants, tablet lower sections, enlarged text and live OS settings remain open.

## Shared tool control finish cleanup

Updated tool-core.css: four residual blue control-border declarations use g-line; native text selection uses g-mid/g-key-ink; tool-nav divider uses g-line; menu-button shadows use g-shadow/g-glass-edge; focus rings use g-accent. Preserved blue app-identity tokens and semantic category/scenario palettes, print colors and geometry. Sync refreshed four HTML sources; cache refresh, sync --check and523+6 wiring audit pass.

Current Simply Endorsed browser view: entered New student task and checked only Establish the training goal in the temporary checklist. System resolved Night; reviewed checkbox and1of7 status. Day desktop and Night phone390 completion-review panels retain the disabled final checkbox and explanatory text. Appearance changes retained temporary progress. Unchecked the synthetic review item, restored System/default viewport and returned to All tasks. This adds bounded task/disabled-state evidence; no endorsement issued or personal data entered. The previous regression checkpoint remains valid for tested logic, but its tool-core.css hash predates this CSS-only cleanup.

## Certificate Generator app-owned selection and focus

Updated certgen.css selected-certificate rule to directly use g-gold/g-key-ink/g-edge/g-shadow, replacing certificate-tinted selected fills and rings that had been superseded by the shared cascade. Certificate identity swatches/side accents remain. Input focus halos now use a30% shared gold accent rather than the neutral tool-soft surface; semantic invalid borders remain red. Primary-action base/hover shadows use the shared shadow and highlight tokens. Certificate artwork/canvas/export code, crop guides and geometry were not changed.

Refreshed one CSS reference, caches and523+6 wiring audit; sync --check passes. Browser verified First Solo selection, empty student-name Continue validation and gold focus in desktop Night and tablet834 Day, plus tablet selected-certificate grid. Photo/Review and export actions remain disabled with missing student names. Previously saved synthetic instructor defaults Sample Instructor/CFII retained; no names entered or artwork exported in this pass. Restored System/default viewport. This is bounded form/selection evidence; native-file delivery and remaining crop/review combinations stay open.

## Simply Endorsed fallback controls and offline revision fix

Static scan found no undefined g-* tokens across34 site/app stylesheets (22 definitions,20 referenced names). Updated legacy Simply Endorsed browse launcher primary/hover, selected DPE chips, secondary surfaces, focus and navigation edge to shared gold/gray tokens. App category/status data and palettes retained. Added simply-endorsed.css to sync content-hash assets, replacing v=2. These legacy fallback controls are source-integrated, not newly rendered-accepted by this pass.

Found offline worker still precaching tool-core.css, simply-endorsed.css and part61.css as v=1; its revision previously only hashed the redirect HTML and shared asset entries. refresh-tactile-caches.py now updates all three style URLs by content and hashes every precached asset plus normalized worker policy. Current cache simply-endorsed-tactile-5edfdbeac820; PilotSolve unchanged. Added an isolated filesystem regression that changes each style, app.js and worker policy: every change rotates only the endorsement cache; repeated refresh remains byte-identical. Extended worker-install regression to require current style hash URLs. Six focused Node tests and one Python fixture test pass; sync check and523+6 route audit pass. Native offline browser delivery remains a separate acceptance item.

## Factory-reference Night responsive and provider fallback

Navigated current local factory-reference page. DOM confirms Sketchfab SDK loaded and assigned the expected provider embed URL; hosted model did not signal ready. Waited for the existing45-second initialization deadline: loading changed to Hosted reference unavailable with an Open Rotax configurator link; camera/equipment controls remained disabled. No cause beyond failed readiness is established, and no geometry was fetched or substituted by this work.

Reviewed tablet834 Night loading/control shell and phone390 Night disclosure, disabled controls, unavailable panel and gold return action. Returned via Explore the animated parts to the local Rotax engine application. Default viewport restored; System preference retained. This closes previously unobserved responsive Night shell/failure-state coverage, but successful provider loading and enabled model controls remain unverified. No source change needed.

## PilotSolve populated aircraft readout correction

Entered a synthetic aircraft profile (1500lb empty weight,40in arm) through the actual numeric keypad and loaded profile stations. Empty station computed60000.00lb-in; incomplete other stations correctly withheld total loading. Desktop Night revealed the calculated field label touching its value. Added scoped flex spacing/wrapping and tabular numerals to .pilot-app .calculated-field in app-owned prototype.css; no calculation/state/data changes.

Runtime28-file guard, TypeScript and Vite production-config build passed. build:app offline stage initially used its default nonexistent sibling site path; reran build-offline.mjs with SUAREZ_SITE_ROOT pointing to this worktree successfully. Packaged prototype-CbtqH6Pp.css, prototype-CaUbXWQg.js, mobile-Co4tACtC.js and preview-CNp9GoIX.js. Regenerated source patch, refreshed caches (pilotsolve-site-accfb0d215f3),523+6 audit passes; archived four superseded bundles with manifest under pilotsolve-loading-readout-followup.

Reloaded current package and repeated the synthetic profile entry. Phone390 Day and Night screenshots show the calculated label and60000.00 separated, solid readout, tactile editable values and no page overflow. Appearance Settings round trip retained station values. Restored System/default viewport and reloaded; confirmed no station-name fields and blank total, then returned Tools. No saved calculations changed or backup exported. Other stations, populated trips, helpers and native backup delivery remain open.

## PilotSolve populated trip review

Used native keypad and time separator to enter one Synthetic preview leg:60NM, true course90degrees, TAS120kt, wind from0/speed0, fuel flow10gal/hr and departure12:00UTC. Computed results displayed120kt groundspeed,0WCA,90degree true/magnetic/compass headings,00:30ETE,5gal and12:30arrival. This is a synthetic arithmetic/interaction example, not operational planning guidance.

Reviewed desktop Night and phone390 Day/Night populated leg/totals and gold Add leg. Settings appearance round trips retained all values/results; phone document width390. Solid result surfaces remain readable. No new source correction needed. Restored System/default viewport, reloaded and confirmed zero Leg name controls before returning Tools. No saved data or exports changed. Multi-leg/phase variants, helper workflows and native backup delivery remain open.

## Homepage desktop lower controls

Reviewed1280x720 Night pathway cards, instruction-service rows and empty-contact validation; Day pathway cards, tool cards and retained contact error. Keyboard Tab from New to flying reached Already training with clear gold focus. Current homepage pathways are three links, not selectable journey tabs; training-page journey controls are covered separately. Gold booking/email-draft actions and gray cards retained readable contrast in observed sections. No contact values entered, no email/call launched. Closed draft disclosure, restored System and returned hero.

No source correction needed. These checks add desktop lower-section evidence; expanded instructor biography/certificates, remaining viewport combinations and enlarged-text/native-effect acceptance remain open.


## Homepage expanded biography and credentials

Reviewed expanded biography and FAA credential disclosure at desktop1280 Night/Day and phone390 Night/Day. Native summary Enter toggles and Tab into the first credential source link worked, with visible gold focus. Phone document width remained390. Existing source links were not opened and content accuracy was not re-audited.

Found a legacy -8px margin on the profile Say hello control causing its box to overlap the availability label by about4px on desktop. Replaced it with12px separation and a phone-specific8px top gap/zero left margin. Reloaded current source and verified desktop Day/Night separation and phone Day spacing. No navigation/content changes. Route wiring audit remains523/523 plus6/6 supporting documents; no app rebuild needed for this homepage-only correction. Restored System, desktop viewport and collapsed biography/credentials. Tablet variants, enlarged text and remaining acceptance gates remain open.


## FlightRisk methodology lower references and actions

Reviewed lower reference cards, calibration note and Run an assessment action group at desktop1280 Night, tablet834 Day/Night, and phone390 Day (Night phone action group also reviewed). Gold primary actions and selected navigation, gray secondary controls, solid reading cards and semantic risk badges remain distinct. Phone document width390 matches viewport. Keyboard Tab from Open the tool reached Read the guide with visible gold focus; FAA FRAT source link to PAVE source link likewise showed visible Day phone focus after scrolling settled. No external source or contact link opened, and no scoring/content changes made. Restored System/default desktop viewport.

These observations complete the named lower-section visual checks for those combinations; desktop Day/phone Night reference combinations, full-page keyboard traversal, enlarged text and native-effect acceptance remain open. No implementation correction required.


## Crank & Core legacy copper stylesheet retired

Material source scan found engine-explorer/app/brand/copper-theme.css still linked by the app shell beneath Aurum overrides. Archived the exact file outside deployable source to style-archive-before-aurum-20260924/crank-copper-theme-retirement/legacy-copper-theme.tar.gz; verified archive bytes before removal and recorded SHA-256/size in docs/design/archived-copper-theme.json. Removed its HTML link and retained the circular product-mark geometry and wordmark accent through the Aurum adapter. No model/scientific colors or runtime logic changed.

Refreshed shared content-hash references/caches. Route audit523+6 passes; both crank-blueprint regression tests pass. Reloaded browser confirms no copper stylesheet link, circular mark50%, and selected Lycoming button uses canonical gold gradient/dark label. Desktop Night collection/Whole engine Study and Day Fuel injection Study views rendered correctly after live selection. Restored System and engine collection. This removes a conflicting active legacy source; it does not complete remaining model-specific/zoom acceptance.


## FOI primary material source consolidation

Removed welcome/completion primary buttons from the legacy gray-secondary rule and gave both a scoped canonical gold/edge/key-ink material with shared glass-edge/shadow tokens. Start was already gold via shared ID override; completion Review later cards had remained gray in app-owned CSS. Replaced remaining targeted button/contact-action literal shadows with shared tokens. Semantic rating/answer colors and all study behavior/data remain unchanged.

After reload, DOM computed styles confirm Start and hidden Review later cards both resolve to the same canonical gold gradient/dark text/bronze edge. The hidden completion control has style evidence only, not a rendered completion-flow review. Resumed existing Card2 and flipped it; desktop Night and phone390 Day ratings remained amber/neutral/green and readable. Restored System/default viewport and reloaded: Card2/pass2,238 left,0review unchanged. Section-dialog regression test passes; sync and523+6 route wiring audit pass. No saved progress reset or external link opened.


## Shared decorative fallback consolidation

Audited public/shared CSS literal colors. Retained category palettes, sky artwork, semantic risk/progress colors and neutral masks. Replaced two old FOI rating palette declarations in premium.css with the same semantic token mixes used by the app; replaced olive/gold search-focus/hover shadows and FOI primary shadow with shared gold/material tokens. Conversion-bar call-button default/hover backgrounds now use shared glass/raised surfaces. Footer fallback text/surface/focus values now resolve through Aurum rather than historical blue-black literals. Layout and navigation untouched.

Sync refreshed531 HTML sources; Simply Endorsed cache is simply-endorsed-tactile-2a749a23e8ec. Wiring523+6 and44 canonical contrast pairs pass (minimum4.75:1). Eight Node footer/offline tests and seven Python footer-sync/cache-refresh tests pass. Browser reload checked FOI Night primary controls; phone390 homepage footer Explore disclosure and Learn focus rendered in Day/Night. Restored System/default viewport and collapsed mobile Explore. Legacy conversion-bar/filter-rail controls have source integration only here; no claim of rendered coverage for those dormant/alternate controls.

Follow-up: footer CSS had not been included in sync-avionics shared_versions. Added it and refreshed519 HTML references to its actual content hash bcb3bee0de; repeat --check reports0stale,523+6 audit passes. This closes the stale footer URL discovered during final hash verification.


## Managed theme asset integrity

Replaced duplicated sync-only asset lists with config/theme-assets.json and scripts/theme_assets.py. Both sync and audit now use the same 32 content-hashed theme assets. Audit validates stylesheet/script URLs anywhere in registered or supporting HTML, including footer body links, relative app URLs and single-quoted attributes; rejects missing/stale/duplicate v parameters and the retired Crank copper stylesheet. External assets and nonstylesheet links are excluded. All authored stylesheet families found in the route report are now managed; generated Vite filenames retain their build hashes.

Added library-search.css, premium-home.css and model-info.css; two formerly unversioned entry-point links refreshed. Seven focused unit cases plus the existing cache-refresh test pass, including changed asset bytes invalidating an old URL. Final sync check0stale and strengthened523+6 audit pass. This is structural cache/integration evidence, not new visual/workflow acceptance. No CSS declarations or application logic changed in this pass.


## Simply Endorsed completed temporary intake review

Using the local New student task with no pilot data, checked the six prerequisite/conditional controls. The final Review the next training milestone checkbox changed from disabled to enabled. Checked it to display7of7reviewed and the explicit temporary-review/endorsements-separate message. Reviewed desktop Night completion summary and phone390 Day summary/final row, plus Night final row. Day keyboard Tab from final checkbox reached Details & source with visible gold outline. Appearance changes retained temporary checked state. Semantic completion green, category identity and gold navigation remained readable.

Used Reset to clear all seven temporary selections; verified0of7,2unresolvedconditions and final checkbox disabled. Returned All tasks, restored System and desktop viewport. No endorsements issued, no pilot records entered, no source accuracy claim. No source correction required. Other guided workflows and enlarged text remain unverified.


## Embedded route palette cleanup

Source inventory found0inline style-attribute color literals but10embedded style rules with hex colors across five authored routes. Replaced training journey dot/selected gradient/border and gold connector/glow literals with shared tokens; replaced Simply Endorsed/CertPath muted fallback text, endorsement introduction icon/step badge backgrounds/text, and engine host canvas/title colors. No layout, navigation, diagrams or content changes. Follow-up embedded-hex scan finds0matches (bounded to CSS color/background/border declarations, not a complete color/cascade proof).

Reviewed training Instrument desktop Night and Commercial phone390 Day selections; endorsement introduction icons/numbered steps desktop Day and numbered steps phone390 Night, no horizontal overflow; engine host/collection desktop Night. Restored System/default viewport. Route audit523+6passes. These visual observations do not establish instructional accuracy or complete viewport/interaction acceptance; fallback text rules in the two apps remain source-level evidence for this pass.


## FOI rendered completion flow on isolated local origin

Opened a separate local origin http://localhost:8898/foi-cards/ (primary preview uses127.0.0.1). Its initial UI showed no saved pass,238remaining,0review. Selected the11-card ADM and safety section, marked its first card Review, then advanced the remaining10with Next. Actual workflow reached Session complete with238remaining/1review and the gold Review later cards action. Desktop Night screenshot shows gold primary plus visible keyboard focus; phone390 Day screenshot shows primary/secondary/back controls without clipped text. Clicking Review later cards opened Card121,pass1of1. This supersedes prior style-only evidence for that completion action. No original127.0.0.1progress was accessed or modified by these interactions.

Cleanup boundary: Reset study progress click on the isolated localhost origin caused a browser input timeout; getJsDialog returnedundefined, AX/key/close commands timed out while tab inventory still listedtab7. No reset/closure confirmed; isolated test data may remain238remaining/1review in localhost storage. Default viewport restoration also did not execute after close timed out. Original session unchanged is supported by separate-origin isolation, not a post-check this turn. Native browser reset prompt recovery remains open. No source changes in this pass.


## Shared/PilotSolve regression follow-up and browser recovery

Browser inventory confirms isolated testtab7no longer exists. Main preview responds normally and viewport restored1280x720. Reopened original127.0.0.1FOI and confirmed Card2/pass2,238remaining,0review unchanged. The localhost test-storage reset remains unverified; tab closure does not prove clearing it.

Fresh shared Node61/Python18/PilotSolve36tests pass (115total); PilotSolve runtime28protected files pass. All five current packaged PilotSolve bundles exactly match local dist bytes. Strengthened route audit523+6, canonical contrast44/min4.75, and0unreferenced generated assets pass. Saved current artifact hashes and exact scope in regression-followup.json; older full-app checkpoint remains historical. No claim that these checks prove remaining rendered/OS/zoom/native-export acceptance.


## FlightRisk guide responsive timeline

Reviewed phone390Night timeline steps2/3/5and Day step1; tablet834Day/Night alternating timeline covering steps1–4. Gold visited markers/connectors, solid gray callouts and unchanged instructional risk-color images remain readable without horizontal overflow (tablet document834). Native images deliberately retain their white artwork backgrounds. Opened INCOMPLETE FAQ at tablet Night and used Tab to next question; visible gold focus and readable expanded answer. Closed FAQ, restored System/default1280viewport.

No source correction required. These bounded observations close the named responsive timeline gap for reviewed portions, not every full-page/appearance combination, enlarged-text acceptance, or aviation claim accuracy.


## Reduced-motion pseudo-elements and pressed links

Shared CSS review found quiet/reduced-motion blocks omitted ::before and the old reduced-motion button:active transform override was less specific than primary pressed rules; anchor primary actions were also uncovered. Added ::before to animation/transition/scroll suppression and a specificity-matched active control selector for button/link/summary/role-button controls. Layout transforms remain untouched. Applied equivalent behavior to the explicit quiet-effects setting.

Browser FlightRisk guide menu Solid controls check resolves quiet; Start an assessment plus its ::before/::after all report animation none/transition0s, dock backgroundrgb74 opaque/backdropnone and screenshot retains proper placement. Unchecked Solid controls and confirmed normal effects restored, System unchanged. Native OS reduced-motion/active pointer rendering remains unverified. Sync545sources; caches pilotsolve-site-d838f896dca0 and simply-endorsed-tactile-25d061e215db;523+6wiring passes. Recent regression-followup hashes predate this CSS/cache update and are historical for those artifacts.

Focused navigation/tactile/offline/PilotSolve-cache regression run:23tests pass after the reduced-motion CSS update. These logic tests supplement, rather than prove, the rendered CSS motion behavior.


## Quiet text-action material

Added a shared quiet-action adapter for btn--tertiary,button.text-button,part61-text-button and part61-quiet-button. These declared text actions now have transparent surfaces/borders, no hardware shadow, gold text and underline on hover/focus; dimensions/padding and shared focus ring unchanged. This addresses the raised Back to deck treatment observed during FOI completion without altering primary gold actions.

Browser reviewed homepage Say hello focused at desktop Night and phone390Day; gold text and clear focus ring retain layout/touch area. Restored System/default viewport. FOI/CertPath selectors require follow-up rendered checks after this shared change. Sync545sources, caches pilotsolve-site-9e65f88e1b8d and simply-endorsed-tactile-29721178de24; route audit523+6passes. Canonical contrast44pairs pass; repeat sync0stale. Prior regression checkpoint hashes remain historical.


## Quiet-action app follow-up

CertPath: opened default Private/No certificate scenario and blank0of18experience form. Desktop Night and phone390Day show zero-fill/Next group text controls as gold with no background image/shadow; focused zero-fill has visible gold ring. Regular Back remains gray secondary hardware and returns to background. No values entered/zero-filled, contact gate not accessed. Restored System/default viewport and reloaded to intro.

FOI: reopened isolated localhost origin; prior test Review mark still persisted, confirming reset had not cleared it. Resumed its1-cardpass and advanced Next to completion. Reviewed quiet Back to deck focus at desktop Day and phone390Night while primary Review remained gold and secondary remained gray. Back to deck returned welcome correctly. Restored isolated origin to System, default viewport and closed tab8successfully. Test origin retains238remaining/1review; primary127.0.0.1study data not touched. No new source correction needed; this supplies rendered follow-up for the quiet-action adapter.

## FlightRisk IFR risk and confirmation controls

In the local assessment, a synthetic rated IFR path reached LOW; switching to Not rated displayed the existing HIGH override in red. Desktop Night and phone390 Day/Night results retained gold Print and gray Back to edit, distinct from green/red risk meanings. The visible example was returned to Private/VFR/Not rated at score3 LOW, System appearance and default viewport. Temporary hidden IFR answers may still be stored; no print delivery or scoring-model validation is claimed.

FlightRisk Reset, Clear local data and Results > New assessment now open the same Aurum confirmation component. Desktop Night Reset/Clear/New and phone Night New, Day Clear were inspected. Gold confirm, gray safe cancel, solid reading panel and centered phone stacking remained legible. Initial focus landed on Keep current data; cancel and Escape retained the visible score and restored focus to the invoking control. Canceling nested New assessment returned to the results overlay. No live clear/reset confirmation was performed on the primary test origin. Source tests, package/archive and structural evidence are recorded in IMPLEMENTATION_STATUS.md; full native/zoom/export acceptance remains open.

## Crank & Core Rotax firing guide

Opened the actual Rotax explorer and Learning guide at desktop Night. Its closed and expanded Power-stroke order sections had old dark copper/slate text on the gray Night inspector, visibly fading the onset captions and timing explanation. Scoped material overrides make those labels use shared ink/muted/accent tokens and the selected controls use gold; the instructional four-stroke color strips and 3D model were retained. After reloading the versioned CSS, desktop Night and phone390 Day/Night screenshots show readable closed/expanded sections. The user-facing phone panel/dock fit without horizontal overflow in the observed state. Selecting cylinder4 changed the visible next-cylinder label to2 and crank angle to180 degrees; selected gold followed cylinder4. Restored cylinder1 and System/default viewport. Eleven computed Night label pairs against the solid panel measured5.75:1or higher. Enlarged text and other engine modules remain unverified.

## Crank & Core Rotax component browser/details

Desktop Night Parts exposed low-contrast pinned-runtime slate labels under otherwise readable component names. Selecting Left crankcase half also exposed dark purpose, part-number, disclosure and reference text in Details. Scoped editable-overlay rules now put muted ink on these secondary reading surfaces, gold on the selected part row, and dark text on that gold. Browser readback confirmed the selected row's gold gradient and `rgb(43,35,22)` caption versus `rgb(208,208,208)` for an unselected caption. Desktop Night/Day Details and phone390 Day/Night lower references were visually reviewed; Technical details and Geometry evidence & references expanded. Phone Night Source catalog showed readable entry/status captions, and phone Study controls remained usable. System appearance/default viewport restored; engine model and catalog data unchanged. Full model-specific, enlarged-text and native preference coverage remains open.

## Blog narrow table and zoom attempt

The blog directory and private-pilot-cost article reflowed at 640px and 320px without page-level horizontal overflow in the local System/Night preview. The cost table at 320px revealed its third column was clipped because its 278px frame had no scroll range even though the table's content extended farther right. After adding a 35rem minimum width to the table inside its existing `.table-wrap`, the frame reports 282px of horizontal scroll at 320px. A horizontal scroll reached the complete third-column explanations; focusing the table and pressing ArrowRight moved the scroll offset. At 390px the table has 212px of horizontal range and no page overflow; at 1280px it fits without horizontal scrolling. The default viewport override was restored.

Native Chrome zoom was verified at 110% and 125%, then the computer-control connection disappeared during further zoom input. The eventual zoom level is unknown, and actual 200% review remains open. This is not counted as enlarged-text acceptance.

## Aero Lab Geometry Studio output controls

On isolated localhost at desktop1280 System/Night, opened Aircraft & atmosphere then the lazy Geometry Studio. Selected Edit geometry was gold, while the main Save .afoil output was gray like Open and Export SVG. The final preserved-source rebuild gives Save an explicit `studio-save` class and the shared gold gradient/dark text/bronze edge. The temporary site-only adapter was removed. Browser readback on the rebuilt `GeometryStudio-Dst1IrYT.css` chunk confirmed the same gold material in Night and Day, and screenshots show the footer hierarchy. Tab from Save reaches Export SVG with a gold border and visible gold focus outline. Scene geometry and export logic were not edited.

Save .afoil and Export SVG each produced their app-level exported status, but neither generated a browser download event in this test surface. Native file delivery remains open. At 390px the existing desktop-tool handoff was visible without horizontal overflow, and returning to1280px restored the workspace. System appearance was restored on the isolated origin; the temporary test tab was closed.

## CertPath stale zero-control and compact endorsement drawer

CertPath on isolated localhost: the current goal card, pilot background and blank 0/18 experience groups showed gold selected/primary controls and gray secondary/quiet actions. The old red `.part61-zero-field-button` and `.part61-zero-input-row` classes had no markup or script references; their unused CSS was removed while red validation remains. Reloaded after the hashed style update and observed the gold Start your path control, with no data entered or results accessed.

Simply Endorsed at320px System/Night: task header/search, Browse categories drawer, expanded Private pilot group and selected two-item Private pilot library fit the viewport without page overflow. Category icons/badges retained their blue information color; the selected Library dock control was gold. This adds compact-width evidence, not a full workflow or actual text-zoom check. Default viewport restored; temporary isolated tabs closed.

## Rebuilt Aero Lab Studio and compact PilotSolve controls

The packaged Aero Lab rebuild now loads `index-DEMBVoqP.js` and lazy `GeometryStudio-Dst1IrYT.css` with no site-only Studio adapter. Opened the current package at desktop1280 System/Night and explicit Day: Save .afoil computed the same metallic gold gradient, bronze border and dark label in both, while Open and Export SVG stayed gray. The live Studio Save action still reported export completion after the rebuild. Closed the Studio, restored System and closed the isolated test tab. The previous generated bundles were SHA-archived outside the site; source tests, asset-byte comparison and archive dry run are recorded in IMPLEMENTATION_STATUS.md. Native saved-file delivery remains unverified.

On isolated PilotSolve at320px System/Night, the tool launcher and Crosswind calculator's result, unit controls and step keys resolved to gold/gray materials. The document width equaled320px. Scrolling within the app exposed the disabled Save and Use result actions, How this works and Clear this calculation above the dock. No inputs or saved calculations were changed; temporary tab closed and viewport reset. Actual 200% browser zoom remains separate.

## Current FlightRisk cross-screen material recheck

On the current isolated localhost package, desktop1280 System/Night Assessment, Guide and Methodology loaded through the FlightRisk dock. Start assessment, guide launch, selected profile controls and selected dock keys retained the same metallic gold; scoring/navigation secondary actions stayed gray. The assessment's green/amber/red risk meanings and the methodology's red hazard/green mitigator labels remained distinct from the gold interaction layer. The Guide hero and Methodology explanatory cards used solid graphite reading surfaces.

At390px, Methodology, Guide and Assessment each had document width390px. The Guide's first illustrated step stayed inside its timeline card; Assessment's flight-rule and Private Pilot selections, with its bottom dock, stayed visible. Explicit Day showed the `#E3E3E3` family behind solid reading cards and gold actions. In the Methodology table, A Pilot became gold when selected while other section controls stayed gray; the mobile factor rows exposed detail buttons and retained red/green point badges without page-level horizontal overflow. Restored System appearance and the1280px default viewport. This is a current-build material/navigation spot check, not a full risk workflow, native print, reduced-motion, or enlarged-text acceptance.

At320×700 on a fresh isolated Methodology tab, document width stayed320px. Selecting A Pilot scrolled to the section and rendered gold selection; the two-column factor rows, dark detail toggles, point badges and bottom dock stayed within the viewport. Expanding the first factor exposed its fatigue detail inside the table and changed the toggle's accessibility state to expanded. Restored the default viewport and closed the tab. This does not substitute for actual200% browser zoom.

## Current public-control sample and homepage sky correction

Sampled the current desktop/System-Night computed materials on twelve representative public routes: home, training, main Blog index/article, Learn index/category/article, endorsement index/introduction/guide, discovery-flight landing and tools directory. Primary and selected controls used the gold gradient/dark label, secondary controls used graphite, and the Simply Endorsed task-card colors were category identities rather than a competing global action palette. Initial-shell scans of Simply Endorsed, CertPath, FlightRisk, Aero Lab, Crank & Core, FOI Cards, PilotSolve and Certificate Generator found no solid saturated action fills outside those task categories. This is a sampled control audit, not visual acceptance of every interaction or all523routes.

The Day switch exposed a real homepage defect at local nighttime: the hero reported appearance `day`, heading `rgb(39,39,39)` and sky phase `night` with `--sky-night:1`, rendering dark text against a dark cloud scene. The corrected sky keeps the original renderer/cloud motion and solar calculation but constrains solar variation within the selected appearance. The updated local Day hero at the same nighttime reported sky phase `day` and `--sky-night:0.12`; screenshot showed dark heading/lede legible over pale, muted clouds. Switching to Night without reloading immediately reported phase `night`/factor1 and showed light text on dark clouds. System was restored on the isolated origin. The static fallback now starts at a dark factor before paint when Night is resolved. Browser-computed state and screenshots cover this desktop state; a live OS switch and full responsive hero pass remain open.

At390×844, the rebuilt hero showed Night light text over dark clouds and Day dark text over pale clouds after live appearance switching. The gold primary, gray secondary and selected dock controls remained legible in both, and document width stayed390px. Restored System/default1280viewport and closed the temporary tab.

At834×1112, the same hero/controls were visually reviewed in System/Night and explicit Day. Both sky phases matched text color, the gold/gray actions stayed distinct, and document width equaled834px. Restored System/default viewport and closed the test tab. Actual zoom and live OS switching remain open.

## Crank & Core ignition comparison at narrow phone width

Opened the Rotax 912 ULS ignition lab in the isolated preview. At390px, the Night and Day opening surfaces retained the gray reading hierarchy, metallic selected Energy paths/Circuit ON controls, and intact teaching models. At320px Day, the three-column comparison table squeezed Function and Distribution into broken fragments. The app-owned Aurum overlay now gives this table a 560px minimum width inside a horizontally scrollable section, with a visible sideways-scroll cue and a keyboard-focusable named region; the heading and cue remain visible as the table moves. The table content and model bundle are unchanged.

After loading the updated assets at320px, the comparison region measured302px client width and592px scroll width. ArrowRight moved the scroll position; the right-hand Conventional magneto values became readable while the heading stayed visible. Next spark still updated the lab with the cue present. Day and Night opening controls were visually reviewed at390px, and the320px Day table was reviewed before and after scrolling. This closes only this responsive table defect; model-specific interaction, native touch, enlarged text and actual200% zoom remain open.

## Simply Endorsed search action hierarchy

The active workspace Search button was a flat gray36px control, despite being the primary action beside the search field. Updated the app-owned workspace stylesheet so Search uses the canonical reflective gold, bronze edge and dark label at44px, while the query's Clear button remains raised gray at44px. The older blue guidance table-of-contents rule found during source inspection did not appear in the current guidance routes and was left unchanged.

On the isolated localhost preview, desktop System/Night Guidance showed the new gold Search action. At390px Night and explicit Day, submitting “solo” produced86 matches and exposed distinct gold Search/gray Clear controls; clearing the query returned to the Time limits guidance article with focus in the empty search field. At320px Day, both buttons fit inside the288px search form, and document scroll width equaled320px. This verifies the observed header/search states, not every Simply Endorsed workflow or native enlarged-text behavior.

## CertPath legacy palette retirement and multi-goal check

The active CertPath Goal screen and its reachable multi-goal picker were inspected in System/Night on the isolated preview. Private remains a gold selected control, the goal search uses a gold focus edge, goal options and order/remove actions are gray, and Continue is metallic gold. Selecting Instrument Rating - Airplane as a second goal closed the picker and kept both goals as solid reading cards. The former `.part61-calculator-header` and `.part61-step-rail` elements are present only in the hidden legacy shell in this screen.

Removed the superseded five-color gradient from the legacy header rule; its later canonical gold rule remains authoritative. Replaced its fixed white step-circle fallback/halo with shared gray canvas tokens and preserved the green completed-step meaning. Reloaded the current CSS and measured the header pseudo-element as the canonical gold gradient. This is source retirement plus a bounded multi-goal visual check, not a claim that the hidden legacy rail is user-visible or that the full CertPath result flow is accepted.

## Southern Indiana landing and PilotSolve settings follow-up

In the isolated current preview, Southern Indiana's desktop Night hero retained a gold discovery-flight primary action, gray training-path secondary action and readable contact action. At390px Night, the hero, expanded FAQ and contact form fit with the bottom dock; Name focus used gold and the email-draft action was gold. At390px Day, the hero, service cards and contact form remained readable on gray/solid surfaces. At320px Day and Night, the service-card text and links remained readable, the dock remained clear as content scrolled, and document width equaled320px in both. No form was submitted. This extends only the local-landing surfaces named in the acceptance matrix; lower sections, actual text zoom and other local landing variants remain open.

The current packaged PilotSolve Settings screen at320px shows Night, Day and System, resolving the earlier Dark-label note. Night and Day each showed a metallic gold selected segment, gray unselected segments, readable solid settings cards, and no document-level horizontal overflow. The shared preference was restored to System, the viewport override reset and the temporary isolated tab closed. No backup or profile was changed; native backup delivery and actual200% browser zoom remain open.

The distinct Louisville flight-instructor landing at390px showed its hero in Day/Night with a gold call action, gray training-path action, retained CFI identity mark and credential badges. Night service cards and Day service cards kept their solid reading surfaces and quiet underlined links. Day and Night contact fields and gold email-draft action fit above the dock; Name focus was visible in Day. Document width equaled390px in both. No email draft was opened. System/default viewport were restored and the temporary tab closed. The current Chrome browser surface was unavailable to computer control, so this does not close the actual200% zoom gate.

## Remote ground landing and Simply Endorsed category contrast

The remote-ground service landing was inspected in the isolated preview at desktop1280 Night and phone320 Night/Day. Its hero uses gold booking/call actions and gray guide/question actions; the contact fields and expanded FAQ remain solid and readable. At320px, the document width equaled320px in both appearances and the dock cleared the lower controls as the page scrolled. No contact draft or call was opened. System/default viewport were restored.

Simply Endorsed's Knowledge Test list exposed a real small-text contrast gap: Test prerequisites used `#b5aaff` on the Night `#4a4a4a` card, 4.29:1. The category's bright lavender is now `#bbb0ff`, 4.54:1 on that card; its deeper Day purple and filled-card category identity remain unchanged. A follow-up found that the lighter Night hover card would have reduced six category labels below4.5:1, so Night hover now keeps the solid `#4a4a4a` surface and adds a gold border. A real pointer hover on the current preview reported `:hover` true, `rgb(74,74,74)` background and `rgb(236,211,155)` border. After managed model/style URLs rotated, the browser showed `rgb(187,176,255)` on `rgb(74,74,74)` in desktop and320px Night, with no document-level overflow. Desktop Day kept `rgb(99,86,216)` on `rgb(239,239,239)`. The expanded source-bound audit checks white on all13 category fills and label colors on normal/hover solid Day/Night surfaces:109/109 material/category pairs pass, minimum4.54:1. This is a bounded semantic-palette check, not a full rendered accessibility audit.

## Tools directory orbit first-frame correction

The Night tools directory initially rendered all seven app icons/labels together before their transform transitions spread them into the selected orbit. The catalog now suppresses transitions while its first layout is committed, then re-enables them for selection changes. On the isolated preview, the first visible frame at desktop1280, phone390×844 and tablet834×1112 showed distributed icons around the focused stage; the phone document width equaled390px. Next/Previous selection still changed the stage, pressed app and status, ending at `1 of 7 · PilotSolve`. Explicit Day was then checked at1280px desktop and390px phone on the first visible frame: the pale gray canvas, solid stage, gold filter/open action, selected orbit ring and bottom dock remained distinct; phone document width again equaled390px. A left pointer drag across the390px Night orbit rail selected FlightRisk, updating its stage, pressed icon and `2 of 7` status; Previous restored PilotSolve. System/default viewport were restored and temporary tabs closed. This verifies initial layout and pointer selection at those sizes; native touch swipes, actual200% zoom and reduced-motion rendering remain separate acceptance work.

## CertPath result-panel material correction and app entry sweep

Before correction, the local Night CertPath verdict and combined-summary nodes computed translucent white (`rgba(255,255,255,.58)`) behind `#f4f4f4` text. The shared callout/readout surface now computes opaque `#4a4a4a` in Night and `#efefef` in Day, with matching light/dark text. The current Night Goal screen kept its gold Private selection and Continue action, gray Instrument/Commercial and Add another goal controls, and unchanged geometry. These result nodes were measured in the DOM; a populated result was not rendered because the normal result path is contact gated. No calculation or exported content changed.

The initial visible viewport of FOI Cards, PilotSolve, Aero Lab, Crank & Core, Certificate Generator and Simply Endorsed was scanned in both appearances for flat colored control fills. The only such surfaces found were Simply Endorsed's six task cards, whose colors identify task categories. This is a computed-style sweep of entry states, not visual acceptance of every page or deeper app state. System was restored and the temporary test tabs were closed.

## Shared dock selected-key first frame

On the isolated preview, the shared dock's selected key used the correct gold gradient but its label briefly computed at an intermediate gray during navigation initialization. The final stylesheet removes `color` from that key's transition list. After fresh HTML reloads, Home, Tools, Learn and FlightRisk each immediately computed `rgb(43,35,22)` selected ink with the canonical gold gradient; the previously sampled stale Learn document was refreshed to the current `avionics.css` URL before acceptance. At390px, the Tools selected key and surrounding dock were visually checked in Day and Night, with document width equal to390px. System/default viewport were restored and the temporary tab closed. This checks first-frame ink on those routes, not every navigation transition or actual browser zoom.

A read-only diagnostic walked rendered buttons and links on twelve distinct public/app entry pages in Day and Night. It found no solid-background text pairs under4.5:1, and the sampled primary gold controls shared one computed gradient recipe. Gradients, nested text overrides, dialogs, hidden controls and deeper app states require separate rendered review; this scan does not pass the complete route matrix.

## Shared tool-core blue fallback retirement

The old `theme-blue` body class still supplied blue tool/action/category defaults to Simply Endorsed and Certificate Generator even though their active controls had been overridden. The block now resolves to Aurum gray/gold tokens. On the current isolated Simply Endorsed entry screen, the Search action remained gold with dark ink, and all six task-card category fills stayed unchanged in Night and explicit Day. The body fallback accent changed from old blue to `#ecd39b` Night and `#73551b` Day; Certificate Generator also resolved gold after its current stylesheet loaded. System was restored. This is a source/entry-surface verification of default tokens, not a full guided-workflow or export acceptance.

## Shared reduced-effects pointer and transition check

The current homepage was opened in a headed Chromium browser with `prefers-reduced-motion: reduce` emulated. Before correction, the gold primary button retained `box-shadow 0.22s, filter 0.22s, transform 0.1s`; after correction it computed `none`, as did the dock key. A DOM/pseudo-element scan found no active transition or animation above 10ms in the homepage shell. The menu's Appearance tab initially failed a real pointer click: pointerdown hit Appearance but pointerup hit the tablist after the generic `:active { transform: none }` rule removed its positional transform. After excluding the tab from that press override, pointer Explore → Appearance succeeded in emulated Day and Night. Night's menu visually retained its gold selected Appearance and System controls on gray glass. With reduced-motion emulation cleared, explicit Solid controls still set the primary transition to `none` and allowed the same pointer tab clicks; switching Solid controls off restored normal 220ms/100ms feedback. The test browser ended at System and Solid controls off. Native OS reduced-motion and actual browser 200% zoom remain unverified.

On the Tools directory, the same browser emulation removed computed transitions from the orbit app key and Open PilotSolve control. A DOM/pseudo-element scan found no active animation/transition above 10ms, while Next app still updated the stage to FlightRisk, its pressed orbit key and `2 of 7` status. This is one directory interaction in emulated reduced motion, not native touch or a complete route acceptance pass.

## FlightRisk provisional result and Aero Lab download delivery

In a fresh headed local browser, FlightRisk's Day/Night entry kept the selected VFR and Private Pilot controls gold, secondary controls gray and risk colors distinct. Confirm flight profile displayed a green Profile confirmed status; Back to edit and reload preserved it. Review incomplete opened an opaque provisional result dialog at desktop in Day and Night, with amber completion warning, green/amber/red score arc, gold Print summary and gray Back to edit/New assessment. At 320×700, the Day/Night dialog fit the viewport without horizontal overflow; scrolling reached the score breakdown and training-aid note above the fixed footer actions. The assessment still had 49 required answers/profile values remaining; no print or final result was accepted.

Aero Lab's default two-object Geometry studio then produced actual browser download events: `aero-scene.afoil` and `aero-scene.svg`, both with null browser failure and temporary local files of 7,449 and 3,969 bytes. The `.afoil` text had one scene, two objects and matched parenthesis counts; XML parsing confirmed the SVG root and viewBox. Each file was reopened through Open .afoil or SVG; the live status reported the loaded filename and the editor still exposed two objects. The appearance check for this studio predates this export observation; this pass verified local-browser default-scene delivery/round-trip, not edited-scene fidelity or installed-app download behavior.

## Certificate Generator JPEG event and exported artwork

In a fresh headed browser, the normal First Solo wizard reached Review with synthetic Test Pilot / Sample Instructor names and the optional photo omitted. Clicking the specific preview `#cgDownloadBtn` yielded a successful browser download of `FirstSolo_Test_Pilot.jpg`, saved as a valid 1080×1920 JPEG. The rendered file retained the original blue-glass certificate art, placeholder illustration and legible synthetic names. Switching to emulated Night changed the UI canvas to `#414141` but yielded a byte-identical placeholder JPEG on a second successful download (SHA-256 `7c58b3cc9098cb3e023392661eb66317532aace03e1a898aa91b3bc286efad58`).

The downloaded synthetic JPEG was then used as a local test photo. Upload opened the cropper, Use photo changed the summary to Photo Added, and Review's Download JPEG completed again. The third valid 1080×1920 JPEG differed from the placeholder and visibly placed the crop inside the photo window without changing the surrounding certificate template. This verifies the local headed-browser download for these two First Solo paths, not all templates or mobile Photos/share delivery.

## Crank & Core Rotax desktop learning and ignition states

At 1200px desktop width, the current Rotax collection opened the 912 ULS viewer. The Learning guide in Day and Night used a solid gray reading panel over the #E3E3/#414141 canvas, a gold selected Learn dock key, neutral playback/range/step controls and unchanged engine geometry. Opening Power-stroke order displayed gold selected cylinder 1/Power controls and retained phase-specific lines.

From Study, Compare ignition opened the Ignition Lab. Night showed gold Energy paths and Circuit ON states, gray Expose internals/Shared scale/Reset options, opaque comparison cards and the original reconstructed model colors. Shared scale then changed to gold selected; Next spark moved the status from 110° to 155° and updated the event text to cylinder 4. Day showed the same gold-selected controls and solid reading layers; scrolling inside the modal exposed the lower component-picker/explanation area without changing the diagram colors. In browser-emulated reduced motion, the Day lab's element/pseudo-element scan found no active CSS animation or transition above 10ms. This is bounded desktop visual and interaction evidence; no source change was warranted, and the remaining full lab/model matrix is still open.

## Blocked-storage appearance and stateful-app entry

In a fresh headed Chromium session, an initialization script made every `localStorage` access throw `SecurityError` before page scripts ran. With browser color-scheme emulated dark, the homepage resolved after reload to System/Night: `data-theme=system`, `data-gold-mode=dark`, body `rgb(65,65,65)` and live theme color `#414141`. Switching the emulated OS to light without reload changed the same page to System/Day, `rgb(227,227,227)`, theme color `#E3E3E3`, and a Day sky phase. The menu still offered Night, Day and System with System pressed. Clicking Night changed the current session to the Night material and pressed state; reload returned to System/Day because storage remained unavailable, as intended.

Under the same blocked-storage script, PilotSolve, Simply Endorsed, CertPath, FlightRisk, Aero Lab, Crank & Core, FOI Cards and Certificate Generator each returned HTTP 200, displayed content, resolved to System/Day with `#E3E3E3` browser theme color, and emitted no uncaught page errors during the entry check. PilotSolve explicitly displayed “Storage is unavailable. Export a backup before closing.” The homepage and all eight tools then loaded in emulated System/Night with body `#414141` and matching theme color. The only browser console warning was Plausible ignoring localhost. This verifies entry and shared appearance under simulated denial, not persistence of tool data, full app workflows, or a real OS settings change.

## Certificate Generator template export sweep

In a separate headed Chromium session, the Certificate Generator wizard was populated only with Test Pilot and Sample Instructor, no photo. After the earlier First Solo check, each of the remaining nine certificate choices was selected through the UI, reviewed, and downloaded from the preview in Day and Night. All 18 browser download events completed with null failure; all saved outputs decoded as valid 1080×1920 JPEGs. For each template, the Day and Night files were byte-identical: Private Pilot, Sport Pilot, Instrument, Commercial, Multi-Engine Rating Added, Multi-Engine Commercial, MEI Instructor, CFI and CFII. Switching appearance did not clear the populated review, and selecting a new template retained the synthetic names while updating the certificate and default instructor rating.

The nine exported placeholder artworks were visually opened: titles, synthetic names, photo placeholder and Kentucky Flight Training Center mark remained inside the composition. Their template-specific blue, green, red, gold and instrument-panel artwork stayed intact; the Aurum UI palette did not leak into the export. The two Multi-Engine variants produced different JPEG bytes, but both suggested the existing filename `MultiEngine_Test_Pilot.jpg` because the export routine derives names from their shared display title. That naming collision predates the theme and is outside this material pass. The generated local artifacts are ignored under `output/playwright/`; no production export art was edited. Installed-device Photos/share behavior, real user photos across all templates and phone delivery remain unverified.

## Simply Endorsed legacy guidance material retirement

The loaded `assets/tool-system/simply-endorsed.css` still contained fixed blue fills, blue focus/hover washes and blue fallbacks for the older guidance-mode buttons, table of contents links, AC reference keys, numbered steps and flashcard back. These are material colors, not the workspace's meaningful endorsement-category accents. Replaced them with shared Aurum gold for selected actions and raised/solid gray for secondary and reading surfaces. The current task-first workspace uses `workspace.js`, so its active guidance route does not expose those older mode controls; the edit retires a competing stylesheet state rather than claiming a changed active workflow.

After sync and cache refresh, the active First Solo → A.3 detail route reloaded with the new stylesheet URL. Day and Night screenshots showed gold Search, Guidance selection and Copy model text; solid gray reading/model-wording panels; and the retained orange Student pilot/A.3 category identity. The A.3 link opened its details and source/model text as before. A temporary computed-style probe of the loaded legacy selector resolved the active guidance-mode key to the same metallic gold gradient, bronze border and dark ink in both appearances; the older table-of-contents and flashcard selectors resolved to gray/gold rather than blue. This probe verifies cascade only, because those controls are not exposed by the current task-first route. System emulation was cleared and the isolated browser closed.

## Aero Lab selected lesson and teaching controls

In the live Aero Lab lesson view, the selected lesson and teaching tabs already had the gold fill and dark ink but lacked the bronze rim used by other selected controls. The first lesson key was also 37px high inside a 49px rail. A scoped shared-theme rule now draws an inset bronze rim without changing tab geometry, and gives each lesson key a 44px touch area while retaining the rail's 49px height. No lesson content, simulation data or diagrams changed.

After the theme URL and offline caches were refreshed, a headed browser confirmed the selected Free exploration key and Air motion teaching tab had the bronze inset edge in both System/Night and explicit Day. At 1024px, all five lesson keys measured 44px, the rail stayed 49px, and the document had no horizontal overflow. Clicking Find the stall moved the selected rim and changed the learning heading to “A stall begins with an angle”; clicking Pressure moved the teaching-tab selection and left the other tabs gray. At 834px the existing handoff screen appeared as designed. Browser-emulated reduced motion computed zero-duration transitions and animations on the selected key while retaining its edge. Native touch, OS motion settings, remaining lab states and full Aero Lab acceptance are still open.

## Service and local landing control consistency

The current isolated preview was checked on Discovery Flight, Flight Training, Ground School, FAA Written Test Prep, Checkride Oral Prep, Remote Ground Instruction, Private Pilot Ground School and Southern Indiana Flight Training. At 320px in browser-emulated System/Day and System/Night, each route resolved to the requested `#E3E3E3` or `#414141` canvas and had document scroll width exactly 320px. Across those route/appearance combinations, all visible `.btn--primary` controls used the same metallic gold gradient, bronze edge and dark ink; `.btn--secondary` controls used the corresponding raised gray treatment. No surveyed primary or secondary control extended beyond the viewport. The Discovery Flight desktop Day hero and Southern Indiana 390px and 320px Night hero were also visually opened; the Southern Indiana 320px Night contact form showed solid gray inputs and a gold Open email draft action.

This is a rendered material/width census of the named controls and a visual spot check, not a full-page review of every service route. No email draft was opened or sent, and no form data or page source was changed. Remaining content sections, keyboard/error states, native touch and enlarged text still require acceptance.

## Reading-template action and category distinction

A headed desktop Day check of the FAA Medical Certificates for Student Pilots Learn article showed its breadcrumb, reading intro and table-of-contents links. Clicking What the AME Checks changed the URL hash and scrolled the matching heading near the top of the viewport. At 320px in browser-emulated System/Day and System/Night, this Learn article and the Private Pilot Timeline Louisville blog article both kept document width at 320px. Their lower article CTAs resolved to the canonical metallic gold primary and raised gray secondary treatments in both appearances. The Learn article's TOC keys stayed gray; their blue dots and the breadcrumb dot come from its Private Pilot `--category-accent` and remain meaningful category identity, not an older blue action material. The blog article's 320px Night intro was visually inspected.

This covers two representative reading routes and their named actions, not all generated article variants or their entire long-form content. No aviation claims were evaluated or changed; keyboard, enlarged-text and remaining full-page visual states remain open.

## Full registered-route rendered control census and FOI state colors

The current isolated site preview was loaded in a headed Chromium session for every path in `config/site-pages.json`: 523 unique registered routes, with exact ordered path equality verified for each generated batch. In browser-emulated System/Night at 320×700, and again in System/Day at 1280×900, all 523 returned HTTP 200, resolved the requested `#414141` or `#E3E3E3` body canvas and browser theme color, and kept document width within the viewport. Every visible `.btn--primary` found by this census computed the shared reflective gold gradient, bronze `rgb(154,112,27)` edge and dark `rgb(43,35,22)` label. Each pass returned zero findings. The shared CSS was SHA-256 `3f7cded7e1fffc3cebfbfcb339482bc7290a1776b526addeae7022459993b74b`; the appearance script was `cdcd8e82f4190a831cf92a9c41097da4c74d3e057a920671c9fc5076e6006542`.

A separate 390px System/Night and System/Day pass loaded the same 523 paths and examined visible buttons, button-role elements, `.btn` links and submit inputs for saturated blue/cyan in their computed background or border values, including gradient color stops. No such button-like control was found in either pass. This is a diagnostic for old action materials, not proof that every control is gold or that category colors are absent: it does not inspect pseudo-elements, hidden menus, hover/focus/selected states, canvas/SVG art or later workflow screens. The per-batch scripts and FOI screenshots are local ignored artifacts under `output/playwright/`; this log records the durable scope and result. Structural route wiring and previously recorded template screenshots remain separate evidence.

FOI Cards was then opened through Start remaining cards and Flip card. In desktop Day, Review retained its amber outcome edge, Memorized its green outcome edge, and Next its gray secondary treatment; all three had the shared raised-control shadow. In 390px System/Night, the same distinctions remained, document width was 390px, and scrolling to the true bottom exposed both CTA controls and contact links above the fixed dock. Calculated text contrast on the solid rating buttons was 10.47:1, 13.94:1 and 9.18:1 in Day, and 5.89:1, 6.89:1 and 4.89:1 in Night for Review, Next and Memorized respectively. No rating was submitted, and this does not cover every FOI study state or native touch.

## Dynamic selected-control class restoration

A deeper Aero Lab browser pass found a shared-state defect hidden by entry screenshots. The selected Particle trails, Instantaneous streamlines and Chord & angle overlay controls had the gold fill and bronze edge, but inherited the older inset-underline shadow from the generic `.av-control[aria-pressed="true"]` rule. That rule now uses the same raised highlight/contact shadow as other selected hardware. More importantly, toggling Particle trails off and on made React rewrite the existing button's `class` attribute, removing the `av-control` class that `assets/tactile.js` had added on initial scan. The gold fill remained because of app-owned CSS, but class-specific tactile depth and press feedback became inconsistent after interaction.

The shared tactile enhancer now observes class changes on existing button-like elements and schedules an idempotent rescan only when an eligible control loses `av-control`; its ordinary-button native-control exclusion remains. A JSDOM regression test reproduces the class rewrite and verifies restoration without enhancing a native button. After current `assets/avionics.css?v=a3050f76e5` and `assets/tactile.js?v=2163887bfe` loaded, a headed browser toggled Particle trails off/on in Day and Night. The control returned to `overlay-toggle selected av-control` with the canonical gold gradient, bronze edge and raised shadow; selecting Pressure moved its bronze-rimmed teaching selection. Night's body stayed `#414141`, Day stayed `#E3E3E3`, and document width remained 1200px. Browser-emulated reduced motion retained the restored class and selected metal while computing 0s transition and animation. Day and Night overlay screenshots were visually opened; no simulation model or diagram source changed.

Shared Node tests passed 64/64, focused theme/cache Python tests 11/11, sync check found zero stale HTML sources, and the structural audit passed 523 registered routes, 6 supporting documents and 4 manifests. Shared sync rotated 545 HTML URLs and cache refresh rotated PilotSolve to `pilotsolve-site-193057ad4323` and Simply Endorsed to `simply-endorsed-tactile-863ee8705906`. A separate Simply Endorsed Night entry check loaded the new tactile URL and kept Search gold with dark ink. This verifies the named dynamic control path and a shared regression, not every app's later selected states or native touch.

## Crank & Core Lycoming model controls

The current isolated preview entered Lycoming IO-360-L2A through the collection UI and then opened its 3D model. At desktop System/Day, Study → Fuel injection showed the gold selected study key while Learn opened the continuous-flow port-injection guide on a solid reading surface. Fuel servo opened the RSA-5AD1 detail; its Zoom to part, Show only and Hide controls remained gray secondary actions. View → X-ray became a raised gold selected control with a bronze edge and retained `av-control` after the interaction. The model and propeller kept their original teaching colors.

At 390×844 and 320×700 in browser-emulated System/Night, the View panel stayed within the viewport, and X-ray plus Internal detail each retained a raised gold selected state when both were on. The same two selections remained gold after changing the emulated OS to Day, where the canvas was `#E3E3E3`; Night used `#414141`. At 320px the selected View keys were 44px high, the document width equaled 320px, and reduced-motion emulation computed 0s animation and transition on the selected control. Parts → Manuals moved selection to the Manuals key, and selecting 3D model moved it back. The modeled-part and manual search inputs accepted “servo”; the clear-search action stayed gray and 44px high. Reloading the direct Lycoming explore URL restored the app entry, selected model label and current asset URLs with System/Day theme color.

Visual evidence: `output/playwright/lycoming-fuel-view-night-390.png`, `lycoming-fuel-view-night-320.png`, `lycoming-fuel-view-day-320.png`, and `lycoming-parts-search-day-320.png`. These are bounded rendered checks of Lycoming's collection, teaching, View and Parts states; external manuals, full model-specific interactions, native touch, 200% zoom and live OS changes remain open. No source code, model data or artwork changed in this pass.

## FlightRisk IFR selected material and risk override

In a fresh headed local browser profile, the FlightRisk assessment's VFR flight-rules key was keyboard focused and computed a 3px gold outline. Its selected radio, selected Private Pilot card and Not rated key had the shared metallic gradient, bronze border and dark ink. Selecting Instrument (IFR) moved the gold treatment from VFR to IFR and changed the live risk readout to the red HIGH override, preserving the existing safety meaning. The instrument-rating warning text appeared; no profile was confirmed or assessment saved outside this browser profile.

At 320×700 in browser-emulated System/Night, the body and browser theme color were `#414141`; the document scroll width equaled 320px. The IFR, Private Pilot and Not rated selected controls retained their metallic background and dark ink. Their computed transition and animation durations were 0s with reduced motion. Changing the emulated OS to Day updated the body and theme color to `#E3E3E3` without losing selection or the red risk override. The certificate choices remained in the app's existing labeled horizontal swipe rail; the selected Private Pilot card was partly visible before swiping, as indicated by the on-screen swipe cue. No layout, calculation or risk-band source changed. Screenshots: `output/playwright/flightrisk-ifr-night-320.png` and `flightrisk-ifr-day-320.png`. Actual browser 200% zoom, remaining risk bands, full workflow and print delivery remain open.

## CertPath Logged Events stale teal retirement

The loaded `assets/tool-system/part61.css` still defined a separate teal material family for the Step 3 Logged Events header, action text, event edge, summary and completion count. That color had no category or result meaning, and the current redesign only hid some declarations by later override. The block now uses shared gray surface/line, gold accent/bronze edge and normal reading ink tokens. The green completed-status style in the separate redesign stylesheet remains unchanged.

A headed local browser loaded `part61.css?v=46fc1748ca`, advanced through Goal → No certificate yet → Logbook totals, opened Logged events and Private Pilot events, and checked one synthetic event. The count changed from 0/9 to 1/9. The Day desktop view kept opaque gray reading panels, quiet gold Clear events and gray event controls; the Night 320px view used body/theme color `#414141`, a light-gold action and checked checkbox, with document scroll width 320px. Screenshots are `output/playwright/certpath-events-day-desktop.png` and `certpath-events-night-320.png`. The browser profile was isolated from user data. Complete plan/contact-save workflow and actual 200% browser zoom remain open.

After the stylesheet edit, shared asset sync changed one HTML URL and standalone cache refresh produced `pilotsolve-site-193057ad4323` and `simply-endorsed-tactile-0d540044de9a`. The 523/523 route, 6/6 supporting-document and 4/4 manifest wiring audit passed; CertPath storage regression passed, 11 focused theme/cache tests passed, sync reported zero stale URLs, 109/109 shared contrast pairs passed and diff whitespace was clean.

## PilotSolve selected-navigation source cleanup

The isolated Aurum authoring source still contained two early blue selected bottom-nav gradients and a Pista-era blue/near-black fallback palette before its later gold layer. The two obsolete selected-nav rules were removed; the earlier app fallback variables now reference the shared `--g-*` Day/Night values, and an obsolete hard-coded blue primary declaration was removed. The final app-owned gold primary and selected rules, geometry, source logic and semantic green values remain. Regenerated `docs/design/source-patches/pilotsolve-aurum.patch` from the preserved tactile baseline; `patch --dry-run` applied all six file sections cleanly.

The rebuilt mobile package loads `prototype-Cv6bjZxl.css`, `prototype-CAVt1yWr.js` and `mobile-CUf-5Yi6.js`. Its CSS contains zero instances of the four targeted old blue literals (`#0864c8`, `#b5dcff`, `#2465d8`, `#3d7cf0`). The superseded CSS and two JS bundles were SHA-verified in `/Users/diegosuarez/Projects/suarez-aurum-archives/pilotsolve-fallback-palette-20260925/superseded-app-assets.tar.gz`, then retired from the active app. The archive registry and offline cache were refreshed; current PilotSolve cache is `pilotsolve-site-17a65a21ab78`.

In a fresh headed browser on the packaged `/pilotsolve/` route, System/Day showed `#E3E3E3` and the Tools key used the canonical metallic gradient, bronze edge and dark ink. At 320px, tapping Trip moved gold selection to Trip and changed the hash to `#trip`; the inactive Tools/Aircraft keys stayed gray. Emulated System/Night changed the canvas and browser theme color to `#414141` while retaining Trip's gold state and 320px document width. Reloading the direct `#trip` link loaded the new CSS and retained Trip; reduced-motion emulation computed 0s animation and transition. Screenshots: `output/playwright/pilotsolve-trip-day-320.png` and `pilotsolve-trip-night-320.png`. No calculation values or user records were entered in this browser profile.

Source checks: 28 protected runtime files passed, 36 app tests passed, TypeScript and production Vite build passed. Three source offline-worker and four shipped worker tests passed, plus 12 theme/cache/archive tests. Shared sync reported zero stale HTML, and the structural audit passed 523 registered routes, six supporting documents and four manifests. This retires a specific competing selected-control fallback; other source-level legacy declarations and full tool/zoom/native interaction review remain open.

## Aero Lab authored fallback retirement

The isolated Aero Lab source still declared a sage base, source-button and lesson-rail materials ahead of its later Aurum adapter. Those decorative fallbacks now use shared neutral surface, line and muted tokens. The authored active lesson key uses the shared metallic gold gradient, dark ink and raised hardware shadow. Diagram, flow-status and teaching colors remain intact. The web build passed TypeScript/Vite and all 14 source unit tests; the separate engine smoke script stopped after its initial checks because the isolated source and preserved baseline both lack `app/examples/flap.afoil`. No model or data file was changed.

The eight rebuilt assets replaced the old Aero Lab package on the isolated site, and `docs/design/source-patches/aero-lab-aurum.patch` dry-applied to all eight affected files against the preserved tactile baseline. Five superseded bundles were SHA-verified and archived at `/Users/diegosuarez/Projects/suarez-aurum-archives/aerolab-base-material-20260925/superseded-app-assets.tar.gz`; the active Aero Lab directory now contains eight assets and the stale-asset scanner reports zero retirement candidates.

In a headed browser on the packaged `/aero-lab/` route, selecting “01 · How lift works” updated the lesson heading and moved the metallic gold, bronze-rimmed selected state. Selecting Pressure moved the teaching control's gold state from Air motion to Pressure. System/Day computed body `#E3E3E3`; emulated System/Night computed `#414141`, with the selected lesson and Pressure controls retaining the shared gold gradient and dark ink. The gray Forces control stayed secondary. Reduced-motion emulation computed 0s transition and animation on the selected Pressure control. The Night viewport screenshot was visually inspected at `output/playwright/aero-lab-source-night-desktop.png`. Shared sync found zero stale HTML; the structural audit passed 523 routes, six supporting documents and four manifests; 12 theme/cache/archive tests passed. Remaining Aero Lab states, native touch, 200% zoom and installed-app export behavior still need review.

## FlightRisk authored material bridge retirement

The FlightRisk source still contained a light-only HSL bridge, cool-tinted component shadows and near-black body/nav fallbacks. Those decorative declarations have been removed or mapped to shared `--g-*` neutral and focus tokens. The separate low/moderate/high risk colors and warning red remain. A shared-token sidebar utility replaces its old HSL-specific border reference. The 17-file source patch, including an already-authored confirmation component absent from the previous patch, dry-applied to the preserved baseline. The source passed 27 unit tests and the TypeScript/Vite build.

The new ten-asset package is active in the isolated site. Its ten superseded bundles were hash-verified and archived outside the site at `/Users/diegosuarez/Projects/suarez-aurum-archives/flightrisk-base-material-20260925/superseded-app-assets.tar.gz`; the stale-asset scanner now reports zero candidates. Browser entry on the rebuilt assessment used `index-DJkT1Jkx.css` and System/Day body `#E3E3E3`; its selected VFR radio and Start assessment action had the metallic gold gradient and dark ink. At 320px System/Night, selecting IFR moved that treatment to IFR, kept the instrument-rating warning, and left document width at 320px with body `#414141`. The Guide at desktop System/Day showed gold primary actions, gray secondary action, and a focused primary link with a 3px gold outline. The Methodology at 320px System/Night showed its heading, gold primary actions and no horizontal overflow. All three routes emitted zero browser errors. Screenshots visually checked: `output/playwright/flightrisk-source-night-320.png` and `flightrisk-source-guide-day-desktop.png`.

The shared cache refresh completed, sync found zero stale HTML, the 523-route/six-document/four-manifest wiring audit passed, and 12 theme/cache/archive tests passed. This proves the named source and rendered paths, not all risk bands, saved-data flows, print delivery, native touch or actual 200% zoom.

## Simply Endorsed conditional control hardware

The active workspace stylesheet ended with a blanket `button {box-shadow:none}` override. In a live First Solo checklist, that left Reset and the unpressed/pressed Not applicable toggle flat, with the pressed toggle gray at 34px. The override is now scoped: ordinary actions inherit the shared contact shadow, while quiet Menu/back/category-launcher controls remain unraised. The pressed conditional toggle uses the canonical gold gradient, bronze edge and dark ink, and its touch height is 44px. The Copy model text primary control and selected appearance segment retain their raised gold treatment. Checklist completion green and category palettes were not changed.

The headed isolated preview opened First Solo, selected Not applicable for A.5, and observed the label change to “Applies after all” while the status read “Not applicable.” Selecting it again restored “Check applicability.” In System/Night and System/Day at 320px, the pressed control retained the gold gradient and dark ink; the page canvas computed `#414141` and `#E3E3E3` respectively, with document width 320px. Reduced-motion emulation computed 0s transition/animation, and the 320px Night/Day row screenshots were visually checked at `output/playwright/simply-endorsed-na-night-320.png` and `simply-endorsed-na-day-320.png`. Opening A.5 showed a raised gold Copy model text action; the menu showed System selected in gold. The browser profile was temporary and carried no user records.

The workspace stylesheet URL rotated to `workspace.css?v=bec68eaef6`. Its 1,678 workspace assertions, the broader 98-test app suite, 12 theme/cache/archive tests, zero-stale HTML sync and 523-route/six-document/four-manifest wiring audit passed. This checks the named conditional path and materials, not all checklist variants, native touch or actual 200% browser zoom.

## Tools directory filter and orbit hardware

The Tools directory's inactive purpose filters were transparent flat pills, and its Previous/Next orbit controls had flat gray fills. App-owned CSS now gives these controls the shared gray gradient, fine border and contact shadow; the selected filter retains the canonical metallic gold/bronze treatment. Removed the local blanket shadow reset for catalog buttons and the stale selected-filter rule that tried to suppress its depth. Orbit app icons remain transparent so their selected identity ring and app colors keep their meaning; geometry, ordering and navigation logic were unchanged.

The isolated directory loaded `tool-catalog.css?v=458ffd9904`. At desktop System/Day, inactive filters computed the raised gray gradient and contact shadow, while All tools and Open PilotSolve stayed gold. Selecting Aircraft systems moved the gold filter and featured Aero Lab; Next changed the stage to Crank & Core with “2 of 2” and kept the gray arrow hardware. At 390px System/Night, the canvas was `#414141`, selected filter gold, secondary filter gray, and document width equaled the viewport. At 320px System/Day, filters remained 44px high and the document fit 320px. A pointer-down on Flight planning computed a 2px travel, inset pressed shadow and 100ms transition, then returned to rest after release with Flight planning gold and selected. Reduced-motion emulation computed 0s animation/transition and no pressed travel. Desktop Day, 390px Night and 320px Day screenshots were visually opened at `output/playwright/tools-filter-hardware-day-desktop.png`, `tools-filter-hardware-night-390.png` and `tools-filter-hardware-day-320.png`.

Catalog interaction tests passed 8/8; focused theme/cache/archive tests passed 12/12; shared contrast passed 109/109 pairs with minimum 4.54:1; sync found zero stale HTML and the 523-route/six-document/four-manifest wiring audit passed. Remaining native touch, 200% zoom and full template interaction acceptance are still open.

## Certificate Generator control depth

The shared premium stylesheet grouped Certificate Generator action controls with its static preview card and removed every shadow in that group. This flattened unselected certificate choices and secondary actions despite the loaded shared tactile layer. The control-level shadow reset has been removed; the preview card keeps a flat, opaque reading surface. The Generator's app layout, template artwork and export code were not changed.

The isolated route loaded `premium.css?v=921e60c2d4`. At desktop System/Day, unselected certificate choices computed the shared inset/contact shadow; First Solo selection remained metallic gold with dark ink, and the share/secondary control gained gray hardware depth. The visible certificate artwork retained its original First Solo illustration. At 320px System/Night, the canvas was `#414141` with document width 320px; unselected choices and secondary actions retained raised gray depth beside the gold selected choice. Choosing CFI, continuing to Names and then selecting CFII moved the gold rating selection while the other rating keys remained gray hardware. Reduced-motion emulation computed 0s animation/transition on the selected rating key. Day desktop, Night 320px selection and Night 320px rating screenshots were visually checked at `output/playwright/certgen-control-hardware-day-desktop.png`, `certgen-control-hardware-night-320.png` and `certgen-rating-hardware-night-320.png`. The browser used synthetic selection only and did not create an export.

Shared sync updated 529 HTML asset URLs, including the Generator, then cache refresh completed. The 523-route/six-document/four-manifest wiring audit, 12 theme/cache/archive tests, 109 contrast pairs, JavaScript syntax check and diff whitespace check passed. `certificate-generator/js` and `certificate-generator/templates` had no worktree changes. This checks the named controls, not every template/viewport combination or installed-device share delivery.

## FOI shared-menu action materials

FOI's reflowed shared-menu actions were inheriting the generic transparent menu-row style. Removed that stale CTA flattening rule and the obsolete premium selector for the old chipnav sheet. The app-owned CTA now uses the canonical gold fill, dark ink, bronze edge and contact shadow; Reset study progress uses raised gray hardware. The menu retains its existing action order and 44px rows.

In the isolated `8898` preview, desktop System/Day computed the metallic gradient and bronze edge on Plan Training and a gray gradient/contact shadow on Reset study progress. The Day screenshot was visually inspected at `output/playwright/foi-actions-day-desktop.png`. At 390px System/Night, both controls were 44px tall, fit the 326px action region, and retained those materials; `output/playwright/foi-actions-night-phone.png` was visually inspected. At 320px, both actions and the related-tool rows remained 44px tall inside the 256px action region, with document width exactly 320px. Opening Reset study progress closed the menu and focused Keep progress; Escape closed the confirmation without clearing progress. Reduced-motion emulation computed 0s transition and animation with no travel on both menu actions. No reset confirmation was submitted in this browser profile.

The FOI dialog/navigation Node tests passed 13/13, theme/cache/archive Python tests passed 12/12, contrast audit passed 109/109 pairs with 4.54:1 minimum, sync refreshed 545 HTML URLs, cache refresh completed, the 523-route/six-document/four-manifest audit passed, and diff whitespace was clean. This checks the named menu states, not enlarged text, native touch or every FOI interaction.

## Crank & Core ignition lab toggle review

The isolated Crank & Core Rotax route opened Study → Compare ignition in a headed browser. The lab's Day desktop and Night desktop views kept the solid gray reading shell, raised gray secondary controls and metallic gold selected Energy paths/Circuit ON controls. Its two 3D teaching canvases rendered their components after settling in both appearances. At 390px System/Night the dialog fit within the viewport, the document width stayed 390px, and the visible selected controls retained gold with dark ink; the model colors remained independent of the UI material. Selecting Shared scale moved that control into the same gold state while leaving Energy paths selected. Grounding the CDI circuit changed its label to GROUNDED while the other circuit stayed gold/ON; the grounded control remained readable gray hardware. At 390px System/Day, the selected controls stayed gold and the document again fit 390px. Reduced-motion emulation computed 0s transition/animation and no transform on the selected Energy paths control.

Screenshots visually inspected: `output/playwright/crank-ignition-lab-day-before.png`, `crank-ignition-lab-night-after.png`, `crank-ignition-lab-night-390.png` and `crank-ignition-lab-day-390.png`. This was a rendered-state coverage pass with no source edit. Full lab interaction, model-specific variants, native touch and actual enlarged-text/zoom behavior remain open.

## Browser zoom evidence boundary

In a separate headed homepage browser, Meta+plus left `innerWidth=1200`, `devicePixelRatio=2` and `visualViewport.scale=1` unchanged. A temporary CSS `zoom:200%` probe produced a screenshot but kept the layout viewport at 1200px, so it was discarded as proof of actual 200% browser zoom. The available native-app UI was locked and could not set browser zoom through Chrome controls. The actual-zoom acceptance gate remains open; no page CSS was changed from this inconclusive probe.

## CertPath ledger filter selection and orphan tab material

The current CertPath result uses four disclosures; it renders no `.part61-results-tabs` row. Its four `.part61-results-tabpanel` content containers remain active and were preserved. Removed the orphan result-tab row material from the base/redesign/shared styles and the inline font selector, including the old per-tab green/amber/purple swatches. The active ledger filter had `aria-pressed=true` but computed the same raised gray material as every inactive filter. The app-owned redesign stylesheet now gives only the pressed filter the canonical metallic gold fill, bronze edge, dark ink and contact shadow.

A fresh headed browser chose the default Private Pilot goal, No certificate yet, and the explicit no-flight-time shortcut. The test tab disconnected `window.CertPathStorage` immediately before Build my plan, so the existing calculation rendered locally without a contact submission or API request. Before the edit, All Rows was pressed but visually gray like the other filters. After a real reload loaded `part61.css?v=99fa983b06` and `part61-redesign.css?v=2ae0c4394c`, All Rows computed gold while the four other 44px filters remained gray. Clicking Remaining Only moved both `aria-pressed` and the gold material to that filter; the ledger rendered 12 rows. At 390px System/Night, the selected filter remained gold and the document width equaled 390px. At 320px Night, the filter remained 44px tall, width fit the viewport, and reduced-motion emulation computed 0s transition/animation. The result still contained all four active disclosure panels and zero orphan tab rows. Day before/after and Night phone screenshots were visually inspected at `output/playwright/certpath-ledger-filter-before-day.png`, `certpath-ledger-filter-after-day.png` and `certpath-ledger-filter-night-390-visible.png`.

CertPath storage tests passed, including the normal contact-gated save path under mocked requests; Simply Endorsed workspace tests passed 1,678 assertions. Theme/cache/archive tests passed 12/12, contrast 109/109 pairs with 4.54:1 minimum, shared sync and cache refresh completed, the 523-route/six-document/four-manifest audit passed, and diff whitespace was clean. The normal live API save, full calculator workflow, native touch and actual browser zoom remain unverified.

## CertPath multi-goal stage hardware

An initial selected-control sweep of ten active app entries found the expected gold states on Tools, Aero Lab and Crank & Core. The selected orbit app icon remains transparent by design so its app identity ring can communicate selection. Several entries expose their selected state only after interaction, so this sweep was used to choose a deeper path, not to certify those apps.

On CertPath Goal, adding an Instrument Rating after the default Private Pilot goal exposed live Earlier/Later/Remove controls with transparent fill and 40px height. The app-owned stage-action rule now uses the same raised gray gradient, ink, border and contact shadow as other secondary hardware, with a 44px minimum target. A fresh reload loaded `part61-redesign.css?v=081687a289`; the four enabled controls computed the gray gradient and 44px height at 320px System/Day. Clicking Later swapped the goal order from Private → Instrument to Instrument → Private, with no document overflow. At 320px System/Night the page canvas was `#414141`, buttons used the lighter graphite gradient with light ink, and document width equaled 320px. Reduced-motion emulation computed 0s animation/transition and no travel. Day and Night phone screenshots were visually checked at `output/playwright/certpath-stage-actions-after-day-320-visible.png` and `certpath-stage-actions-after-night-320.png`.

The rule changed only the stage-action surface and touch height. The CertPath storage tests, 12 theme/cache/archive tests, 109 contrast pairs, sync/cache refresh, 523-route/six-document/four-manifest wiring audit and diff whitespace check passed. Further multi-goal calculation, native touch and actual browser zoom remain open.

## Aero Lab Geometry Studio launch material

The main Aero Lab Aircraft & atmosphere panel still presented Geometry studio as raised gray secondary hardware, while the editor's Save action was already gold. The isolated source now gives the launch button the shared gold-primary class and a token-backed gold fallback; its brief loading overlay uses neutral gray and ink tokens instead of sage. A second source pass replaced the old sage lesson-card, prompt, badge, Recover button, segmented-control and supporting-copy fallbacks with shared gray/gold tokens. The bank-force diagram colors and editor geometry were untouched.

After rebuilding and packaging the app, a headed browser opened Aircraft & atmosphere on the current `/aero-lab/` route. In Day, the launch computed the canonical metallic gradient, bronze `rgb(154,112,27)` border, dark `rgb(43,35,22)` ink, contact shadow and 44px height over the `#E3E3E3` canvas. Night retained the same readable gold hardware over `#414141`. Element screenshots were visually inspected at `output/playwright/aero-studio-launch-day.png` and `aero-studio-launch-night.png`. Clicking the launch opened Geometry Studio; Save .afoil was still gold and Open/Export SVG remained secondary. System was restored and the temporary browser closed.

The rebuilt lesson 02 “Find the stall” view showed neutral prompt text `rgb(89,89,89)`, a bronze book icon, a gray 44px Recover control and a solid gray card over the Day canvas. Clicking Recover moved modeled angle of attack to 3.0°. In Night, the prompt computed `rgb(208,208,208)`, the card `rgb(74,74,74)` and the canvas `#414141`; Recover stayed 44px and gray. The Day/Night lesson cards were visually inspected at `output/playwright/aero-stall-lesson-day.png` and `aero-stall-lesson-night.png`. This is a named lesson state, not a full scientific-content review.

The Aero Lab source passed 14 unit tests and TypeScript/Vite build after the final source pass. All eight packaged assets match `dist/assets` by SHA-256, and the nine-file Aurum source patch dry-applies to the preserved baseline. Two five-file superseded-bundle sets were hash-archived outside the site at `aerolab-studio-launch-20260925` and `aerolab-lesson-material-20260925`; the stale-asset scanner now reports zero. Shared sync updated no HTML, cache refresh completed, 523 routes/six supporting documents/four manifests passed, 12 theme/cache/archive tests and 109 contrast pairs passed, and diff whitespace was clean. This verifies the named launch and lesson paths, not the loading overlay's transient render, full Studio/lesson workflows, native touch or actual 200% zoom.

## FlightRisk answered-row Night contrast

The live assessment's answered risk-factor row still used a translucent red CSS fill. In Night, that blended with the surrounding gray to an observed `rgb(152,143,141)` while its label stayed `rgb(244,244,244)`, yielding only 2.87:1 contrast. The isolated FlightRisk source now uses its existing solid `bg-risk-high-tint` and `bg-risk-low-tint` classes for answered risk and mitigation rows. Unanswered and answered “No” rows keep the neutral solid surface; red/green tracks, edges and point badges retain their operational meaning. No scoring, answer, persistence, geometry or print logic changed.

After rebuilding the published-base package, a headed browser reloaded the saved synthetic answer. Night risk “Yes” computed a solid `rgb(61,40,40)` row with `rgb(244,244,244)` label (12.46:1); Night mitigation “Yes” used `rgb(32,56,43)` (11.49:1). Day risk and mitigation rows computed `rgb(247,226,222)` and `rgb(226,241,231)` with `rgb(39,39,39)` labels (12.01:1 and 12.78:1). At 320px Day/Night, the answered row fit a 254px region and the document width equaled 320px. Clicking the risk switch to “No” restored the gray row and cleared its checked state. Before/after Night, green mitigation, Day and 320px screenshots were visually inspected at `output/playwright/flightrisk-risk-toggle-night-before.png`, `flightrisk-risk-toggle-night-after.png`, `flightrisk-mitigator-night.png`, `flightrisk-risk-toggle-day-after.png`, `flightrisk-risk-toggle-day-320.png` and `flightrisk-risk-toggle-night-320.png`. This browser used a temporary local profile; System was restored and the session closed.

FlightRisk passed 27 source tests and its TypeScript/Vite build. Its ten packaged assets match the local `dist/assets` bytes; the expanded 18-file Aurum source patch dry-applies to the preserved baseline. Ten superseded bundles were hash-archived outside the site at `flightrisk-solid-risk-20260925`, with zero stale assets remaining. Shared sync updated no HTML, cache refresh completed, 523 routes/six supporting documents/four manifests passed, 12 theme/cache/archive tests and 109 canonical contrast pairs passed, and diff whitespace was clean. This closes the named answered-row contrast defect, not the remaining risk-band workflow, actual 200% zoom, native touch or print delivery.

## Aero Lab authored neutral header and intro fallback

A source audit of the active Aero Lab stylesheet found fixed sage values on the header's neutral border/divider/version treatment and muted intro labels. The app's later rules already overrode some of these in current screens, but the authored fallbacks could diverge in alternate states. Eight neutral declarations now resolve through `--g-line` or `--g-muted`; the green identity and all airflow/instrument/reference colors were preserved. The source passed 14 unit tests and `npm run build:web`. The nine-file Aurum patch was regenerated and dry-run applied to the preserved tactile baseline; the final eight published assets match `dist/assets` by SHA-256. Five superseded generated files were hash-verified in `/Users/diegosuarez/Projects/suarez-aurum-sources/_archives/aerolab-neutral-fallback-20260925/`, and the stale-asset dry run returned zero.

After a real reload, the isolated `/aero-lab/` route loaded `index-CdFETstd.js` and `index-cLgXDRWe.css`. System/Night computed body `rgb(65,65,65)`, eyebrow `rgb(208,208,208)` and intro text `rgb(244,244,244)`; System/Day computed body `rgb(227,227,227)`, eyebrow `rgb(89,89,89)` and intro text `rgb(39,39,39)`. Desktop viewport screenshots were visually inspected at `output/playwright/aerolab-neutral-source-day.png` and `aerolab-neutral-source-night.png`. The gold selected Free exploration/Air motion buttons, gray secondary controls and scientific tunnel remained visible. A compressed viewport screenshot made the selected labels look faint, but a direct button screenshot and computed style confirmed dark text on gold; no defect was introduced. The temporary browser was closed. Shared sync found zero stale HTML, cache keys remained current, and the 523-route/six-document/four-manifest audit, 22 Python tests, 109 contrast pairs and diff whitespace check passed. This is a source-retirement and entry-screen verification, not full app acceptance.

## CertPath neutral Paperwork tag source check

The Part 61 neutral status selector grouped Paperwork with dashboard slate and other non-semantic tags, but fixed `#f1f5f9` fill and `#475569` text under both appearances. The editable shared stylesheet now uses `var(--g-raised)` and `var(--g-ink)` for this group; green/amber/red statuses and blue/purple/teal requirement-category tokens were left intact. The live local CertPath entry loaded the new `part61.css?v=6d377deb86`. A fresh synthetic zero-hour Private Pilot plan was built with `window.CertPathStorage` disconnected immediately before Build my plan, so the result rendered without contact submission or API save. Expanding Requirements & missing events exposed the Paperwork legend. Through the actual menu appearance control, its visible selector computed Night `rgb(244,244,244)` text on `rgb(84,84,84)` and Day `rgb(39,39,39)` on `rgb(247,247,247)`. At 320px Night, the seven legend tags wrapped into a 284px-wide area, document width equaled the 320px viewport, and Paperwork remained visible. Day, Night and 320px Night screenshots were visually inspected at `output/playwright/certpath-paperwork-legend-day.png`, `certpath-paperwork-legend-night.png` and `certpath-paperwork-legend-night-320.png`. A separate built-in sample action moved to the expected contact gate; no personal information was entered or submitted. This verifies the result legend, not the complete saved-results flow.

The browser preference was returned to System and both temporary sessions were closed. Shared sync updated one HTML stylesheet URL; cache refresh rotated the PilotSolve and Simply Endorsed worker keys. The 523 registered routes, six supporting documents and four manifests passed the theme-wiring audit; 22 Python tests and 109 canonical contrast pairs passed. Live save, complete results workflow, native touch and actual browser zoom remain open.

## Simply Endorsed orphan mobile launcher retirement

The active `/simply-endorsed-cfi/` shell loads `workspace.js` and `workspace-model.js`; no active HTML references `simply-endorsed/js/app.js`, the only script that mentions `browseLauncher`. The current shell and workspace scripts have no launcher element or class. Removed the loaded stylesheet's complete `browse-launcher` rule family, including its old blue-white mobile background, and two orphan selectors from the page's inline typography rule. Existing `.rail-drawer-header`, content-area layout selectors and responsive breakpoints were retained. The pre-Aurum 53-source archive was copied from the old volume to `/Users/diegosuarez/Projects/suarez-aurum-sources/_archives/style-baseline-20260924/`; its tarball and manifest match the originals by SHA-256, and every tar entry matches the manifest. `docs/design/style-baseline-mirror.json` records the verification. The archive remains outside deployment.

The isolated page loaded `simply-endorsed.css?v=fa530b932e` after sync. At 390px System/Day, Browse categories opened the current dialog, Student pilot expanded, and First Solo navigated to `?view=library&category=student-pilot&subcategory=first-solo`. At 390px System/Night, the First Solo heading, Search gold primary, selected Library dock key, solid gray endorsement cards and orange category badges remained visible. Document width equaled 390px in both appearances, and no `.browse-launcher` element existed. Day/Night screenshots were visually inspected at `output/playwright/simply-endorse-launcher-retired-day-390.png` and `simply-endorse-launcher-retired-night-390.png`; the temporary browser was closed. The workspace suite passed 1,678 assertions; 22 Python theme/cache/archive tests, 109 contrast pairs, zero-stale sync, 523 routes plus six support documents/four manifests, and diff whitespace passed. This is bounded current-workspace evidence, not a claim that the old unused `app.js` is functional or that all guided workflows are accepted.

## Simply Endorsed orphan guidance card material retirement

The active Simply Endorsed shell does not load the older `simply-endorsed/js/app.js`. Only that unloaded file refers to `.privileges-card`, `.journey-node` and `.scenario-card`; the current HTML, `workspace.js`, `workspace-model.js` and guidance-content script do not. Removed the complete Privileges & Limitations card block and old Student Journey/Scenarios card blocks from the loaded stylesheet: 644 CSS lines in total, including translucent white card fills and old responsive variants. Kept the current accordion, guidance link and quick-reference rules. The original stylesheet is preserved in the verified 53-source pre-Aurum archive, so the retired styling is recoverable outside deployment. No FAA content, checklist state logic or route data changed.

After sync, the live isolated Guidance page loaded `simply-endorsed.css?v=8a8110376d`, and the browser parsed 491 rules from it. Guidance home linked to the current Student Journey list, First Solo detail, Scenarios list and failed-area scenario detail. A direct First Solo task link also opened. None of these screens contained the retired classes. At 390px System/Night the scenario detail used the `#414141` canvas and gold selected Guidance dock key; at 390px System/Day the First Solo journey detail used solid reading material and the `#E3E3E3` canvas. Both had document width 390px. The Night scenario and Day journey screenshots were visually inspected at `output/playwright/simply-legacy-retired-scenario-night-390.png` and `simply-legacy-retired-journey-day-390.png`; the temporary browser was closed. This validates representative current routes, not the entire guidance corpus or its aviation claims.

The Simply Endorsed workspace suite passed 1,678 assertions. Shared Python theme/cache/archive tests passed 22/22, canonical contrast 109/109, sync found zero stale HTML, route wiring passed 523 registered routes plus six support documents/four manifests, and diff whitespace was clean. The worker cache rotated to `simply-endorsed-tactile-49ba0ea2ea17`. Remaining legacy CSS, full guided interactions, native touch and actual browser zoom remain open.

## Simply Endorsed old teaching-mode stylesheet retirement

After the prior orphan card removal, the loaded CSS still contained a 1,208-line Teaching & Guidance View block with accordion, training requirement, Quick Reference, CFI Career, flashcard and other old `app.js` presentation rules. Static comparison found 101 CSS class tokens in the block, with only two textual matches among the actual loaded HTML/scripts: `.guidance-view` appeared only in the page's orphan inline font selector, and `.cfr-link` appeared within `.requirement-ref .cfr-link`, but the required `.requirement-ref` parent is never emitted by current `workspace.js`. The live page loads `workspace.js`, not legacy `app.js`. Removed that old block, its duplicate mode/sidebar overrides and the inline orphan selector. Current SEO reference anchors themselves were retained. The archived original stylesheet and 53-source hash manifest remain outside the deployable site. No guidance wording, FAA references, task data or checklist logic changed.

The isolated live page loaded `simply-endorsed.css?v=1ceb30ef8a` and parsed 315 CSSOM rules. Day desktop Guidance home showed six current cards, a gold selected Guidance navigation key and solid reading surfaces. At 390px System/Night, First Solo detail, Quick Reference and CFI Career opened through direct links on the `#414141` canvas, with document width exactly 390px. Current SEO reference links still computed gold `rgb(236,211,155)` in Night. Screenshots were visually inspected at `output/playwright/simply-guidance-legacy-retired-home-day-1200.png`, `simply-guidance-legacy-retired-first-solo-night-390.png` and `simply-guidance-legacy-retired-reference-night-390.png`. The test browser was closed. The workspace suite passed 1,678 assertions, shared Python tests 22/22, canonical contrast 109/109, sync zero stale HTML, 523 routes/six supporting documents/four manifests, and whitespace checks. These observations verify the current route shells and selected content states, not every guidance topic, aviation claim, native touch or actual browser zoom.

## PilotSolve legacy blue-glass material retirement

The published mobile source still declared old blue gradients for primary/equals/selected segments, blue-glass result panels, blue dock-selection layers, and blue-tinted hover/pressed shadows ahead of its final Aurum rules. These decorative declarations are now gold or neutral token-backed fallbacks; the obsolete blue dock-selection rules were removed. The only remaining blue literals in the app-owned stylesheet are the two `--diagram-vector` values used by the wind diagram. The protected phone/runtime files, calculator code and result formats were unchanged.

The rebuilt published package loads `prototype-DjmZwi7j.css`, `prototype-BTCi2k1e.js` and `mobile-CzLIGwDK.js`. Nine targeted old blue literals have zero occurrences in that CSS. At 390px System/Day, the live Crosswind screen showed a solid gray result panel and gray 44px keypad keys, with a gold/dark-ink Done key and bronze active input edge; its body was `#E3E3E3`. Entering `12` for wind speed updated the required-input count, and Next advanced the keypad to wind direction. Emulated System/Night changed the body to `#414141`, result panel to solid `rgb(74,74,74)` and inactive keys to graphite, while retaining gold Done and a 390px document width. In reduced-motion emulation, Done and its glint computed zero-duration transition/animation and no travel. Day/Night Crosswind screenshots were visually inspected at `output/playwright/pilotsolve-blue-retired-day-keypad-390.png` and `pilotsolve-blue-retired-night-keypad-390.png`.

The Calculator utility then showed gold selected Numbers and equals controls beside gray 57px numeric keys; the local `1+2=` sequence returned `3`. A Night 390px screenshot was visually inspected at `output/playwright/pilotsolve-blue-retired-night-calculator-390.png`. Temporary browser profiles were closed with System preference intact; no saved result or user data was created.

PilotSolve passed its 28-protected-file runtime guard, 36 source tests, TypeScript/Vite/offline build, three source-worker tests with the isolated site root specified, and four shipped worker tests. The five published app assets match the local `dist/client/assets` bytes; the six-file Aurum source patch dry-applies to the preserved baseline. Three superseded bundles were hash-archived outside the site at `pilotsolve-blue-glass-retirement-20260925`, with zero stale assets remaining. Shared sync updated no HTML, cache refresh rotated the PilotSolve worker to `pilotsolve-site-a988751a4294`, the 523-route/six-document/four-manifest audit passed, 12 theme/cache/archive tests and 109 contrast pairs passed, and diff whitespace was clean. This retires the named decorative blue layer and verifies the sampled calculator states, not every helper, backup, native touch or actual 200% zoom.

## Simply Endorsed final legacy module and 640px reflow probe

The in-app browser and headed Playwright browser ignored Command-plus/Command-equals: `innerWidth`, device scale and visual viewport scale stayed unchanged. The Mac was locked, so live OS appearance and native browser zoom controls were unavailable. A 640px CSS viewport was therefore checked as separate narrow-layout evidence, **not** as actual 200% zoom acceptance. Day home, tools, Guidance, FlightRisk, FOI Cards and PilotSolve, plus Night CertPath, each had document width equal to 640px. Visual screenshots at `output/playwright/tools-640-day-reflow-probe.png`, `pilotsolve-640-day-reflow-probe.png`, `flightrisk-640-day-reflow-probe.png` and `certpath-640-night-reflow-probe.png` showed current gray/gold materials and no evident clipped controls. CertPath was checked at its actual `/part-61-calculator/` path; a mistaken `/certpath/` probe returned 404 and was excluded from acceptance.

CSSOM selector matching on the live current Guidance screen found only `.icon-burger`, `.icon-close`, `.footer-guidance-link` and `#sidebarToggleBtn` from the 315-rule loaded `simply-endorsed.css`; the first, second and fourth were in the hidden legacy header. Temporarily removing that stylesheet changed only the visible footer Guidance link and its parent styles. The 49,456-byte stylesheet was archived at `/Users/diegosuarez/Projects/suarez-aurum-sources/_archives/style-baseline-20260924/simply-endorsed-legacy-before-final-retirement-20260925.css`, SHA-256 `1ceb30ef8adce8e5d8c1ec821c0d6de42f6b821f5a87c181d468258ad1e3ea42`, with a JSON manifest; its original pre-Aurum version is also in the verified 53-source archive. The loaded file now has four CSSOM rules retaining the footer mobile display and legacy header behavior. At 640px Day, `output/playwright/simply-guidance-640-day-before-legacy-retirement.png` and `simply-guidance-640-day-after-legacy-retirement.png` were pixel-identical (Pillow image difference bounding box `None`). At 320px Night, direct `?view=tasks&task=first-solo` showed First Solo on `#414141` with width 320/320; Browse categories opened the existing dialog with 44px close control and Student pilot category colors; screenshots are `output/playwright/simply-first-solo-320-night-after-legacy-retirement.png` and `simply-category-dialog-320-night-after-legacy-retirement.png`. Current stylesheet URL is `simply-endorsed.css?v=78c7d20c64`, and the footer Guidance action still computes `inline-flex` on mobile. Workspace tests passed 1,678 assertions, offline cache tests 2/2, Python theme/cache/archive tests 16/16, canonical contrast 109/109, and the route/supporting/manifest audit 523+6+4. Full workflow, native touch, OS setting and actual zoom gates remain open.

## CertPath completed-field badge in actual Experience flow

Before the correction, a temporary `.complete` state on `#part61HoursCompleteness` computed gray on gray in both appearances: Night `rgb(244,244,244)` on `rgb(74,74,74)` and Day `rgb(39,39,39)` on `rgb(239,239,239)`. The loaded shared neutral adapter matched `.group-completion` broadly and overrode the earlier fixed light-green completion rule. The app-owned completed selector now has greater specificity and resolves to `var(--av-green)` on solid `var(--g-raised)` with an inset semantic rim. Its uncompleted state remains neutral.

In a fresh headed browser, Start your path → Private Pilot goal → No certificate → Logbook totals → Set remaining blanks to 0 changed the Core time badge from 0/4 to 4/4 without bypassing calculator logic. Day computed `rgb(49,104,35)` on `rgb(247,247,247)` (6.25:1); Night computed `rgb(161,229,131)` on `rgb(84,84,84)` (5.07:1). The badge remained 44×28px in both modes, and at 320px its right edge stayed inside the viewport with document width 320/320. Screenshots were visually inspected at `output/playwright/certpath-complete-core-night-desktop.png`, `certpath-complete-core-night-320.png` and `certpath-complete-core-day-320.png`. The adjacent Build my plan button remained metallic gold; Back and the experience choices retained raised gray shadows; Set remaining blanks to 0 and Next group remained quiet text actions. Test data contained only zero values in the temporary local browser; no contact information or API save was used. This validates one real completion state and material hierarchy, not the full saved plan or every field group. The Part 61 wizard suite passed 98/98, CertPath storage regression passed, Python theme/cache/archive checks passed 16/16, shared contrast checks passed 109/109, sync found zero stale HTML, the 523+6+4 route/supporting/manifest audit passed and Git whitespace checks were clean.

## Shared tool core obsolete topbar retirement

A Night 390px first-frame census of Simply Endorsed, CertPath, FlightRisk, PilotSolve, Aero Lab, Crank & Core, FOI Cards and Certificate Generator found the expected primary/selected gold, secondary raised gray and quiet navigation/text actions; the sample did not include deeper states or every route. CSSOM selector matching then found 214 recursive rules in `tool-core.css`, but only 35 matched the current Simply Endorsed entry, 67 the CertPath entry and 28 the Certificate Generator entry at desktop; the `.topbar-*` family did not match any current screen. Static search found no current header markup: only `shared-utils.js` queries `.topbar-sticky` and tolerates its absence. The original current 32,184-byte core is saved outside deployment at `/Users/diegosuarez/Projects/suarez-aurum-sources/_archives/style-baseline-20260924/tool-core-before-topbar-retirement-20260925.css` with SHA-256 `8a4f0570af590a587963af4336b633f0a4fb3d9ee435f8bf60e690304dd8ae18` and a JSON manifest. All 85 pure `.topbar-*` rules plus mixed-selector references were retired; the current stylesheet is 21,674 bytes, has no `.topbar-*` names, and parses to 113 top-level CSSOM rules instead of 152.

The first after-edit browser still loaded the old `8a4f0570af` stylesheet from an active service worker, so its apparent match was discarded. A fresh profile loaded the final `tool-core.css?v=9a6c86ec5c`. At 390px emulated Night with reduced motion, a FNV hash over 12 computed material/layout properties on every visible element was unchanged for all three entries: Simply Endorsed 1,621 elements / `a9a3d5cb`, CertPath 73 / `6a2380d5`, Certificate Generator 145 / `389bd46`; each document width remained 390px. A 1280px Day comparison also showed unchanged visible signatures, but the old profile had already received the new CSS for Simply Endorsed and CertPath, so that pair is only a current-state check for those two. Direct First Solo entry still rendered its gold selected Tasks dock; the CertPath Private goal and Certificate Generator First Solo radio became gold after clicks. The final Night screenshots were inspected at `output/playwright/simply-first-solo-night-after-tool-core-topbar-retirement.png` and `certpath-intro-night-after-tool-core-topbar-retirement.png`. The workspace suite passed 1,678 assertions, navigation/offline cache tests 12/12, Python theme/cache/archive tests 16/16 and contrast 109/109. Sync refreshed four HTML URLs, the Simply Endorsed worker cache rotated to `simply-endorsed-tactile-7fc0a499e57e`, the 523+6+4 wiring audit passed and Git diff had no whitespace errors. This retires a stale shared header family; it does not prove every dynamic tool state or actual browser zoom.

## FlightRisk completed VFR risk-band walkthrough

The isolated preview at `http://127.0.0.1:8898/flight-risk-assessment/` was opened in a fresh headed Playwright profile. A Private Pilot VFR profile was confirmed. Synthetic experience values were total 120, type 20, PIC 80, last 30 days 1, last 90 days 10, and night 8 hours, producing three auto-flags (+9). The aircraft airworthiness concern was answered Yes (+5), and each of the six section completion controls filled only its unanswered items with No. The resulting final 14 score was MODERATE. The dialog displayed the amber gauge, status, action panel and breakdown on solid surfaces with a metallic gold Print summary button and gray Back to edit/New assessment controls. The Day/Night desktop and 320px phone images were visually reviewed: `output/playwright/flightrisk-complete-moderate-day-desktop.png`, `flightrisk-complete-moderate-night-desktop.png`, `flightrisk-complete-moderate-day-phone.png` and `flightrisk-complete-moderate-night-phone.png`.

On the same saved example, answering two more aircraft hazards Yes raised the final score to 24/HIGH; the red status and action panel stayed semantically distinct from gold controls at 320px in Day and Night. Images: `output/playwright/flightrisk-complete-high-day-phone.png` and `flightrisk-complete-high-night-phone.png`. Answering those three hazards No and increasing type/recent hours to 60/5/25 removed the auto-flags and yielded 0/LOW. The green status and action panel were inspected at 320px Day and 1280px Night: `output/playwright/flightrisk-complete-low-day-phone.png` and `flightrisk-complete-low-night-desktop.png`. Night LOW status and panel title computed `rgb(139,214,163)` on solid `rgb(32,56,43)`, 7.37:1. At 320px, document width equaled viewport width; dialog bounds were 0–320px and footer buttons were 44px high with bounds 16–156, 164–304 and 16–304px.

Reload restored all six reviewed sections, the confirmed profile, the synthetic hour values and the completed LOW score. The existing banner nevertheless says “Your unfinished assessment was restored from this device.” That copy mismatch is a follow-up outside the wording-preservation scope. No theme or scoring source was changed in this walkthrough. FlightRisk Vitest passed 27/27; the current site passed 109/109 canonical contrast pairs and the 523-route/six-supporting-document/four-manifest audit. Browser emitted zero console errors (one non-error warning). This evidence covers one completed Private Pilot VFR path and the three score bands, not IFR/other certificate combinations, print delivery, native touch or actual 200% zoom.

## FlightRisk print summary in the packaged preview

Chromium's actual print-to-PDF path for the completed LOW assessment first generated two Letter pages. Visual inspection showed the summary and signature on page one, with the shared `#site-footer` alone on page two. A first source print-CSS candidate hid the footer but retained `min-h-[100dvh]` flex shell layout and bottom dock padding, leaving the signature on a second page. A browser-only CSS probe confirmed that removing those screen layout constraints reduced the PDF to one page. The final authored `src/index.css` print rules scope that behavior to `body.flightrisk-view`, hide the direct site footer, set print padding to zero, and use a block, non-viewport-height shell. Screen styles are unaffected.

After rebuilding the standalone FlightRisk package, a fresh browser loaded `index-Dg3SZKKw.js` and `index-CD9KpcSV.css` from the isolated site, restored the synthetic VFR state, and printed the current LOW and HIGH results. `output/pdf/flightrisk-low-vfr-print.pdf` and `output/pdf/flightrisk-high-vfr-print.pdf` are each one US Letter page by `pdfinfo`. `pdftotext` found LOW/HIGH risk, the complete seven-row score breakdown, disclaimer and PIC signature; the HIGH copy also includes top drivers, mitigations and all three experience auto-flags. Poppler renders `output/pdf/flightrisk-low-vfr-print.png` and `flightrisk-high-vfr-print.png` were inspected: all content fits, the semantic green/red chips are readable, and no footer or clipped section remains. A browser probe replaced `window.print` temporarily, clicked the desktop Print summary button, and observed exactly one invocation. The real native print dialog/device delivery was not exercised.

The final ten FlightRisk assets match source `dist` and the 17-file source patch dry-applies to the preserved tactile baseline. Ten superseded assets from each of the two print iterations were SHA-verified and archived outside deployment at `flightrisk-print-footer-20260925` and `flightrisk-print-shell-20260925`; the stale-asset scanner returned zero candidates. Shared sync updated zero HTML sources; cache refresh and the 523+6+4 route/supporting/manifest audit passed. The print fix does not change risk calculations, data formats, on-screen layout or exported artwork.

## FlightRisk rated-Private IFR completion and reload

The final packaged preview was opened in a fresh 834×1112 browser profile under System/Night. Selected Instrument (IFR), Rated and Private Pilot, then confirmed the profile. Synthetic total/type/PIC/30-day/90-day/night/actual-instrument hours were 500/100/300/12/50/80/30, producing no automatic flags. The six section completion controls filled only unanswered items with No; in IFR Flight Plan, Precision +0 was explicitly selected before completing the section. The Night tablet approach row showed a metallic-gold selected Precision control beside three gray alternatives, with existing red/amber point labels; see `output/playwright/flightrisk-ifr-approach-night-tablet.png`.

The final score was 0/LOW. Day and Night 834px result-dialog screenshots (`output/playwright/flightrisk-ifr-low-day-tablet.png` and `flightrisk-ifr-low-night-tablet.png`) show the semantic green result, opaque reading surface, gold Print summary and gray Back to edit/New assessment. Reload restored IFR, Rated, confirmed profile, all six reviewed sections, 30 instrument hours, LOW and the checked Precision approach. At 320px Night, the approach grid displayed two rows of two controls with Precision gold (`output/playwright/flightrisk-ifr-approach-night-phone.png`), and the completed result dialog fit with its action bar (`flightrisk-ifr-low-night-phone.png`); document scroll width equaled the 320px viewport. The browser reported zero console errors (one non-error warning). No source change was needed for this path. Other certificate/risk combinations and native touch/zoom remain open.

## FOI Cards selected section chooser

At 390px Night, the active All remaining pass initially opened the Study a section dialog with every row on raised gray and no selected indicator (`output/playwright/foi-section-dialog-night-390.png`). The authored `foi-cards/app.js` now marks only the current remaining-card pass with `.current` and `aria-current=true`; `foi-cards/styles.css` gives that row the shared gold gradient, bronze inset rim and dark key ink without changing its dimensions. The browser computed Night All remaining as gold-gradient `rgb(43,35,22)` text, with the next Human behavior row gray `rgb(244,244,244)` text. Screenshot `output/playwright/foi-section-dialog-selected-night-390.png` was visually inspected. The canonical gradient/ink audit's minimum text ratio remains 4.54:1.

Choosing Learning started its existing 70-card pass without changing the overall 238 left-to-memorize count; reopening the dialog moved the gold state to Learning. Day 390px and final Day/Night 320px images (`output/playwright/foi-section-dialog-learning-day-390.png`, `foi-section-dialog-learning-day-320-final.png`, `foi-section-dialog-learning-night-320-final.png`) show the current row within the viewport, with gray alternatives and the existing dock. At 320px Night, document scroll width equaled 320px. Reload exposed Resume pass; after resuming, the Learning selection and 1-of-70 pass survived. Opening the chooser before resuming showed zero `aria-current` rows, avoiding a false active marker on the welcome screen. The loaded URLs rotated to `styles.css?v=1864517b8a` and `app.js?v=d1cb6bb9a1`. FOI dialog tests passed 3/3; the 238-card/eight-page/20-anchor content audit, 22 shared Python tests, 109 contrast pairs, 523+6+4 wiring and diff whitespace checks passed. No card content, progress, rating behavior or layout changed.

## Aero Lab expanded wind tunnel materials

At desktop 1280×720, the wind tunnel's Expand control entered browser full-screen presentation. The Air motion mode in Day (`output/playwright/aero-expanded-air-day-desktop.png`) and Night (`aero-expanded-night-desktop.png`) displayed gold selected teaching/overlay keys, gray secondary keys and the original blue/green airflow visualization. Changing to Pressure before expanding preserved the selected Pressure, Surface pressure arrows and Chord & angle gold controls in Night (`aero-expanded-pressure-night-desktop.png`), while the red pressure visualization and blue/red scale retained their scientific roles. Browser snapshot reported the Pressure/overlay controls as pressed. The full-screen document width equaled the 1280px viewport; activating Expand again exited full screen. When resized to 320px Night, the app intentionally showed its desktop handoff with a gold Copy link for your desktop action (`aero-expanded-night-phone-320.png`). This was a rendered material check, not a numerical aerodynamics validation; no source or model data changed.

## Shared expanded-menu current link

At local `/` in Day, the open Explore menu's `Home Current` row computed no background image, dark text, and a light gray outline; `output/playwright/nav-menu-before-day.png` shows it beside the already gold selected Explore tab and dock Home key. The selected System appearance button was independently checked and already had the gold gradient, so it did not need a change. The final shared CSS gives only `.liquid-panel > a[aria-current]` the canonical `--g-gold`, `--g-edge` and `--g-key-ink`, including its `::after` Current label.

In a fresh browser after synchronization, the Day Home row and label both computed `rgb(43,35,22)` over the seven-stop gold gradient (`nav-menu-after-day.png`). Switching through the actual Appearance menu to Night left the same gradient and ink on `#414141` with no document overflow (`nav-menu-after-night.png`). At 390×844 Night, the menu stayed inside x=12–378 and the current row inside x=32–358; document width was 390 (`nav-menu-after-night-phone.png`). Direct navigation to `/learn/private-pilot/` in that Night phone session set the Learn dock key and Explore link current; the link remained gold on hover. The browser reported zero console errors. Shared Python tests passed 22/22, contrast audit 109/109, asset audit 523+6+4, and Git whitespace check was clean. Native touch, actual browser zoom and live OS changes remain open.

At 320×720 Night on the same nested Learn route, the expanded menu remained within x=12–308, its gold Learn row within x=32–288, and document width equaled the 320px viewport; `output/playwright/nav-menu-learn-night-320.png` was visually checked. This narrow-viewport observation is not an actual 200% browser-zoom test.

With the root font size set to 32px as a separate text-enlargement probe at 320×720 Night, reopening the menu activated its vertical-tab layout. The content scrolled from 0 to its 217px maximum, revealing every Explore link and the gold Learn Current row without horizontal overflow (`nav-menu-learn-night-320-large-text.png` and `nav-menu-learn-night-320-large-text-scrolled.png`). Escape closed the dialog and returned focus to Menu. This probe does not establish actual browser-zoom behavior.

## Enlarged text in the shared dock and homepage hero

At 320×720 with a 32px root font, CertPath's pre-fix dock rendered Profile/Hours/Plan as overlapping labels (`output/playwright/certpath-intro-320-large-text.png`); Profile and Hours each had scroll width greater than their 48px client width. The shared `.liquid-dock` phone label now uses a 12–15px clamp. A fresh final browser measured all five visible CertPath controls at scroll width equal to client width, retained the gold Goal selection, gray remaining keys, 320px document width, and the System preference following Day/Night browser schemes. Final images: `output/playwright/certpath-dock-320-large-text-final-day.png` and `certpath-dock-320-large-text-final-night.png`. The public homepage dock also had no overflowing visible key at the same viewport/root size.

The homepage hero's pre-fix 32px-root heading computed 81.6px at 320px, and “With confidence.” scrolled to 354px inside a 276px line box, clipping the word. A viewport-width cap in `assets/home-avionics.css` now keeps the normal 320px/16px-root headline at its existing 40.8px, while a 32px root yields 44.8px and wraps the entire phrase within 276px. Fresh Day/Night images `output/playwright/home-hero-320-large-text-final-day.png` and `home-hero-320-large-text-final-night.png` show the full heading, gold primary and gray secondary actions, and 320px document width. At 390px Night and 32px root, the headline is 54.6px and both spans' scroll widths equal their 346px client widths (`home-hero-390-large-text-final-night.png`); normal 390px remains 43.2px. The existing sky images and animation code were not changed. These are root-font enlargement checks, not native 200% browser zoom. Final shared tests 22/22, canonical contrast 109/109, asset wiring 523+6+4 and whitespace checks pass.

## Standalone mobile controls with enlarged text

At `/simply-endorsed-cfi/?view=tasks&task=first-solo`, 320×720 and a 32px root font, the before image `output/playwright/simply-first-solo-320-large-text-day.png` shows the four app-owned dock labels colliding. The active workspace stylesheet's `.se-mobile-nav` font was `.66rem` (about 21px at that root size). After bounding only those labels to 11–15px, a fresh System/Day browser measured Tasks, Library, Guidance and Menu at scroll width 68px/client width 68px, with the selected Tasks background still the canonical seven-stop gold gradient (`simply-first-solo-320-large-text-after-day.png`). System/Night retained the same bounds, `#414141` canvas, dark selected ink and gray inactive labels (`simply-first-solo-320-large-text-after-night.png`). The orange Student pilot task heading and checklist content were unchanged. Its Night menu remained a 282px-wide dialog within x=19–301, with 913px content in a 670px scrollport (`simply-menu-320-large-text-night.png`); Escape closed it and focused Menu. The page never exceeded 320px width.

At `/foi-cards/` in the same Night/320px enlarged-text session, the welcome Start remaining cards action stayed gold (`foi-welcome-320-large-text-night.png`). Beginning the real 238-card pass showed the long Card 1 question within the viewport; flipping showed an opaque answer and 48px high Review, Next and Memorized controls in two rows (`foi-card-320-large-text-night.png`, `foi-answer-320-large-text-night.png`). Review retained amber, Memorized green, and Next raised gray; the lower CTA could be reached by page scroll (`foi-card-320-large-text-night-bottom.png`). No card/status data was edited by the style work.

At `/pilotsolve/` in Night/320px with the same 32px root, the Tools tiles and bottom dock fit without document overflow (`pilotsolve-320-large-text-night.png`). Calculator Numbers selection remained gold and numeric keys gray (`pilotsolve-calculator-320-large-text-night.png`). Its `.pilot-scroll` had 311px of available vertical scroll; at the bottom the gold equals key sat above the fixed dock (`pilotsolve-calculator-320-large-text-night-scrolled.png`). The keypad calculation `1+2=` displayed `3`. No PilotSolve source changed in this pass. The Simply Endorsed workspace suite passed 1,678 assertions, the shared Python suite 22/22, canonical contrast 109/109, route/supporting/manifest audit 523+6+4 and Git whitespace checks. These are bounded root-font checks, not native 200% zoom or touch acceptance.

At `/aero-lab/` in System/Day and System/Night at 320×720 with the 32px root, the desktop handoff's 32px heading, explanation and gold Copy link for your desktop action stayed within the 320px document width (`output/playwright/aero-handoff-320-large-text-day.png`). The action initially extended below the fold behind the fixed dock, but normal page scrolling moved its full 268×106px bounds to y=266–372, clear of the dock (`aero-handoff-320-large-text-day-scrolled.png`, `aero-handoff-320-large-text-night-scrolled.png`). The copy action changed to Link copied and exposed the status “Aero Lab link copied to clipboard.” The desktop simulation and its science/geometry were not involved in this phone handoff observation. No source edit was needed.

## FlightRisk and Certificate Generator at enlarged phone text

FlightRisk `/flight-risk-assessment/` initially had 522px document width at 320×720/32px root. The Saved Setup form's CSS grid track auto-expanded to 457px, and the instrument-rating group's 160px minimum-width choices overran a 132px slot. Source-owned zero-minimum grid tracks, phone-stacked rating choices, and wrapping card headers brought the fresh packaged route to exactly 320px in System/Day and explicit Night. `output/playwright/flightrisk-saved-320-root32-day.png`, `flightrisk-rating-320-root32-day.png`, and `flightrisk-rating-320-root32-night-settled.png` were reviewed; the selected button is gold with dark text in both modes, and the stacked choices are 72–80px high. Normal 16px-root Night also fits the viewport. The app logic and risk colors were not changed. FlightRisk's 27 source tests and TypeScript/Vite build pass, the 18-file source patch dry-applies, and all ten previous generated assets were hash-archived outside deployment at `/Users/diegosuarez/Projects/suarez-aurum-archives/flightrisk-text-fit-20260925/` with no stale candidates remaining.

Certificate Generator `/certificate-generator/` first reached 383px document width at Step 1 under the same 320px/32px-root probe; the app's `1fr` main grid carried its min-content width. Changing the grid to `minmax(0,1fr)` kept Step 1 at 320px. After choosing First Solo, Step 2 reached 387px from its `1fr` name-field rows and intrinsic input widths; zero-minimum field tracks and contained inputs restored a 320px width. Synthetic Ada Student/Alex Instructor names advanced through optional Photo to Review. At Step 4 the old two-button bar's Download JPEG text collided with Back and extended 3px horizontally; the narrow-phone flex-wrap/label rule now places them on separate 280px-wide rows when text is enlarged, with both above the bottom dock. Fresh authored CSS delivered Steps 1–4 at 320px; Review's 1080×1920 canvas dimensions and enabled download/share controls remained. `output/playwright/certgen-review-320-root32-night.png` was visually reviewed with metallic gold Download and gray Back, plus the current gray page. At a normal 16px root, the 320px buttons remain side by side. This uses one synthetic path and does not claim JPEG/device delivery or actual 200% browser zoom.

The Night name-entry validation path was also exercised at 320px/32px root. Clearing the synthetic student surname and pressing Continue kept Step 2 active, set `aria-invalid=true` on that input, focused it within x=41–279, and retained 320px document width. `output/playwright/certgen-names-invalid-320-root32-night.png` shows the red validation edge and gold focus ring without clipping. The remaining inputs and shared dock stayed reachable; no app logic was changed for this check.

After both fixes, `scripts/sync-avionics.py`, cache refresh, and `scripts/audit-aurum.py` passed (523 routes, six supporting documents, four manifests). The shared Python suite passed 22/22, FlightRisk 27/27, Simply Endorsed 1,678 assertions, and the canonical contrast audit 109/109 with minimum text ratio 4.54:1; Git diff whitespace was clean. Browser sessions had zero console errors and one existing non-error warning per route. All results remain in the isolated preview; production was untouched.

## Crank & Core collection stage with enlarged text

At `/engine-explorer/app/` in Day/320px/32px root, the pinned collection's introduction grew to 608px while its absolute preview and model tap area stayed at 320px from the stage top, covering the copy and both engine choices. The editable `engine-explorer/app/assets/avionics-overlay.css` now uses a text-relative preview offset and grows the stage by the same amount; the decorative engine title and specs follow the preview without changing the pinned scene or model assets. A first selector lost to the runtime's more specific `.app.collection:has(.category-picker):not(.fi-app) .viewport` declaration and was corrected before final verification. A subsequent breakpoint sweep found the same collision from 401px through desktop sizes; the final offset scales across all collection widths while preserving the original 16px-root stage height and preview position.

Fresh final measurements at 320px showed the normal 16px-root stage still 820px high, with a 29px intro-to-title gap and 12px specs-to-action gap. At 24px root the stage was 1020px with 81px/15px gaps; at 32px it was 1220px with 16px title, 90px hit-area and 20px specs/action gaps. Document width remained 320px. `output/playwright/engine-explorer-320-root32-day-final-top.png`, `engine-explorer-320-root32-day-final-model.png` and `engine-explorer-320-root32-night-final-model.png` were visually reviewed: the full introduction precedes the engine title and 3D model, and the gold Explore engine button no longer masks specs. Switching to Lycoming updated the selected engine URL/state while preserving the same positive geometry and Night materials. Only presentation CSS and its shell cache hash changed; 523+6+4 route wiring passed. This is a collection-stage text-resize check, not native zoom, model-science validation or full explorer acceptance.

After the final fluid-offset rule, a fresh 16px/32px-root matrix at widths 320, 360, 390, 401, 620, 621, 834 and 1280px found document width equal to viewport width in all 16 cases. The 16px-root stage stayed 820px high at each width; 32px-root stages ranged from 980px at tablet/desktop to 1220px at 320px. All intro-to-title and intro-to-hit-area gaps were positive; enlarged-text specs-to-action gaps were 7–48px. The pre-existing normal tablet/desktop specs and action boxes overlap vertically by 38px but remain in separate horizontal areas; that geometry is unchanged at 16px root. The final Night 834px Lycoming screenshot (`engine-explorer-834-root32-night-final-tablet.png`) shows the introduction, selected engine control, decorative title and 3D stage in sequence. The final fluid formula, not the earlier narrower phone probe, is the current implementation.

## Tools directory active stage with enlarged text

At `/tools/` in Day/320px/32px root, the PilotSolve stage title ended at x=309 while its card ended at x=304 (`output/playwright/tools-320-root32-day-initial.png`). The original `.app-stage h2` mobile declaration later in `assets/tool-catalog.css` overrode an initial cap candidate; after moving the rule to the end of the authored mobile block, a fresh browser loaded the final stylesheet hash and computed 32px instead of 52.8px for the enlarged title. All seven app names ended within the stage identity's x=283 boundary at 320px and x=353 at 390px; document widths matched both viewports. A normal 16px root still computed the prior 26.4px title. `output/playwright/tools-320-root32-night-pilotsolve-final.png` was visually reviewed: PilotSolve name, solid gray reading copy, gold Open PilotSolve action and selected Tools dock fit without clipping. In Night/390px/32px root, selecting Training and instruction gave its filter the canonical metallic gradient, while the Next orbit arrow retained a raised-gray gradient; stepping selected Simply Endorsed CFI without overflow. The app identities and directory data were untouched. This is a focused text-fit and material-state check, not native 200% browser zoom or complete keyboard/touch acceptance.

## Learn library Browse view with enlarged text

At `/learn/` in Day/320px/32px root, both Browse tabs initially shared one 280px row, but the Topics button's text scroll width was 111px inside a 93px client width; `output/playwright/learn-tabs-320-root32-day.png` and `learn-topics-320-root32-night.png` showed its label extending beyond the gray/gold material. The Learn-owned phone rule now uses flex wrapping and max-content flex basis with zero minimum width. At 320px/32px root, both buttons occupy complete 270px rows with scroll width equal to client width; at 240px/32px root they fit 190px rows without page overflow. A normal 320px/16px-root comparison retained the prior 238px tab group and side-by-side 141px/82px buttons. The final Night screenshot `output/playwright/learn-topics-320-root32-night-final.png` shows the selected Topics gold tab, raised-gray Training paths tab and category cards on a solid gray reading surface.

The same Night session filled the search field with a synthetic no-match term. The live status reported zero results; the focused search input stayed x=20–300 and the Clear filters button x=20–224 with raised-gray background. Clicking Clear restored `/learn/` and the unfiltered library. No content or search logic changed. Shared sync refreshed 478 HTML sources using the new Learn style hash; cache refresh and the 523+6+4 integration audit passed. These are bounded text-resize and empty-search checks, not native 200% browser zoom, all article variants, or full keyboard/touch acceptance.

## Blog directory and article with enlarged phone text

At `/blog/` with a 320×720 viewport and 32px root font, the category group, featured story, card grid, hero and lower reference action stayed within the 320px document. Selecting Medical & Certificates wrapped its label inside a 280px-wide gold button and updated the live status to two articles without horizontal overflow (`output/playwright/blog-category-320-root32-day.png`). The other category labels also wrapped within their gray controls. No directory style change was needed.

At `/blog/private-pilot-cost-louisville-ky/` under the same text enlargement, System/Night applied the `#414141` canvas and matching browser theme color. The title and introduction remained readable within x=20–300 (`output/playwright/blog-article-intro-320-root32-night.png`). Its three-column cost table intentionally retained a 1120px internal width inside a 278px scrollport while the document stayed 320px wide. Focusing the labeled table and pressing ArrowRight moved its internal scroll position from 0 to 40px (`blog-article-table-320-root32-night.png` shows the scrolled state). This is a single article and table interaction sample; it does not establish every article variant, native touch, or actual 200% browser zoom.

## Service split panels and form at enlarged phone text

At `/discovery-flight-louisville-ky/` in a 320×720 viewport with a 32px root font, four of five split panels exceeded their 278px inner width by 5–54px while the enclosing card clipped the excess. The lower Read the full walkthrough first panel's copy and Call or text Diego / Plan full training actions ended beyond the visible right edge even though document width was 320px. The phone rule in `learn/style.css` now allows headings, paragraphs, list items and links inside split panels to wrap at the available width; a viewport cap keeps the enlarged heading at 35.2px so walkthrough stays whole instead of splitting midword. At a normal 16px root, that heading remains its prior 24px.

A fresh final browser loaded the updated Learn stylesheet and measured all five Discovery Flight split panels at scroll width = client width (278px) in System/Night; the lower actions ended at x=271 within the x=20–300 card. The selected primary action used the canonical gold gradient and the secondary used raised gray (`output/playwright/discovery-panel-320-root32-night-final.png`, `discovery-actions-320-root32-night-final.png`). System/Day kept the `#E3E3E3` canvas and browser theme color; the panel fit at 320px and 390px with the 32px root. An empty contact-form submit showed a 222px-wide validation message within the card, and the gold Open email draft action stayed inside the viewport (`discovery-form-error-320-root32-day-final.png`). No email draft was opened or message sent.

The other six service routes—Flight Training, Ground School, Written Test Prep, Checkride Oral Prep, Remote Ground Instruction, and Private Pilot Ground School—were loaded in System/Night at 320px/32px root. Every split panel and its headings reported zero internal horizontal overflow and every document remained 320px wide. This is a shared-layout containment sweep, not full-page visual or form acceptance for those routes. After the final CSS edit, content sync refreshed 478 HTML files, cache refresh and the 523+6+4 route/supporting/manifest audit passed; 22 shared tests, 109 contrast pairs (minimum 4.54:1), and Git whitespace checks passed. Actual browser 200% zoom and native touch remain open.

## Editorial FAQ enlargement across service routes

At `/southern-indiana-flight-training/` in Day/320×720 with a 32px root font, the long Jeffersonville question exceeded its 220px collapsed FAQ card by 19px and was clipped by the card's hidden overflow. The shared editorial phone rule now caps summary text at 7.5vw when the root size is larger, keeps the normal 16px-root summary at 16px, and allows long words to wrap. The final Day screenshot `output/playwright/southern-indiana-faq-320-root32-day-final.png` shows Jeffersonville? fully within its card. A Night click expanded the same answer on a solid gray FAQ surface; the card and answer stayed 220px wide, the page stayed 320px wide, and the browser theme color matched `#414141` (`southern-indiana-faq-320-root32-night-final.png`).

A follow-up sweep found an opened Flight Training FAQ answer with a 266px text width inside the same 220px card. The phone rule now also permits answer text to wrap. In a fresh browser, all FAQ items were opened in turn on Southern Indiana plus all seven service pages at 320px/32px root: 34 questions and their answers reported no internal horizontal overflow, and every page stayed 320px wide. This is a geometry/content-containment sweep; individual answer reading order, screen-reader output and native 200% browser zoom remain open. After the final rule, sync refreshed 478 HTML sources, cache and 523+6+4 route/supporting/manifest audit passed; 22 shared tests, 109 contrast pairs (minimum 4.54:1), and Git whitespace checks passed.
