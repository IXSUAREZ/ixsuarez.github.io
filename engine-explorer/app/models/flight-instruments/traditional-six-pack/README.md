# Traditional six-pack reference package

Generated with `npm run model:instruments`. Editable source is in `src/flight-instruments/`.

`bench.glb` and its gzip copy are static authoring snapshots. The application generates the same named geometry and drives its indications from the shared simulation. This is an original reconstructed instructional model. Exact physical geometry acceptance is held; see `manifest.json` and `docs/flight-instruments/`.
