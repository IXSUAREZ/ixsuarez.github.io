# Independent app authoring changes

This redesign uses isolated authoring copies. The canonical Studio sources remain at `/Volumes/VIXIONDRIVE/VIBE CODING PROJECTS/Suarez CFI Studio/Projects/Flight Tools/`. The paths below were compared by file hash against their corresponding canonical files, excluding generated builds, dependencies, temporary files, and reference datasets. No large source tree is duplicated in this document.

## PilotSolve

- Isolated source: `/Users/diegosuarez/Projects/suarez-avionics-work/sources/pilotsolve`
- Canonical comparison source: `PilotSolve/` under the Studio path above
- Changed existing files: `mobile.html`, `public/manifest.webmanifest`, `scripts/build-offline.mjs`, `scripts/prepare-suarezcfi.mjs`, `src/prototype.css`, `src/PilotApp.tsx`
- Added files: `public/apple-touch-icon.png`, `public/icon-48.png`, `tests/site-offline-worker.test.mjs`
- Changes: shared first-paint appearance bootstrap; Dark/Day/System and Solid controls; contextual Tools/Trip/Aircraft/Menu dock; matte avionics styling; retained saved-data schema, calculations, numeric input behavior, and offline capability. The site preparation script precaches the shared appearance stylesheet/script and generates a cache ID from their bytes.
- Build: from the isolated source, `npm run build:app`, then `node scripts/prepare-suarezcfi.mjs /Users/diegosuarez/Projects/suarez-avionics-work/site`. A final shared-asset-only cache refresh also ran `node scripts/build-offline.mjs` before the site preparation script.
- Deployed local preview output: `/Users/diegosuarez/Projects/suarez-avionics-work/site/pilotsolve/`.
- Current local cache: `pilotsolve-site-422b3fe1c6e0`. The service-worker precache URLs were verified locally before the final shared stylesheet update; the cache was then regenerated from the final local shared files and its two offline worker tests passed. The worker intercepts only `/pilotsolve/` and the two exact shared appearance URLs; unrelated `/assets/` paths remain outside this worker. This cache has not been publicly deployed.
- Validation: 34 source tests and 2 offline-worker tests passed, TypeScript and production build passed. Browser QA confirmed the four dock destinations and exact appearance labels. Physical iPhone Safari and externally published production were not tested.

## Crank & Core

- Isolated source: `/Users/diegosuarez/Projects/suarez-avionics-work/sources/crank-core`
- Canonical comparison source: `Crank & Core/` under the Studio path above
- Changed existing files: `public/factory-reference/index.html`, `public/factory-reference/reference.css`
- Added files: `src/avionics.css`, `scripts/baseline-overlay.js`, `scripts/baseline-overlay-extra.css`, `scripts/prepare-baseline-overlay.mjs`
- The isolated Studio source contains newer, untracked instrument study code and 15 development model assets that were **not** in the pre-redesign published app. Rebuilding that source directly would promote those unrelated model and content changes, so `npm run build` is **not** the release input. The release pins the complete published baseline runtime and its model corpus, then adds only presentation chrome. Native baseline React buttons remain mounted and own view/calculation behavior; the bottom dock delegates to them. `index.html`, `src/main.tsx`, and `src/ExplorerChrome.tsx` in the isolated copy match canonical Studio again; the release adapter lives in `scripts/baseline-overlay.js`.
- Reproducible preparation: from the isolated source run `node scripts/prepare-baseline-overlay.mjs`. It copies `/Users/diegosuarez/Projects/suarez-avionics-work/baseline/site/engine-explorer/app/` into `/Users/diegosuarez/Projects/suarez-avionics-work/site/engine-explorer/app/`, then adds first-paint shared appearance, the contextual dock adapter/CSS, and the themed factory-reference shell. Do not replace this step with `npm run build` until the Studio source and released model corpus have been reconciled separately.
- The 2026-09-22 visual follow-up added a semantic Dark/Day loading-surface override to the isolated source `scripts/baseline-overlay-extra.css` and regenerated the deployed-local overlay CSS. The pinned runtime and model corpus remain unchanged.
- Deployed local preview output: `/Users/diegosuarez/Projects/suarez-avionics-work/site/engine-explorer/app/`. The parent `/engine-explorer/` collection shell owns the website dock outside exploration; the child dock appears only while exploring.
- Baseline protection: all 23 deployed model files and all existing bundled JS/CSS/assets match the pre-redesign baseline byte-for-byte. The model-tree SHA-256 is `89ce44dda81e4451f624a66434f9d9adb195ee623b015c4ee43d14f6d4c71fe9` for both baseline and local preview. The only output differences are `index.html`, `factory-reference/index.html`, `factory-reference/reference.css`, and added `assets/avionics-overlay.js`/`.css`. No new instrument assets or calculations ship.
- Validation: 22 focused navigation/camera/simulation/catalog tests passed in the isolated source. The original full geometry/provenance suite cannot run completely in the lightweight isolated copy because large `references/` and `output/` datasets were excluded. The dock overlay passed JavaScript syntax and delegated-click smoke checks. Lead browser QA confirmed the pinned engine explorer and six-pack views, one child dock, Menu, Day switching, Escape dismissal, and Parts delegation.

## Shared adapter verification

`/Users/diegosuarez/Projects/suarez-avionics-work/site/scripts/tests/avionics-navigation.test.cjs` adds five jsdom tests for delegated wizard prerequisites, Simply Endorsed rerenders, menu focus/dismissal, duplicate inclusion, embedded appearance storage events, and one navigation owner per layer. It and the existing five appearance tests passed together (10/10).

## September 23 tool presentation follow-up

The local preview was updated in the authoring sources and rebuilt without changing calculations, saved-work formats, source content, or certificate export geometry:

| Tool | Authoring change | Preview output |
| --- | --- | --- |
| Simply Endorsed | `site/simply-endorsed/js/app.js` and `site/assets/tool-system/simply-endorsed.css`: clearer result counts, search field, card titles, and FAA reference surfaces in both appearances. | Live site CSS/JS; no generated bundle. |
| CertPath | `site/simply-endorsed/js/part61-calculator-ui.js` and `site/part-61-calculator/storage.css`: active step semantics and readable saved-plan/contact surfaces. | Live site CSS/JS; no generated bundle. |
| FlightRisk | `flightrisk-app/src/components/assessment/ScoreBar.tsx`: label live versus complete scores and improve mobile action focus. | Built with `FLIGHTRISK_BASE=flight-risk-assessment npm run build`, then copied its static `dist` into `site/flight-risk-assessment/`. |
| FOI Cards | `site/foi-cards/index.html`, `app.js`, `styles.css`: explicit study actions, accessible pass progress, responsive action layout. | Live site files; no generated bundle. |
| PilotSolve | `sources/pilotsolve/src/PilotApp.tsx` and `src/prototype.css`: visible calculation-result counts. | `npm run build:app`, then `node scripts/build-offline.mjs` and `node scripts/prepare-suarezcfi.mjs /Users/diegosuarez/Projects/suarez-avionics-work/site`. |
| Aero Lab | `sources/aero-lab/src/components/DesktopGate.tsx`, `SiteNavigation.tsx`/`.css`, and `src/App.tsx`: clearer device guidance, lesson-control labels, five-item desktop/four-item compact dock, and grouped lab/site menu actions. | `npm run build:web`, then copied its static `dist` into `site/aero-lab/`. |
| Crank & Core | `sources/crank-core/scripts/baseline-overlay.js` and `baseline-overlay-extra.css`: group exploration actions and improve menu focus. `site/engine-explorer/index.html` gives the collection iframe its baseline 746px height and ordinary wrapper scroll on short windows. | `node scripts/prepare-baseline-overlay.mjs`; 44 JS bundles and 23 model files remain byte-identical to the published baseline. |
| Certificate Generator | `site/certificate-generator/js/certificate-generator.js` and `site/assets/tool-system/certgen.css`: visible preview readiness and clearer builder steps. | Live site CSS/JS; canvas dimensions and export path untouched. |

Focused checks: FOI content audit passed for 238 cards; PilotSolve protected runtime check, 34 calculation/session tests, and 6 offline-worker tests passed; Aero Lab 14 tests passed; FlightRisk 26 tests passed; Crank & Core 5 navigation tests passed. The Simply Endorsed and CertPath wizard suite passed 98/98 cases, covering calculations, validation, draft restoration, print, and export safeguards. The PilotSolve local offline cache after the final shared stylesheet change is `pilotsolve-site-422b3fe1c6e0`; its worker tests pass. The lead's browser pass covers rendered interactions and viewports; the checks above are not a claim of physical-device, print, or hardware testing.

The short-window follow-up changed `flightrisk-app/src/components/methodology/SectionTables.tsx` and `primitives.tsx` so the scoring-table navigation stays in normal flow on a matte surface and table text and points render immediately. `HeroSection.tsx` and `src/components/guide/GuideHero.tsx`, `SectionHeading.tsx`, `BandMeaning.tsx`, `GuideFaq.tsx`, `SafetyCta.tsx`, and `WorkedExample.tsx` remove delayed entry reveals from headings and article content. Rebuild with the same `FLIGHTRISK_BASE=flight-risk-assessment npm run build` command, then copy `dist/` to `site/flight-risk-assessment/` while excluding the worker scaffolding. The route-prefixed build and 26 FlightRisk tests passed again. `sources/crank-core/scripts/baseline-overlay-extra.css` lifts the collection's Explore control on short desktop frames; regenerate using only `node scripts/prepare-baseline-overlay.mjs`. This remains a CSS-only overlay over the pinned runtime.

The Simply Endorsed detail screenshot exposed a selected row with a pale background and light site-theme text. The rendered lookup is owned by `site/simply-endorsed/js/workspace.css`; its selected row, detail citation link, and category active states now use semantic raised/ink/cyan colors in Dark and Day. The workspace stylesheet URL advances to `v=7` for fresh browser loading. No endorsement content or lookup behavior changed.

### Blueprint loader follow-up

The preview overlay now imports the schematic SVG and animation timing from the canonical Studio `src/EngineLoading.tsx` and `src/collection.css`. The public bundle fetched during this change (`index-aicmwaz5.js`, `index-wTZkXEb-.css`) still contained the older spinner; the latest Studio source was used rather than claiming the fetched public bundle contained the blueprint. `scripts/baseline-overlay.js` adds a presentation child only while the pinned runtime's `.loading-model` exists. The source overlay CSS hides duplicate loading copy and the oversized background title, uses the site's semantic surfaces, and retains static reduced-motion artwork. Existing native loading completion and error handling own removal; no artificial loading delay was added.

The generated preview `assets/avionics-overlay.js` and `.css` were refreshed using the same concatenation/copy steps as `prepare-baseline-overlay.mjs`, without recopying the runtime or icons. The lifecycle test `scripts/tests/crank-blueprint.test.cjs` passes, and all 44 baseline JavaScript bundles and 23 model files match their pinned bytes. Browser checks observed the blueprint for both Rotax and Lycoming and the ready Explore state after loading.

The subsequent refinement replaces the collection page's full-width bottom study-model disclosure with a compact `About models` button and responsive dialog, authored in `site/engine-explorer/model-info.css` and `.js`. The loading overlay in `sources/crank-core/scripts/baseline-overlay.js` and `baseline-overlay-extra.css` now draws the existing blueprint in a short staged reveal, uses an indeterminate waiting cue, and fades out when the native model finishes loading; reduced motion uses a static, immediate handoff. Refresh only the generated overlay JS/CSS, preserving the pinned runtime and model corpus. Desktop/phone Dark and Day dialog flows and the ready-model transition were visually checked; the blueprint and shared-navigation tests pass.
